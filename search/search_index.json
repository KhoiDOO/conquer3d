{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Conquer3D","text":"<p>Welcome to the Conquer3D documentation! </p> <p>Conquer3D is a highly efficient Geometric CUDA Tool Box for Python. </p>"},{"location":"#getting-started","title":"Getting Started","text":"<p>To explore the library, please check out the Examples in the navigation bar to see interactive Jupyter Notebooks demonstrating mesh creation, ray intersections, and more.</p> <p>For detailed class and method documentation, see the API Reference.</p>"},{"location":"api/creation/","title":"Creation API","text":""},{"location":"api/creation/#conquer3d.creation","title":"<code>conquer3d.creation</code>","text":""},{"location":"api/creation/#conquer3d.creation.__all__","title":"<code>conquer3d.creation.__all__ = ['create_sphere']</code>  <code>module-attribute</code>","text":""},{"location":"api/creation/#conquer3d.creation.create_sphere","title":"<code>conquer3d.creation.create_sphere(sectors: int = 32, stacks: int = 16, radius: float = 1.0) -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Creates a UV sphere returning (vertices, triangles) tensors on the CPU.</p> <p>Parameters:</p> <ul> <li> <code>sectors</code>               (<code>int</code>, default:                   <code>32</code> )           \u2013            <p>Number of longitudinal sectors. Defaults to 32.</p> </li> <li> <code>stacks</code>               (<code>int</code>, default:                   <code>16</code> )           \u2013            <p>Number of latitudinal stacks. Defaults to 16.</p> </li> <li> <code>radius</code>               (<code>float</code>, default:                   <code>1.0</code> )           \u2013            <p>Radius of the sphere. Defaults to 1.0.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>vertices</code> (              <code>Tensor</code> )          \u2013            <p>(N, 3) float32 tensor of vertex positions.</p> </li> <li> <code>triangles</code> (              <code>Tensor</code> )          \u2013            <p>(M, 3) int32 tensor of triangle indices.</p> </li> </ul> Source code in <code>conquer3d/creation/triangle_creation.py</code> <pre><code>def create_sphere(\n    sectors: int = 32,\n    stacks: int = 16,\n    radius: float = 1.0\n) -&gt; Tuple[torch.Tensor, torch.Tensor]:\n    \"\"\"\n    Creates a UV sphere returning (vertices, triangles) tensors on the CPU.\n\n    Args:\n        sectors (int, optional): Number of longitudinal sectors. Defaults to 32.\n        stacks (int, optional): Number of latitudinal stacks. Defaults to 16.\n        radius (float, optional): Radius of the sphere. Defaults to 1.0.\n\n    Returns:\n        vertices: (N, 3) float32 tensor of vertex positions.\n        triangles: (M, 3) int32 tensor of triangle indices.\n    \"\"\"\n    return create_sphere_c(sectors, stacks, radius)\n</code></pre>"},{"location":"api/data_structure/bvh/","title":"BVH","text":""},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH","title":"<code>conquer3d.data_structure.BVH</code>","text":"<p>               Bases: <code>pybind11_builtins.pybind11_object</code></p> <p>A highly efficient Bounding Volume Hierarchy (LBVH) data structure natively backed by CUDA.</p>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.__module__","title":"<code>conquer3d.data_structure.BVH.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.__init__","title":"<code>conquer3d.data_structure.BVH.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.BVH, in_aabb_mins: torch.Tensor, in_aabb_maxs: torch.Tensor) -&gt; None</p> <p>Construct and build the Karras LBVH from Gaussian AABBs.</p> <p>Parameters:</p> <ul> <li> <code>in_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of AABB minimum coordinates.</p> </li> <li> <code>in_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of AABB maximum coordinates.</p> </li> </ul>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.BVH._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.query","title":"<code>conquer3d.data_structure.BVH.query() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query(self: conquer3d._C.BVH, query_aabb_mins: torch.Tensor, query_aabb_maxs: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Query the BVH with bounding boxes or segments.</p> <p>Parameters:</p> <ul> <li> <code>query_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of query AABB minimum coordinates.</p> </li> <li> <code>query_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of query AABB maximum coordinates.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(query_ids, object_ids)</p> </li> </ul>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.query_point","title":"<code>conquer3d.data_structure.BVH.query_point() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_point(self: conquer3d._C.BVH, query_points: torch.Tensor) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Find the closest leaf AABB to each query point.</p> <p>Parameters:</p> <ul> <li> <code>query_points</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of query points.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor]</code> )          \u2013            <p>(query_ids, object_ids, distances)</p> </li> </ul>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.query_ray","title":"<code>conquer3d.data_structure.BVH.query_ray() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_ray(self: conquer3d._C.BVH, ray_origins: torch.Tensor, ray_dirs: torch.Tensor, max_capacity: int = 10000000) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Find all ray-AABB intersections.</p> <p>Parameters:</p> <ul> <li> <code>ray_origins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of ray origins.</p> </li> <li> <code>ray_dirs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of ray directions.</p> </li> <li> <code>max_capacity</code>               (<code>int</code>)           \u2013            <p>Maximum global capacity for recording intersections. Defaults to BVH_MAX_CAPACITY.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(ray_ids, object_ids)</p> </li> </ul>"},{"location":"api/data_structure/bvh/#conquer3d.data_structure.BVH.query_self","title":"<code>conquer3d.data_structure.BVH.query_self() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_self(self: conquer3d._C.BVH) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Query the BVH against itself.</p> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(query_ids, object_ids) representing unique overlapping pairs.</p> </li> </ul>"},{"location":"api/data_structure/gsbvh/","title":"GSBVH","text":""},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH","title":"<code>conquer3d.data_structure.GSBVH</code>","text":"<p>               Bases: <code>conquer3d._C.BVH</code></p> <p>A specialized Bounding Volume Hierarchy for Gaussian Splatting operations. Inherits from BVH.</p>"},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH.__module__","title":"<code>conquer3d.data_structure.GSBVH.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH.__init__","title":"<code>conquer3d.data_structure.GSBVH.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.GSBVH, in_aabb_mins: torch.Tensor, in_aabb_maxs: torch.Tensor) -&gt; None</p> <p>Construct and build the Karras LBVH for Gaussians.</p> <p>Parameters:</p> <ul> <li> <code>in_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian AABB minimums.</p> </li> <li> <code>in_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian AABB maximums.</p> </li> </ul>"},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.GSBVH._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH.query_edge","title":"<code>conquer3d.data_structure.GSBVH.query_edge() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_edge(self: conquer3d._C.GSBVH, edge_starts: torch.Tensor, edge_ends: torch.Tensor, means: torch.Tensor, opacities: torch.Tensor, covis: torch.Tensor, isos: Union[float, torch.Tensor] = 11.345) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Find the single highest-density Gaussian intersected by each line segment.</p> <p>Parameters:</p> <ul> <li> <code>edge_starts</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge start points.</p> </li> <li> <code>edge_ends</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge end points.</p> </li> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian means.</p> </li> <li> <code>opacities</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N,) float32 tensor of Gaussian opacities.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 6) float32 tensor of Gaussian inverse covariances.</p> </li> <li> <code>isos</code>               (<code>float | Tensor</code>)           \u2013            <p>Iso-surface threshold. Defaults to ISO.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(hit_mask, gaussian_ids)</p> </li> </ul>"},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH.query_edge_pair","title":"<code>conquer3d.data_structure.GSBVH.query_edge_pair() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_edge_pair(self: conquer3d._C.GSBVH, edge_starts: torch.Tensor, edge_ends: torch.Tensor, means: torch.Tensor, covis: torch.Tensor, isos: Union[float, torch.Tensor] = 11.345, max_capacity: int = 10000000) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Find all Gaussians intersected by each line segment.</p> <p>Parameters:</p> <ul> <li> <code>edge_starts</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge start points.</p> </li> <li> <code>edge_ends</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge end points.</p> </li> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian means.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 6) float32 tensor of Gaussian inverse covariances.</p> </li> <li> <code>isos</code>               (<code>float | Tensor</code>)           \u2013            <p>Iso-surface threshold. Can be a float or (N,) tensor. Defaults to ISO.</p> </li> <li> <code>max_capacity</code>               (<code>int</code>)           \u2013            <p>Max global capacity for hits. Defaults to BVH_MAX_CAPACITY.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor]</code> )          \u2013            <p>(hit_mask, edge_ids, gaussian_ids)</p> </li> </ul>"},{"location":"api/data_structure/gsbvh/#conquer3d.data_structure.GSBVH.query_voxel_pair","title":"<code>conquer3d.data_structure.GSBVH.query_voxel_pair() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor | None, torch.Tensor | None]</code>  <code>method descriptor</code>","text":"<p>query_voxel_pair(self: conquer3d._C.GSBVH, vx_aabb_mins: torch.Tensor, vx_aabb_maxs: torch.Tensor, means: torch.Tensor, covis: torch.Tensor, opacities: torch.Tensor, gs_aabb_mins: torch.Tensor, gs_aabb_maxs: torch.Tensor, contact_points: torch.Tensor, isos: Union[float, torch.Tensor] = 11.345, ar_threshold: float = 0.0, p_threshold: float = 0.0, return_centroids: bool = False, max_capacity: int = 10000000) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, Optional[torch.Tensor], Optional[torch.Tensor]]</p> <p>Perform exact Broad-to-Narrow phase intersection between Voxels and Gaussians.</p> <p>Parameters:</p> <ul> <li> <code>vx_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of voxel AABB minimums.</p> </li> <li> <code>vx_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of voxel AABB maximums.</p> </li> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian means.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 6) float32 tensor of Gaussian inverse covariances.</p> </li> <li> <code>opacities</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N,) float32 tensor of Gaussian opacities.</p> </li> <li> <code>gs_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian AABB minimums.</p> </li> <li> <code>gs_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of Gaussian AABB maximums.</p> </li> <li> <code>contact_points</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of contact points.</p> </li> <li> <code>isos</code>               (<code>float | Tensor</code>)           \u2013            <p>Iso-surface threshold. Can be a float or (N,) float32 tensor. Defaults to ISO.</p> </li> <li> <code>ar_threshold</code>               (<code>float</code>)           \u2013            <p>Alpha scaling threshold. Defaults to 0.0.</p> </li> <li> <code>p_threshold</code>               (<code>float</code>)           \u2013            <p>Probability threshold. Defaults to 0.0.</p> </li> <li> <code>return_centroids</code>               (<code>bool</code>)           \u2013            <p>Whether to compute and return intersection centroids/densities. Defaults to false.</p> </li> <li> <code>max_capacity</code>               (<code>int</code>)           \u2013            <p>Max global capacity for hits. Defaults to BVH_MAX_CAPACITY.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor, Tensor | None, Tensor | None]</code> )          \u2013            <p>(hit_mask, voxel_ids, gaussian_ids, centroids, densities)</p> </li> </ul>"},{"location":"api/data_structure/kdtree/","title":"KDTree","text":""},{"location":"api/data_structure/kdtree/#conquer3d.data_structure.KDTree","title":"<code>conquer3d.data_structure.KDTree</code>","text":"<p>               Bases: <code>pybind11_builtins.pybind11_object</code></p> <p>A highly efficient KDTree data structure natively backed by CUDA.</p>"},{"location":"api/data_structure/kdtree/#conquer3d.data_structure.KDTree.__module__","title":"<code>conquer3d.data_structure.KDTree.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/kdtree/#conquer3d.data_structure.KDTree.__init__","title":"<code>conquer3d.data_structure.KDTree.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.KDTree, points: torch.Tensor) -&gt; None</p> <p>Initializes and builds the KDTree on the GPU.</p> <p>Parameters:</p> <ul> <li> <code>points</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of points to build the KDTree.</p> </li> </ul>"},{"location":"api/data_structure/kdtree/#conquer3d.data_structure.KDTree._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.KDTree._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/kdtree/#conquer3d.data_structure.KDTree.query","title":"<code>conquer3d.data_structure.KDTree.query() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query(self: conquer3d._C.KDTree, query_points: torch.Tensor, k: int = 1, exclude_self: bool = False) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Queries the KDTree for the K nearest neighbors.</p> <p>Parameters:</p> <ul> <li> <code>query_points</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of query points.</p> </li> <li> <code>k</code>               (<code>int</code>)           \u2013            <p>Number of nearest neighbors to search for. Defaults to 1.</p> </li> <li> <code>exclude_self</code>               (<code>bool</code>)           \u2013            <p>Whether to exclude the exact query point (if it exists). Defaults to false.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(distances, indices)</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/","title":"MeshBVH","text":""},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH","title":"<code>conquer3d.data_structure.MeshBVH</code>","text":"<p>               Bases: <code>conquer3d._C.BVH</code></p> <p>A specialized Bounding Volume Hierarchy for Triangle Meshes. Inherits from BVH.</p>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.__module__","title":"<code>conquer3d.data_structure.MeshBVH.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.__init__","title":"<code>conquer3d.data_structure.MeshBVH.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.MeshBVH, in_aabb_mins: torch.Tensor, in_aabb_maxs: torch.Tensor) -&gt; None</p> <p>Construct MeshBVH from Triangle AABBs.</p> <p>Parameters:</p> <ul> <li> <code>in_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of triangle AABB minimums.</p> </li> <li> <code>in_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of triangle AABB maximums.</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.MeshBVH._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.build_winding_data","title":"<code>conquer3d.data_structure.MeshBVH.build_winding_data() -&gt; None</code>  <code>method descriptor</code>","text":"<p>build_winding_data(self: conquer3d._C.MeshBVH, vertices: torch.Tensor, triangles: torch.Tensor) -&gt; None</p> <p>Computes the hierarchical area-weighted constants for Fast Winding Number queries.</p> <p>Parameters:</p> <ul> <li> <code>vertices</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of vertices.</p> </li> <li> <code>triangles</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) int32 tensor of triangles.</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.get_ray_intersection","title":"<code>conquer3d.data_structure.MeshBVH.get_ray_intersection() -&gt; typing.Any</code>  <code>method descriptor</code>","text":"<p>get_ray_intersection(self: conquer3d._C.MeshBVH, ray_origins: torch.Tensor, ray_dirs: torch.Tensor, vertices: torch.Tensor, triangles: torch.Tensor, return_distance: bool = False) -&gt; object</p> <p>Find all ray-triangle intersections.</p> <p>Parameters:</p> <ul> <li> <code>ray_origins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (R, 3) float32 tensor of ray origins.</p> </li> <li> <code>ray_dirs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (R, 3) float32 tensor of ray directions.</p> </li> <li> <code>vertices</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of vertices.</p> </li> <li> <code>triangles</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) int32 tensor of triangles.</p> </li> <li> <code>return_distance</code>               (<code>bool</code>)           \u2013            <p>Whether to compute and return ray hit distances. Defaults to false.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>Any</code> )          \u2013            <p>(ray_ids, triangle_ids, intersect_points, [distances])</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.get_self_intersection","title":"<code>conquer3d.data_structure.MeshBVH.get_self_intersection() -&gt; torch.Tensor</code>  <code>method descriptor</code>","text":"<p>get_self_intersection(self: conquer3d._C.MeshBVH, vertices: torch.Tensor, triangles: torch.Tensor) -&gt; torch.Tensor</p> <p>Find all self-intersecting triangle pairs.</p> <p>Parameters:</p> <ul> <li> <code>vertices</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of vertices.</p> </li> <li> <code>triangles</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) int32 tensor of triangles.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor: Shape (K, 2) int64 tensor of intersecting triangle index pairs.</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.is_self_intersection","title":"<code>conquer3d.data_structure.MeshBVH.is_self_intersection() -&gt; bool</code>  <code>method descriptor</code>","text":"<p>is_self_intersection(self: conquer3d._C.MeshBVH, vertices: torch.Tensor, triangles: torch.Tensor) -&gt; bool</p> <p>Check if there are any self-intersecting triangle pairs.</p> <p>Parameters:</p> <ul> <li> <code>vertices</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of vertices.</p> </li> <li> <code>triangles</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) int32 tensor of triangles.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>bool</code> (              <code>bool</code> )          \u2013            <p>True if there is at least one self-intersection.</p> </li> </ul>"},{"location":"api/data_structure/mesh_bvh/#conquer3d.data_structure.MeshBVH.query_point","title":"<code>conquer3d.data_structure.MeshBVH.query_point() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_point(self: conquer3d._C.MeshBVH, query_points: torch.Tensor, vertices: torch.Tensor, triangles: torch.Tensor, return_sdf: bool = False, return_prj_pts: bool = True, sign_mode: int = 0) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Find the closest triangle to each query point.</p> <p>Parameters:</p> <ul> <li> <code>query_points</code>               (<code>Tensor</code>)           \u2013            <p>Shape (Q, 3) float32 tensor of query points.</p> </li> <li> <code>vertices</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of vertices.</p> </li> <li> <code>triangles</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) int32 tensor of triangles.</p> </li> <li> <code>return_sdf</code>               (<code>bool</code>)           \u2013            <p>Whether to compute signed distances instead of unsigned. Defaults to false.</p> </li> <li> <code>return_prj_pts</code>               (<code>bool</code>)           \u2013            <p>Whether to compute and return projected points. Defaults to true.</p> </li> <li> <code>sign_mode</code>               (<code>int</code>)           \u2013            <p>Winding number method (0 for ray casting, 1 for fast winding number). Defaults to 0.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor, Tensor]</code> )          \u2013            <p>(query_ids, triangle_ids, projected_points, distances)</p> </li> </ul>"},{"location":"api/data_structure/pgsbvh/","title":"PGSBVH","text":""},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH","title":"<code>conquer3d.data_structure.PGSBVH</code>","text":"<p>               Bases: <code>conquer3d._C.BVH</code></p> <p>A specialized Bounding Volume Hierarchy for Planar Gaussian Splatting (PGS) operations. Inherits from BVH.</p>"},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH.__module__","title":"<code>conquer3d.data_structure.PGSBVH.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH.__init__","title":"<code>conquer3d.data_structure.PGSBVH.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.PGSBVH, in_aabb_mins: torch.Tensor, in_aabb_maxs: torch.Tensor) -&gt; None</p> <p>Construct and build the Karras LBVH for Planar Gaussians.</p> <p>Parameters:</p> <ul> <li> <code>in_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS AABB minimums.</p> </li> <li> <code>in_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS AABB maximums.</p> </li> </ul>"},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.PGSBVH._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH.query_edge","title":"<code>conquer3d.data_structure.PGSBVH.query_edge() -&gt; tuple[torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_edge(self: conquer3d._C.PGSBVH, edge_starts: torch.Tensor, edge_ends: torch.Tensor, means: torch.Tensor, normals: torch.Tensor, opacities: torch.Tensor, covis: torch.Tensor, isos: Union[float, torch.Tensor] = 11.345) -&gt; tuple[torch.Tensor, torch.Tensor]</p> <p>Find the single highest-density PGS intersected by each line segment.</p> <p>Parameters:</p> <ul> <li> <code>edge_starts</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge start points.</p> </li> <li> <code>edge_ends</code>               (<code>Tensor</code>)           \u2013            <p>Shape (E, 3) float32 tensor of edge end points.</p> </li> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS means.</p> </li> <li> <code>normals</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS normals.</p> </li> <li> <code>opacities</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N,) float32 tensor of PGS opacities.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS inverse covariances.</p> </li> <li> <code>isos</code>               (<code>float | Tensor</code>)           \u2013            <p>Iso-surface threshold. Defaults to ISO.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor]</code> )          \u2013            <p>(hit_mask, gaussian_ids)</p> </li> </ul>"},{"location":"api/data_structure/pgsbvh/#conquer3d.data_structure.PGSBVH.query_voxel_pair","title":"<code>conquer3d.data_structure.PGSBVH.query_voxel_pair() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor | None, torch.Tensor | None]</code>  <code>method descriptor</code>","text":"<p>query_voxel_pair(self: conquer3d._C.PGSBVH, vx_aabb_mins: torch.Tensor, vx_aabb_maxs: torch.Tensor, means: torch.Tensor, normals: torch.Tensor, covis: torch.Tensor, gs_aabb_mins: torch.Tensor, gs_aabb_maxs: torch.Tensor, isos: Union[float, torch.Tensor] = 11.345, return_centroids: bool = False, return_centroid_densities: bool = False, max_capacity: int = 10000000) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, Optional[torch.Tensor], Optional[torch.Tensor]]</p> <p>Perform exact Broad-to-Narrow phase intersection between Voxels and PGS.</p> <p>Parameters:</p> <ul> <li> <code>vx_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of voxel AABB minimums.</p> </li> <li> <code>vx_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M, 3) float32 tensor of voxel AABB maximums.</p> </li> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS means.</p> </li> <li> <code>normals</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS normals.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS inverse covariances.</p> </li> <li> <code>gs_aabb_mins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS AABB minimums.</p> </li> <li> <code>gs_aabb_maxs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (N, 3) float32 tensor of PGS AABB maximums.</p> </li> <li> <code>isos</code>               (<code>float | Tensor</code>)           \u2013            <p>Iso-surface threshold. Can be a float or (N,) float32 tensor. Defaults to ISO.</p> </li> <li> <code>return_centroids</code>               (<code>bool</code>)           \u2013            <p>Whether to compute and return intersection centroids. Defaults to false.</p> </li> <li> <code>return_centroid_densities</code>               (<code>bool</code>)           \u2013            <p>Whether to return densities at centroids. Defaults to false.</p> </li> <li> <code>max_capacity</code>               (<code>int</code>)           \u2013            <p>Max global capacity for hits. Defaults to 10000000.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor, Tensor | None, Tensor | None]</code> )          \u2013            <p>(hit_mask, voxel_ids, gaussian_ids, centroids, densities)</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/","title":"TriangleMesh","text":""},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh","title":"<code>conquer3d.data_structure.TriangleMesh</code>","text":"<p>               Bases: <code>pybind11_builtins.pybind11_object</code></p> <p>A highly efficient Triangle Mesh data structure natively backed by CUDA.</p> <p>Parameters:</p> <ul> <li> <code>in_vertices</code>               (<code>Tensor</code>)           \u2013            <p>A tensor of shape (N, 3) containing float32 vertex coordinates.</p> </li> <li> <code>in_triangles</code>               (<code>Tensor</code>)           \u2013            <p>A tensor of shape (M, 3) containing int32 triangle indices.</p> </li> <li> <code>in_vertex_normals</code>               (<code>Tensor</code>)           \u2013            <p>A tensor of shape (N, 3) containing float32 vertex normals. Defaults to None.</p> </li> <li> <code>in_vertex_colors</code>               (<code>Tensor</code>)           \u2013            <p>A tensor of shape (N, 3) containing float32 vertex colors. Defaults to None.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.__module__","title":"<code>conquer3d.data_structure.TriangleMesh.__module__ = 'conquer3d._C'</code>  <code>class-attribute</code>","text":"<p>str(object='') -&gt; str str(bytes_or_buffer[, encoding[, errors]]) -&gt; str</p> <p>Create a new string object from the given object. If encoding or errors is specified, then the object must expose a data buffer that will be decoded using the given encoding and error handler. Otherwise, returns the result of object.str() (if defined) or repr(object). encoding defaults to sys.getdefaultencoding(). errors defaults to 'strict'.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.bvh","title":"<code>conquer3d.data_structure.TriangleMesh.bvh: MeshBVH</code>  <code>property</code>","text":"<p>The Bounding Volume Hierarchy built for this mesh.</p> <p>Returns:</p> <ul> <li> <code>MeshBVH</code>           \u2013            <p>MeshBVH - The Bounding Volume Hierarchy built for this mesh.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.edge_to_triangle_counts","title":"<code>conquer3d.data_structure.TriangleMesh.edge_to_triangle_counts: torch.Tensor</code>  <code>property</code>","text":"<p>Edge to triangle counts.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Edge to triangle counts.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.edge_to_triangle_indices","title":"<code>conquer3d.data_structure.TriangleMesh.edge_to_triangle_indices: torch.Tensor</code>  <code>property</code>","text":"<p>Edge to triangle indices.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Edge to triangle indices.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.edge_to_triangle_offsets","title":"<code>conquer3d.data_structure.TriangleMesh.edge_to_triangle_offsets: torch.Tensor</code>  <code>property</code>","text":"<p>Edge to triangle offsets.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Edge to triangle offsets.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.edges","title":"<code>conquer3d.data_structure.TriangleMesh.edges: torch.Tensor</code>  <code>property</code>","text":"<p>Unique edges of the mesh.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (E, 2) int32 tensor of unique edges.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.euler_characteristic","title":"<code>conquer3d.data_structure.TriangleMesh.euler_characteristic: int</code>  <code>property</code>","text":"<p>The Euler characteristic (V - E + F) of the mesh.</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>int - The Euler characteristic (V - E + F) of the mesh.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.genus","title":"<code>conquer3d.data_structure.TriangleMesh.genus: int</code>  <code>property</code>","text":"<p>The topological genus of the mesh.</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>int - The topological genus of the mesh.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.num_triangles","title":"<code>conquer3d.data_structure.TriangleMesh.num_triangles: int</code>  <code>property</code>","text":"<p>Total number of triangles.</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>int - Total number of triangles.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.surface_area","title":"<code>conquer3d.data_structure.TriangleMesh.surface_area: torch.Tensor</code>  <code>property</code>","text":"<p>Total surface area of the mesh.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Total surface area of the mesh.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.triangle_areas","title":"<code>conquer3d.data_structure.TriangleMesh.triangle_areas: torch.Tensor</code>  <code>property</code>","text":"<p>Areas of each triangle.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (M,) float32 tensor of triangle areas.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.triangle_normals","title":"<code>conquer3d.data_structure.TriangleMesh.triangle_normals: torch.Tensor</code>  <code>property</code>","text":"<p>Normals of each triangle.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (M, 3) float32 tensor of triangle normals.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.triangles","title":"<code>conquer3d.data_structure.TriangleMesh.triangles: torch.Tensor</code>  <code>property</code>","text":"<p>Mesh triangles.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (M, 3) int32 tensor of triangles.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertex_colors","title":"<code>conquer3d.data_structure.TriangleMesh.vertex_colors: torch.Tensor</code>  <code>property</code>","text":"<p>Vertex colors.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (N, 3) float32 tensor of vertex colors.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertex_normals","title":"<code>conquer3d.data_structure.TriangleMesh.vertex_normals: torch.Tensor</code>  <code>property</code>","text":"<p>Area-weighted vertex normals.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (N, 3) float32 tensor of vertex normals.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertex_to_triangle_counts","title":"<code>conquer3d.data_structure.TriangleMesh.vertex_to_triangle_counts: torch.Tensor</code>  <code>property</code>","text":"<p>Vertex to triangle counts.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Vertex to triangle counts.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertex_to_triangle_indices","title":"<code>conquer3d.data_structure.TriangleMesh.vertex_to_triangle_indices: torch.Tensor</code>  <code>property</code>","text":"<p>Vertex to triangle indices.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Vertex to triangle indices.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertex_to_triangle_offsets","title":"<code>conquer3d.data_structure.TriangleMesh.vertex_to_triangle_offsets: torch.Tensor</code>  <code>property</code>","text":"<p>Vertex to triangle offsets.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Vertex to triangle offsets.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.vertices","title":"<code>conquer3d.data_structure.TriangleMesh.vertices: torch.Tensor</code>  <code>property</code>","text":"<p>Mesh vertices coordinates.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor - Shape (N, 3) float32 tensor of vertices.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.__init__","title":"<code>conquer3d.data_structure.TriangleMesh.__init__() -&gt; None</code>  <code>method descriptor</code>","text":"<p>init(self: conquer3d._C.TriangleMesh, in_vertices: torch.Tensor, in_triangles: torch.Tensor, in_vertex_normals: Optional[torch.Tensor] = None, in_vertex_colors: Optional[torch.Tensor] = None) -&gt; None</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh._pybind11_conduit_v1_","title":"<code>conquer3d.data_structure.TriangleMesh._pybind11_conduit_v1_()</code>  <code>method descriptor</code>","text":""},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.build_bvh","title":"<code>conquer3d.data_structure.TriangleMesh.build_bvh() -&gt; MeshBVH</code>  <code>method descriptor</code>","text":"<p>build_bvh(self: conquer3d._C.TriangleMesh) -&gt; conquer3d._C.MeshBVH</p> <p>Builds and returns the Bounding Volume Hierarchy for the mesh.</p> <p>Returns:</p> <ul> <li> <code>MeshBVH</code> (              <code>MeshBVH</code> )          \u2013            <p>The constructed BVH object.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.compute_edges_to_triangle_map","title":"<code>conquer3d.data_structure.TriangleMesh.compute_edges_to_triangle_map() -&gt; None</code>  <code>method descriptor</code>","text":"<p>compute_edges_to_triangle_map(self: conquer3d._C.TriangleMesh) -&gt; None</p> <p>Computes the edge-to-triangle connectivity map.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.compute_triangle_areas","title":"<code>conquer3d.data_structure.TriangleMesh.compute_triangle_areas() -&gt; None</code>  <code>method descriptor</code>","text":"<p>compute_triangle_areas(self: conquer3d._C.TriangleMesh) -&gt; None</p> <p>Computes the areas of all triangles.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.compute_triangle_normals","title":"<code>conquer3d.data_structure.TriangleMesh.compute_triangle_normals() -&gt; None</code>  <code>method descriptor</code>","text":"<p>compute_triangle_normals(self: conquer3d._C.TriangleMesh) -&gt; None</p> <p>Computes the normals for all triangles.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.compute_vertex_normals","title":"<code>conquer3d.data_structure.TriangleMesh.compute_vertex_normals() -&gt; None</code>  <code>method descriptor</code>","text":"<p>compute_vertex_normals(self: conquer3d._C.TriangleMesh) -&gt; None</p> <p>Computes area-weighted vertex normals.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.compute_vertices_to_triangle_map","title":"<code>conquer3d.data_structure.TriangleMesh.compute_vertices_to_triangle_map() -&gt; None</code>  <code>method descriptor</code>","text":"<p>compute_vertices_to_triangle_map(self: conquer3d._C.TriangleMesh) -&gt; None</p> <p>Computes the vertex-to-triangle connectivity map.</p>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.get_edges_to_triangle_map","title":"<code>conquer3d.data_structure.TriangleMesh.get_edges_to_triangle_map() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>get_edges_to_triangle_map(self: conquer3d._C.TriangleMesh) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Gets the edge to triangle connectivity mapping.</p> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor, Tensor]</code> )          \u2013            <p>(edges, offsets, counts, indices)</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.get_non_manifold_vertices","title":"<code>conquer3d.data_structure.TriangleMesh.get_non_manifold_vertices() -&gt; torch.Tensor</code>  <code>method descriptor</code>","text":"<p>get_non_manifold_vertices(self: conquer3d._C.TriangleMesh) -&gt; torch.Tensor</p> <p>Gets all non-manifold vertices.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor: Tensor of vertex indices that are non-manifold.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.get_ray_intersection","title":"<code>conquer3d.data_structure.TriangleMesh.get_ray_intersection() -&gt; typing.Any</code>  <code>method descriptor</code>","text":"<p>get_ray_intersection(self: conquer3d._C.TriangleMesh, ray_origins: torch.Tensor, ray_dirs: torch.Tensor, return_distance: bool = False) -&gt; object</p> <p>Computes intersections between a batch of rays and the mesh triangles.</p> <p>Parameters:</p> <ul> <li> <code>ray_origins</code>               (<code>Tensor</code>)           \u2013            <p>Shape (R, 3) float32 tensor of ray origins.</p> </li> <li> <code>ray_dirs</code>               (<code>Tensor</code>)           \u2013            <p>Shape (R, 3) float32 tensor of ray directions.</p> </li> <li> <code>return_distance</code>               (<code>bool</code>)           \u2013            <p>If True, returns intersection distances. Defaults to False.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>Any</code> )          \u2013            <p>(ray_ids, triangle_ids, intersect_points, [distances])</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.get_self_intersection","title":"<code>conquer3d.data_structure.TriangleMesh.get_self_intersection() -&gt; torch.Tensor</code>  <code>method descriptor</code>","text":"<p>get_self_intersection(self: conquer3d._C.TriangleMesh) -&gt; torch.Tensor</p> <p>Finds all self-intersecting triangle pairs in the mesh.</p> <p>Returns:</p> <ul> <li> <code>Tensor</code>           \u2013            <p>torch.Tensor: A tensor of shape (K, 2) containing pairs of intersecting triangle indices.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.get_vertices_to_triangle_map","title":"<code>conquer3d.data_structure.TriangleMesh.get_vertices_to_triangle_map() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>get_vertices_to_triangle_map(self: conquer3d._C.TriangleMesh) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Gets the vertex to triangle connectivity mapping.</p> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor]</code> )          \u2013            <p>(offsets, counts, indices)</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.is_edge_manifold","title":"<code>conquer3d.data_structure.TriangleMesh.is_edge_manifold() -&gt; bool</code>  <code>method descriptor</code>","text":"<p>is_edge_manifold(self: conquer3d._C.TriangleMesh, allow_boundary_edge: bool = True) -&gt; bool</p> <p>Checks if the mesh is edge manifold.</p> <p>Parameters:</p> <ul> <li> <code>allow_boundary_edge</code>               (<code>bool</code>)           \u2013            <p>Whether to permit boundary edges (count &lt;= 2). Defaults to True.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>bool</code> (              <code>bool</code> )          \u2013            <p>True if the mesh is edge manifold.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.is_manifold","title":"<code>conquer3d.data_structure.TriangleMesh.is_manifold() -&gt; bool</code>  <code>method descriptor</code>","text":"<p>is_manifold(self: conquer3d._C.TriangleMesh, allow_boundary_edge: bool = True) -&gt; bool</p> <p>Checks if the mesh is fully manifold (edge, vertex, and no self-intersections).</p> <p>Parameters:</p> <ul> <li> <code>allow_boundary_edge</code>               (<code>bool</code>)           \u2013            <p>Whether to permit boundary edges. Defaults to True.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>bool</code> (              <code>bool</code> )          \u2013            <p>True if fully manifold.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.is_self_intersection","title":"<code>conquer3d.data_structure.TriangleMesh.is_self_intersection() -&gt; bool</code>  <code>method descriptor</code>","text":"<p>is_self_intersection(self: conquer3d._C.TriangleMesh) -&gt; bool</p> <p>Checks whether the mesh contains any self-intersecting triangles.</p> <p>Returns:</p> <ul> <li> <code>bool</code> (              <code>bool</code> )          \u2013            <p>True if there is at least one self-intersection, False otherwise.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.is_vertex_manifold","title":"<code>conquer3d.data_structure.TriangleMesh.is_vertex_manifold() -&gt; bool</code>  <code>method descriptor</code>","text":"<p>is_vertex_manifold(self: conquer3d._C.TriangleMesh) -&gt; bool</p> <p>Checks if the mesh is vertex manifold.</p> <p>Returns:</p> <ul> <li> <code>bool</code> (              <code>bool</code> )          \u2013            <p>True if all vertices are manifold.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.query_points","title":"<code>conquer3d.data_structure.TriangleMesh.query_points() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</code>  <code>method descriptor</code>","text":"<p>query_points(self: conquer3d._C.TriangleMesh, query_pts: torch.Tensor, return_sdf: bool = False, return_prj_pts: bool = True, sign_mode: int = 0, distance_mode: int = 0) -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor]</p> <p>Finds the closest triangles and computes distances/SDFs for query points.</p> <p>Parameters:</p> <ul> <li> <code>query_pts</code>               (<code>Tensor</code>)           \u2013            <p>Shape (Q, 3) float32 tensor of query points.</p> </li> <li> <code>return_sdf</code>               (<code>bool</code>)           \u2013            <p>Whether to return Signed Distance Field values. Defaults to False.</p> </li> <li> <code>return_prj_pts</code>               (<code>bool</code>)           \u2013            <p>Whether to return projected points on the mesh. Defaults to True.</p> </li> <li> <code>sign_mode</code>               (<code>int</code>)           \u2013            <p>The method for computing signs. Defaults to 0.</p> </li> <li> <code>distance_mode</code>               (<code>int</code>)           \u2013            <p>Distance computation mode. Defaults to 0.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor, Tensor]</code> )          \u2013            <p>(query_ids, triangle_ids, projected_points, distances)</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.remove_triangles_by_mask","title":"<code>conquer3d.data_structure.TriangleMesh.remove_triangles_by_mask() -&gt; None</code>  <code>method descriptor</code>","text":"<p>remove_triangles_by_mask(self: conquer3d._C.TriangleMesh, keep_mask: torch.Tensor) -&gt; None</p> <p>Removes triangles from the mesh based on a boolean mask.</p> <p>Parameters:</p> <ul> <li> <code>keep_mask</code>               (<code>Tensor</code>)           \u2013            <p>Shape (M,) boolean tensor indicating which triangles to keep.</p> </li> </ul>"},{"location":"api/data_structure/triangle_mesh/#conquer3d.data_structure.TriangleMesh.sample_points","title":"<code>conquer3d.data_structure.TriangleMesh.sample_points() -&gt; tuple[torch.Tensor, torch.Tensor, torch.Tensor | None, torch.Tensor | None]</code>  <code>method descriptor</code>","text":"<p>sample_points(self: conquer3d._C.TriangleMesh, num_points: int, uniform: bool = False, return_normals: bool = False, return_colors: bool = False, use_triangle_normal: bool = True) -&gt; tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor], Optional[torch.Tensor]]</p> <p>Samples random points on the surface of the mesh.</p> <p>Parameters:</p> <ul> <li> <code>num_points</code>               (<code>int</code>)           \u2013            <p>The number of points to sample.</p> </li> <li> <code>uniform</code>               (<code>bool</code>)           \u2013            <p>If True, samples uniformly by area. Defaults to False.</p> </li> <li> <code>return_normals</code>               (<code>bool</code>)           \u2013            <p>If True, returns normals at sampled points. Defaults to False.</p> </li> <li> <code>return_colors</code>               (<code>bool</code>)           \u2013            <p>If True, returns colors at sampled points. Defaults to False.</p> </li> <li> <code>use_triangle_normal</code>               (<code>bool</code>)           \u2013            <p>If True, uses flat triangle normals instead of interpolated vertex normals. Defaults to True.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>tuple</code> (              <code>tuple[Tensor, Tensor, Tensor | None, Tensor | None]</code> )          \u2013            <p>(points, triangle_indices, [normals], [colors])</p> </li> </ul>"},{"location":"api/primitive/gs/","title":"Gaussian Splatting (gs)","text":""},{"location":"api/primitive/gs/#conquer3d.primitive.gs","title":"<code>conquer3d.primitive.gs</code>","text":""},{"location":"api/primitive/gs/#conquer3d.primitive.gs.compute_gs_aabb","title":"<code>conquer3d.primitive.gs.compute_gs_aabb(means: torch.Tensor, scales: torch.Tensor, covis: torch.Tensor, level: int, iso: Union[float, torch.Tensor] = 11.345, tol: float = 1.0 / 8.0) -&gt; Tuple[torch.Tensor, torch.Tensor, torch.Tensor]</code>","text":"<p>Computes the Axis-Aligned Bounding Box (AABB) for 3D Gaussians in world space.</p> <p>Parameters:</p> <ul> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of Gaussian center positions.</p> </li> <li> <code>scales</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of scale factors.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>(N, 6) tensor of covariance inverses (flattened upper-triangular).</p> </li> <li> <code>level</code>               (<code>int</code>)           \u2013            <p>Octree subdivision level. Resolution will be at :math:<code>2^level</code></p> </li> <li> <code>iso</code>               (<code>float</code>, default:                   <code>11.345</code> )           \u2013            <p>The opacity cutoff threshold. Defaults to 11.345.</p> </li> <li> <code>tol</code>               (<code>float</code>, default:                   <code>1.0 / 8.0</code> )           \u2013            <p>Safety tolerance multiplier. Defaults to 0.125.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>aabb_min</code> (              <code>Tensor</code> )          \u2013            <p>torch.Tensor: (N, 3) tensor of AABB minimum corners.</p> </li> <li> <code>aabb_max</code> (              <code>Tensor</code> )          \u2013            <p>torch.Tensor: (N, 3) tensor of AABB maximum corners.</p> </li> <li> <code>contact_points</code> (              <code>Tensor</code> )          \u2013            <p>torch.Tensor: (N, 9) tensor of contact points on the AABB surface.</p> </li> </ul> Source code in <code>conquer3d/primitive/gs.py</code> <pre><code>def compute_gs_aabb(\n    means: torch.Tensor,\n    scales: torch.Tensor,\n    covis: torch.Tensor,\n    level: int,\n    iso: Union[float, torch.Tensor] = 11.345,\n    tol: float = 1. / 8.\n) -&gt; Tuple[torch.Tensor, torch.Tensor, torch.Tensor]:\n    \"\"\"\n    Computes the Axis-Aligned Bounding Box (AABB) for 3D Gaussians in world space.\n\n    Args:\n        means (torch.Tensor): (N, 3) tensor of Gaussian center positions.\n        scales (torch.Tensor): (N, 3) tensor of scale factors.\n        covis (torch.Tensor): (N, 6) tensor of covariance inverses (flattened upper-triangular).\n        level (int, optional): Octree subdivision level. Resolution will be at :math:`2^level`\n        iso (float, optional): The opacity cutoff threshold. Defaults to 11.345.\n        tol (float, optional): Safety tolerance multiplier. Defaults to 0.125.\n\n    Returns:\n        aabb_min: torch.Tensor: (N, 3) tensor of AABB minimum corners.\n        aabb_max: torch.Tensor: (N, 3) tensor of AABB maximum corners.\n        contact_points: torch.Tensor: (N, 9) tensor of contact points on the AABB surface.\n    \"\"\"\n\n    if not all(t.is_cuda for t in [means, scales, covis]):\n        raise ValueError(\"All input tensors must be CUDA tensors.\")\n\n    means_c = means.contiguous().to(torch.float32)\n    scales_c = scales.contiguous().to(torch.float32)\n    covis_c = covis.contiguous().to(torch.float32)\n\n    if isinstance(iso, torch.Tensor):\n        if not iso.is_cuda: raise ValueError(\"iso tensor must be on CUDA\")\n        isos = iso.contiguous().to(torch.float32)\n        iso = 0.0\n    else:\n        isos = None\n        iso = float(iso)\n\n    aabb_min, aabb_max, contact_points = compute_gs_aabb_func(\n        means_c,\n        scales_c,\n        covis_c,\n        isos,\n        iso,\n        tol,\n        level\n    )\n\n    return aabb_min, aabb_max, contact_points\n</code></pre>"},{"location":"api/primitive/gs/#conquer3d.primitive.gs.compute_gs_covi","title":"<code>conquer3d.primitive.gs.compute_gs_covi(means: torch.Tensor, rotations: torch.Tensor, scales: torch.Tensor, level: int, tol: float = 1.0 / 8.0, rotnorm: bool = False) -&gt; torch.Tensor</code>","text":"<p>Computes the covariance matrices for 3D Gaussians in world space.</p> <p>Parameters:</p> <ul> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of Gaussian center positions.</p> </li> <li> <code>rotations</code>               (<code>Tensor</code>)           \u2013            <p>(N, 4) tensor of quaternions [w, x, y, z].</p> </li> <li> <code>scales</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of scale factors (pre-activation).</p> </li> <li> <code>level</code>               (<code>int</code>)           \u2013            <p>Octree subdivision level. Resolution will be at :math:<code>2^level</code></p> </li> <li> <code>tol</code>               (<code>float</code>, default:                   <code>1.0 / 8.0</code> )           \u2013            <p>Safety tolerance multiplier. Defaults to 0.125.</p> </li> <li> <code>rotnorm</code>               (<code>bool</code>, default:                   <code>False</code> )           \u2013            <p>Set to True to force normalizing quaternions.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>covi</code> (              <code>Tensor</code> )          \u2013            <p>torch.Tensor: (N, 6) tensor of covariance inverses (flattened upper-triangular) for each Gaussian.  The 6 values correspond to [Cxx, Cxy, Cxz, Cyy, Cyz, Czz] where C is the covariance matrix of the Gaussian.</p> </li> </ul> Source code in <code>conquer3d/primitive/gs.py</code> <pre><code>def compute_gs_covi(\n    means: torch.Tensor,\n    rotations: torch.Tensor,\n    scales: torch.Tensor,\n    level: int,\n    tol: float = 1. / 8.,\n    rotnorm: bool = False\n) -&gt; torch.Tensor:\n    \"\"\"\n    Computes the covariance matrices for 3D Gaussians in world space.\n\n    Args:\n        means (torch.Tensor): (N, 3) tensor of Gaussian center positions.\n        rotations (torch.Tensor): (N, 4) tensor of quaternions [w, x, y, z].\n        scales (torch.Tensor): (N, 3) tensor of scale factors (pre-activation).\n        level (int, optional): Octree subdivision level. Resolution will be at :math:`2^level`\n        tol (float, optional): Safety tolerance multiplier. Defaults to 0.125.\n        rotnorm (bool, optional): Set to True to force normalizing quaternions.\n\n    Returns:\n        covi: torch.Tensor: (N, 6) tensor of covariance inverses (flattened upper-triangular) for each Gaussian. \n            The 6 values correspond to [Cxx, Cxy, Cxz, Cyy, Cyz, Czz] where C is the covariance matrix of the Gaussian.\n    \"\"\"\n\n    if not all(t.is_cuda for t in [means, rotations, scales]):\n        raise ValueError(\"All input tensors must be CUDA tensors.\")\n\n    means_c = means.contiguous().to(torch.float32)\n    rotations_c = rotations.contiguous().to(torch.float32)\n    scales_c = scales.contiguous().to(torch.float32)\n\n    covi = compute_gs_covi_func(\n        means_c,\n        rotations_c,\n        scales_c,\n        rotnorm,\n        tol,\n        level\n    )\n\n    return covi\n</code></pre>"},{"location":"api/primitive/gs/#conquer3d.primitive.gs.solve_gs_neighbor_mahalanobis_radius","title":"<code>conquer3d.primitive.gs.solve_gs_neighbor_mahalanobis_radius(means: torch.Tensor, covis: torch.Tensor, k: int) -&gt; torch.Tensor</code>","text":"<p>Computes the Mahalanobis radius for each Gaussian based on its k-nearest neighbors.</p> <p>Parameters:</p> <ul> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of Gaussian center positions.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>(N, 6) tensor of covariance inverses (flattened upper-triangular).</p> </li> <li> <code>k</code>               (<code>int</code>)           \u2013            <p>Number of nearest neighbors to consider.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>isos</code> (              <code>Tensor</code> )          \u2013            <p>torch.Tensor: (N,) tensor of Mahalanobis radii for each Gaussian.</p> </li> </ul> Source code in <code>conquer3d/primitive/gs.py</code> <pre><code>def solve_gs_neighbor_mahalanobis_radius(\n    means: torch.Tensor,\n    covis: torch.Tensor,\n    k: int\n) -&gt; torch.Tensor:\n    \"\"\"\n    Computes the Mahalanobis radius for each Gaussian based on its k-nearest neighbors.\n\n    Args:\n        means (torch.Tensor): (N, 3) tensor of Gaussian center positions.\n        covis (torch.Tensor): (N, 6) tensor of covariance inverses (flattened upper-triangular).\n        k (int): Number of nearest neighbors to consider.\n\n    Returns:\n        isos: torch.Tensor: (N,) tensor of Mahalanobis radii for each Gaussian.\n    \"\"\"\n\n    if not all(t.is_cuda for t in [means, covis]):\n        raise ValueError(\"All input tensors must be CUDA tensors.\")\n\n    means_c = means.contiguous().to(torch.float32)\n    covis_c = covis.contiguous().to(torch.float32)\n\n    isos = solve_gs_neighbor_mahalanobis_radius_func(\n        means_c,\n        covis_c,\n        k\n    )\n\n    return isos\n</code></pre>"},{"location":"api/primitive/pgs/","title":"Point Gaussian Splatting (pgs)","text":""},{"location":"api/primitive/pgs/#conquer3d.primitive.pgs","title":"<code>conquer3d.primitive.pgs</code>","text":""},{"location":"api/primitive/pgs/#conquer3d.primitive.pgs.solve_pgs_cluster_tangency_radius","title":"<code>conquer3d.primitive.pgs.solve_pgs_cluster_tangency_radius(means: torch.Tensor, normals: torch.Tensor, covis: torch.Tensor, k: int = 16) -&gt; Tuple[torch.Tensor, torch.Tensor]</code>","text":"<p>Computes the tangency radius for each Gaussian cluster based on its k-nearest neighbors.</p> <p>Parameters:</p> <ul> <li> <code>means</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of Gaussian center positions.</p> </li> <li> <code>normals</code>               (<code>Tensor</code>)           \u2013            <p>(N, 3) tensor of Gaussian principal axes.</p> </li> <li> <code>covis</code>               (<code>Tensor</code>)           \u2013            <p>(N, 6) tensor of covariance inverses.</p> </li> <li> <code>k</code>               (<code>int</code>, default:                   <code>16</code> )           \u2013            <p>Number of nearest neighbors to consider. Defaults to 16.</p> </li> </ul> <p>Returns:</p> <ul> <li> <code>isos</code> (              <code>Tensor</code> )          \u2013            <p>(N,) tensor of computed tangency radii for each Gaussian.</p> </li> <li> <code>invalid_mask</code> (              <code>Tensor</code> )          \u2013            <p>(N,) boolean tensor indicating which Gaussians had invalid computations.</p> </li> </ul> Source code in <code>conquer3d/primitive/pgs.py</code> <pre><code>def solve_pgs_cluster_tangency_radius(\n    means: torch.Tensor,\n    normals: torch.Tensor,\n    covis: torch.Tensor,\n    k: int = 16\n) -&gt; Tuple[torch.Tensor, torch.Tensor]:\n    \"\"\"\n    Computes the tangency radius for each Gaussian cluster based on its k-nearest neighbors.\n\n    Args:\n        means (torch.Tensor): (N, 3) tensor of Gaussian center positions.\n        normals (torch.Tensor): (N, 3) tensor of Gaussian principal axes.\n        covis (torch.Tensor): (N, 6) tensor of covariance inverses.\n        k (int, optional): Number of nearest neighbors to consider. Defaults to 16.\n\n    Returns:\n        isos: (N,) tensor of computed tangency radii for each Gaussian.\n        invalid_mask: (N,) boolean tensor indicating which Gaussians had invalid computations.\n    \"\"\"\n    if not all(t.is_cuda for t in [means, normals, covis]):\n        raise ValueError(\"All input tensors must be CUDA tensors.\")\n\n    isos, invalid_mask = solve_pgs_cluster_tangency_radius_func(\n        means.contiguous().to(torch.float32),\n        normals.contiguous().to(torch.float32),\n        covis.contiguous().to(torch.float32),\n        k)\n\n    return isos, invalid_mask\n</code></pre>"},{"location":"examples/gaussians/gs_aabb/","title":"Gaussian Splatting AABB and Intersection Tests","text":"In\u00a0[1]: Copied! <pre>import torch\nimport numpy as np\nimport plotly.graph_objects as go\nfrom conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb\nfrom typing import Tuple, Optional\nfrom conquer3d.data_structure import GSBVH\n</pre> import torch import numpy as np import plotly.graph_objects as go from conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb from typing import Tuple, Optional from conquer3d.data_structure import GSBVH In\u00a0[2]: Copied! <pre>def generate_nxnxn_voxel_grid(n=3, voxel_size=0.1, center=(0.0, 0.0, 0.0), device='cuda'):\n    \"\"\"\n    Generates an n x n x n grid of voxel AABBs centered at a specific coordinate.\n    Returns vx_aabb_mins, vx_aabb_maxs of shape (n**3, 3).\n    \"\"\"\n    # Create dynamic steps to keep the grid perfectly centered.\n    # For n=3 -&gt; [-1.0,  0.0,  1.0]\n    # For n=4 -&gt; [-1.5, -0.5,  0.5,  1.5]\n    steps = torch.arange(n, dtype=torch.float32, device=device) - (n - 1) / 2.0\n    \n    # Create a 3D meshgrid\n    Z, Y, X = torch.meshgrid(steps, steps, steps, indexing='ij')\n    \n    # Flatten the grid and scale by voxel size to get the center coordinates of each voxel\n    centers = torch.stack(\n        [\n            X.flatten(), \n            Y.flatten(), \n            Z.flatten()\n        ], dim=-1\n    ) * voxel_size\n    \n    # Shift the entire grid to the requested center\n    center_tensor = torch.tensor(center, dtype=torch.float32, device=device)\n    centers += center_tensor\n    \n    # Calculate AABB min and max by subtracting/adding half the voxel size\n    half_size = voxel_size / 2.0\n    vx_aabb_mins = centers - half_size\n    vx_aabb_maxs = centers + half_size\n    \n    return vx_aabb_mins.contiguous(), vx_aabb_maxs.contiguous()\n\ndef generate_batch_wireframe(mins, maxs):\n    \"\"\"\n    Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords\n    with None separators to draw all boxes in a single Plotly trace.\n    \"\"\"\n    x_lines, y_lines, z_lines = [], [], []\n    \n    # Convert to numpy if they are tensors\n    mins_np = mins.cpu().numpy()\n    maxs_np = maxs.cpu().numpy()\n    \n    for min_pt, max_pt in zip(mins_np, maxs_np):\n        # 8 corners of the voxel\n        xc = [\n            min_pt[0], max_pt[0], max_pt[0], min_pt[0], \n            min_pt[0], max_pt[0], max_pt[0], min_pt[0]]\n        yc = [\n            min_pt[1], min_pt[1], max_pt[1], max_pt[1], \n            min_pt[1], min_pt[1], max_pt[1], max_pt[1]]\n        zc = [\n            min_pt[2], min_pt[2], min_pt[2], min_pt[2], \n            max_pt[2], max_pt[2], max_pt[2], max_pt[2]]\n\n        # The 12 edge drawing path (with Nones to lift the pen)\n        x_lines.extend([\n            xc[0], xc[1], xc[2], xc[3], xc[0], None, \n            xc[4], xc[5], xc[6], xc[7], xc[4], None, \n            xc[0], xc[4], None, xc[1], xc[5], None, \n            xc[2], xc[6], None, xc[3], xc[7], None])\n        y_lines.extend([\n            yc[0], yc[1], yc[2], yc[3], yc[0], None, \n            yc[4], yc[5], yc[6], yc[7], yc[4], None, \n            yc[0], yc[4], None, yc[1], yc[5], None, \n            yc[2], yc[6], None, yc[3], yc[7], None])\n        z_lines.extend([\n            zc[0], zc[1], zc[2], zc[3], zc[0], None, \n            zc[4], zc[5], zc[6], zc[7], zc[4], None, \n            zc[0], zc[4], None, zc[1], zc[5], None, \n            zc[2], zc[6], None, zc[3], zc[7], None])\n        \n    return x_lines, y_lines, z_lines\n</pre> def generate_nxnxn_voxel_grid(n=3, voxel_size=0.1, center=(0.0, 0.0, 0.0), device='cuda'):     \"\"\"     Generates an n x n x n grid of voxel AABBs centered at a specific coordinate.     Returns vx_aabb_mins, vx_aabb_maxs of shape (n**3, 3).     \"\"\"     # Create dynamic steps to keep the grid perfectly centered.     # For n=3 -&gt; [-1.0,  0.0,  1.0]     # For n=4 -&gt; [-1.5, -0.5,  0.5,  1.5]     steps = torch.arange(n, dtype=torch.float32, device=device) - (n - 1) / 2.0          # Create a 3D meshgrid     Z, Y, X = torch.meshgrid(steps, steps, steps, indexing='ij')          # Flatten the grid and scale by voxel size to get the center coordinates of each voxel     centers = torch.stack(         [             X.flatten(),              Y.flatten(),              Z.flatten()         ], dim=-1     ) * voxel_size          # Shift the entire grid to the requested center     center_tensor = torch.tensor(center, dtype=torch.float32, device=device)     centers += center_tensor          # Calculate AABB min and max by subtracting/adding half the voxel size     half_size = voxel_size / 2.0     vx_aabb_mins = centers - half_size     vx_aabb_maxs = centers + half_size          return vx_aabb_mins.contiguous(), vx_aabb_maxs.contiguous()  def generate_batch_wireframe(mins, maxs):     \"\"\"     Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords     with None separators to draw all boxes in a single Plotly trace.     \"\"\"     x_lines, y_lines, z_lines = [], [], []          # Convert to numpy if they are tensors     mins_np = mins.cpu().numpy()     maxs_np = maxs.cpu().numpy()          for min_pt, max_pt in zip(mins_np, maxs_np):         # 8 corners of the voxel         xc = [             min_pt[0], max_pt[0], max_pt[0], min_pt[0],              min_pt[0], max_pt[0], max_pt[0], min_pt[0]]         yc = [             min_pt[1], min_pt[1], max_pt[1], max_pt[1],              min_pt[1], min_pt[1], max_pt[1], max_pt[1]]         zc = [             min_pt[2], min_pt[2], min_pt[2], min_pt[2],              max_pt[2], max_pt[2], max_pt[2], max_pt[2]]          # The 12 edge drawing path (with Nones to lift the pen)         x_lines.extend([             xc[0], xc[1], xc[2], xc[3], xc[0], None,              xc[4], xc[5], xc[6], xc[7], xc[4], None,              xc[0], xc[4], None, xc[1], xc[5], None,              xc[2], xc[6], None, xc[3], xc[7], None])         y_lines.extend([             yc[0], yc[1], yc[2], yc[3], yc[0], None,              yc[4], yc[5], yc[6], yc[7], yc[4], None,              yc[0], yc[4], None, yc[1], yc[5], None,              yc[2], yc[6], None, yc[3], yc[7], None])         z_lines.extend([             zc[0], zc[1], zc[2], zc[3], zc[0], None,              zc[4], zc[5], zc[6], zc[7], zc[4], None,              zc[0], zc[4], None, zc[1], zc[5], None,              zc[2], zc[6], None, zc[3], zc[7], None])              return x_lines, y_lines, z_lines In\u00a0[3]: Copied! <pre>device = torch.device(\"cuda:0\" if torch.cuda.is_available() else \"cpu\")\n\nmeans = torch.tensor([[0.5, 0.5, 0.5]]).to(device)\nrotations = torch.tensor([[0.5, 0.2, 0.7, 0.3]]).to(device)\nscales = torch.tensor([[0.1, 0.6, 0.5]]).to(device)\nopacities = torch.tensor([[0.5]]).to(device)\n\nprint(\"Means:\", means.shape)\nprint(\"Rotations:\", rotations.shape)\nprint(\"Scales:\", scales.shape)\nprint(\"Opacities:\", opacities.shape)\n\nlevel = 8\niso = 11.345\ntol = 1. / 8.\nrotnorm = True\n\ncovis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm)\naabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, iso, tol)\n\nprint(\"AABB Min:\", aabb_min.shape)\nprint(\"AABB Max:\", aabb_max.shape)\nprint(\"Contact Points:\", contact_points.shape)\nprint(\"Covariance Inverses:\", covis.shape)\n\nmean_np = means[0].cpu().numpy()\nrot_np = rotations[0].cpu().numpy()\nscale_np = scales[0].cpu().numpy()\n\nmin_pt = aabb_min[0].cpu().numpy()\nmax_pt = aabb_max[0].cpu().numpy()\n\ncontact_points_np = contact_points[0].cpu().numpy()\ncovis_np = covis[0].cpu().numpy()\n\nall_contact_points = np.concatenate([contact_points_np, -contact_points_np], axis=0)\n\nvoxelSize = 2.0 / (1 &lt;&lt; level)\nmin_scale = tol * voxelSize\nscale_np = np.maximum(scale_np, min_scale)\n\nu = np.linspace(0, 2 * np.pi, 50)\nv = np.linspace(0, np.pi, 50)\nx_sphere = np.outer(np.cos(u), np.sin(v))\ny_sphere = np.outer(np.sin(u), np.sin(v))\nz_sphere = np.outer(np.ones(np.size(u)), np.cos(v))\n\nrx, ry, rz = scale_np * np.sqrt(iso)\nx_ellipsoid = x_sphere * rx\ny_ellipsoid = y_sphere * ry\nz_ellipsoid = z_sphere * rz\n\npoints = np.vstack(\n    [\n        x_ellipsoid.flatten(), \n        y_ellipsoid.flatten(), \n        z_ellipsoid.flatten()\n    ]\n)\n\nr, x, y, z = rot_np[0], rot_np[1], rot_np[2], rot_np[3]\nif rotnorm:\n    inv_norm = 1.0 / np.sqrt(r*r + x*x + y*y + z*z)\n    r *= inv_norm; x *= inv_norm; y *= inv_norm; z *= inv_norm\n\nR_mat = np.array([\n    [1 - 2*(y*y + z*z), 2*(x*y - r*z), 2*(x*z + r*y)],\n    [2*(x*y + r*z), 1 - 2*(x*x + z*z), 2*(y*z - r*x)],\n    [2*(x*z - r*y), 2*(y*z + r*x), 1 - 2*(x*x + y*y)]\n])\n\nrotated_points = R_mat @ points\nx_final = rotated_points[0, :].reshape(x_sphere.shape) + mean_np[0]\ny_final = rotated_points[1, :].reshape(y_sphere.shape) + mean_np[1]\nz_final = rotated_points[2, :].reshape(z_sphere.shape) + mean_np[2]\n\nx_lines, y_lines, z_lines = generate_batch_wireframe(aabb_min, aabb_max)\n\nfig = go.Figure()\n\nfig.add_trace(go.Surface(\n    x=x_final, y=y_final, z=z_final, \n    opacity=0.6, \n    colorscale='Plasma', \n    showscale=False,\n    name='Gaussian'\n))\n\nfig.add_trace(go.Scatter3d(\n    x=x_lines, y=y_lines, z=z_lines,\n    mode='lines',\n    line=dict(color='red', width=4),\n    name='AABB'\n))\n\nfig.add_trace(go.Scatter3d(\n    x=all_contact_points[0::3] + mean_np[0],\n    y=all_contact_points[1::3] + mean_np[1],\n    z=all_contact_points[2::3] + mean_np[2],\n    mode='markers',\n    marker=dict(size=5, color='green'),\n    name='Contact Points'\n))\n\nfig.update_layout(\n    scene=dict(aspectmode='data'),\n    scene_camera=dict(projection=dict(type=\"orthographic\")),\n    title=\"3D Gaussian and Computed AABB\",\n    # margin=dict(l=0, r=0, b=0, t=40)\n)\n\nfig.show()\n</pre> device = torch.device(\"cuda:0\" if torch.cuda.is_available() else \"cpu\")  means = torch.tensor([[0.5, 0.5, 0.5]]).to(device) rotations = torch.tensor([[0.5, 0.2, 0.7, 0.3]]).to(device) scales = torch.tensor([[0.1, 0.6, 0.5]]).to(device) opacities = torch.tensor([[0.5]]).to(device)  print(\"Means:\", means.shape) print(\"Rotations:\", rotations.shape) print(\"Scales:\", scales.shape) print(\"Opacities:\", opacities.shape)  level = 8 iso = 11.345 tol = 1. / 8. rotnorm = True  covis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm) aabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, iso, tol)  print(\"AABB Min:\", aabb_min.shape) print(\"AABB Max:\", aabb_max.shape) print(\"Contact Points:\", contact_points.shape) print(\"Covariance Inverses:\", covis.shape)  mean_np = means[0].cpu().numpy() rot_np = rotations[0].cpu().numpy() scale_np = scales[0].cpu().numpy()  min_pt = aabb_min[0].cpu().numpy() max_pt = aabb_max[0].cpu().numpy()  contact_points_np = contact_points[0].cpu().numpy() covis_np = covis[0].cpu().numpy()  all_contact_points = np.concatenate([contact_points_np, -contact_points_np], axis=0)  voxelSize = 2.0 / (1 &lt;&lt; level) min_scale = tol * voxelSize scale_np = np.maximum(scale_np, min_scale)  u = np.linspace(0, 2 * np.pi, 50) v = np.linspace(0, np.pi, 50) x_sphere = np.outer(np.cos(u), np.sin(v)) y_sphere = np.outer(np.sin(u), np.sin(v)) z_sphere = np.outer(np.ones(np.size(u)), np.cos(v))  rx, ry, rz = scale_np * np.sqrt(iso) x_ellipsoid = x_sphere * rx y_ellipsoid = y_sphere * ry z_ellipsoid = z_sphere * rz  points = np.vstack(     [         x_ellipsoid.flatten(),          y_ellipsoid.flatten(),          z_ellipsoid.flatten()     ] )  r, x, y, z = rot_np[0], rot_np[1], rot_np[2], rot_np[3] if rotnorm:     inv_norm = 1.0 / np.sqrt(r*r + x*x + y*y + z*z)     r *= inv_norm; x *= inv_norm; y *= inv_norm; z *= inv_norm  R_mat = np.array([     [1 - 2*(y*y + z*z), 2*(x*y - r*z), 2*(x*z + r*y)],     [2*(x*y + r*z), 1 - 2*(x*x + z*z), 2*(y*z - r*x)],     [2*(x*z - r*y), 2*(y*z + r*x), 1 - 2*(x*x + y*y)] ])  rotated_points = R_mat @ points x_final = rotated_points[0, :].reshape(x_sphere.shape) + mean_np[0] y_final = rotated_points[1, :].reshape(y_sphere.shape) + mean_np[1] z_final = rotated_points[2, :].reshape(z_sphere.shape) + mean_np[2]  x_lines, y_lines, z_lines = generate_batch_wireframe(aabb_min, aabb_max)  fig = go.Figure()  fig.add_trace(go.Surface(     x=x_final, y=y_final, z=z_final,      opacity=0.6,      colorscale='Plasma',      showscale=False,     name='Gaussian' ))  fig.add_trace(go.Scatter3d(     x=x_lines, y=y_lines, z=z_lines,     mode='lines',     line=dict(color='red', width=4),     name='AABB' ))  fig.add_trace(go.Scatter3d(     x=all_contact_points[0::3] + mean_np[0],     y=all_contact_points[1::3] + mean_np[1],     z=all_contact_points[2::3] + mean_np[2],     mode='markers',     marker=dict(size=5, color='green'),     name='Contact Points' ))  fig.update_layout(     scene=dict(aspectmode='data'),     scene_camera=dict(projection=dict(type=\"orthographic\")),     title=\"3D Gaussian and Computed AABB\",     # margin=dict(l=0, r=0, b=0, t=40) )  fig.show() <pre>Means: torch.Size([1, 3])\nRotations: torch.Size([1, 4])\nScales: torch.Size([1, 3])\nOpacities: torch.Size([1, 1])\nAABB Min: torch.Size([1, 3])\nAABB Max: torch.Size([1, 3])\nContact Points: torch.Size([1, 9])\nCovariance Inverses: torch.Size([1, 6])\n</pre> In\u00a0[4]: Copied! <pre>vx_aabb_mins, vx_aabb_maxs = generate_nxnxn_voxel_grid(\n    n=8,\n    voxel_size=0.5, \n    center=(0.0, 0.0, 0.0), \n    device=device\n)\n\nar_threshold = 0.1\np_threshold = 0.1\nreturn_centroids = True\n\nprint(\"Voxel AABB Mins:\", vx_aabb_mins.shape)\nprint(\"Voxel AABB Maxs:\", vx_aabb_maxs.shape)\n\ngsbvh = GSBVH(aabb_min, aabb_max)\n\nhit_mask, out_voxel_ids, out_gaus_ids, centroids, densities = gsbvh.query_voxel_pair(\n    vx_aabb_mins, \n    vx_aabb_maxs, \n    means, \n    covis, \n    opacities, \n    aabb_min,\n    aabb_max,\n    contact_points,\n    iso, \n    ar_threshold, \n    p_threshold, \n    return_centroids\n)\n\nprint(\"Hit Mask:\", hit_mask.shape)\nprint(\"Output Voxel IDs:\", out_voxel_ids.shape)\nprint(\"Output Gaussian IDs:\", out_gaus_ids.shape)\nprint(\"Centroids:\", centroids.shape if return_centroids else \"Not Returned\")\nprint(\"Densities:\", densities.shape if return_centroids else \"Not Returned\")\nprint(f\"Num Hits: {hit_mask.sum().item()}\")\nfor i in range(out_voxel_ids.shape[0]):\n    print(f\"Voxel ID: {out_voxel_ids[i].item()}, Gaussian ID: {out_gaus_ids[i].item()}, Centroid: {centroids[i].cpu().numpy() if return_centroids else 'N/A'}, Density: {densities[i].item() if return_centroids else 'N/A'}\")\n\n# Contruct voxel wireframe for visualization\n\nivx_aabb_mins = vx_aabb_mins[hit_mask]\nivx_aabb_maxs = vx_aabb_maxs[hit_mask]\n\nnivx_aabb_mins = vx_aabb_mins[~hit_mask]\nnivx_aabb_maxs = vx_aabb_maxs[~hit_mask]\n\nhit_x, hit_y, hit_z = generate_batch_wireframe(ivx_aabb_mins, ivx_aabb_maxs)\nmiss_x, miss_y, miss_z = generate_batch_wireframe(nivx_aabb_mins, nivx_aabb_maxs)\n\nif len(miss_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=miss_x, y=miss_y, z=miss_z,\n        mode='lines',\n        line=dict(color='rgba(0, 0, 255, 0.3)', width=2), # Lower opacity blue\n        name='Missed Voxels'\n    ))\n\n# Add Hit Voxels (Green)\nif len(hit_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=hit_x, y=hit_y, z=hit_z,\n        mode='lines',\n        line=dict(color='rgba(0, 255, 0, 0.8)', width=4), # Bright green\n        name='Hit Voxels'\n    ))\n\n# Update layout for a better view of the grid\nfig.update_layout(\n    title=\"Gaussian vs. Voxel Grid Intersection\",\n    width=800,\n    height=800,\n    scene=dict(\n        aspectmode='data',\n        xaxis_title='X',\n        yaxis_title='Y',\n        zaxis_title='Z'\n    )\n)\n\n# fig.show()\n</pre> vx_aabb_mins, vx_aabb_maxs = generate_nxnxn_voxel_grid(     n=8,     voxel_size=0.5,      center=(0.0, 0.0, 0.0),      device=device )  ar_threshold = 0.1 p_threshold = 0.1 return_centroids = True  print(\"Voxel AABB Mins:\", vx_aabb_mins.shape) print(\"Voxel AABB Maxs:\", vx_aabb_maxs.shape)  gsbvh = GSBVH(aabb_min, aabb_max)  hit_mask, out_voxel_ids, out_gaus_ids, centroids, densities = gsbvh.query_voxel_pair(     vx_aabb_mins,      vx_aabb_maxs,      means,      covis,      opacities,      aabb_min,     aabb_max,     contact_points,     iso,      ar_threshold,      p_threshold,      return_centroids )  print(\"Hit Mask:\", hit_mask.shape) print(\"Output Voxel IDs:\", out_voxel_ids.shape) print(\"Output Gaussian IDs:\", out_gaus_ids.shape) print(\"Centroids:\", centroids.shape if return_centroids else \"Not Returned\") print(\"Densities:\", densities.shape if return_centroids else \"Not Returned\") print(f\"Num Hits: {hit_mask.sum().item()}\") for i in range(out_voxel_ids.shape[0]):     print(f\"Voxel ID: {out_voxel_ids[i].item()}, Gaussian ID: {out_gaus_ids[i].item()}, Centroid: {centroids[i].cpu().numpy() if return_centroids else 'N/A'}, Density: {densities[i].item() if return_centroids else 'N/A'}\")  # Contruct voxel wireframe for visualization  ivx_aabb_mins = vx_aabb_mins[hit_mask] ivx_aabb_maxs = vx_aabb_maxs[hit_mask]  nivx_aabb_mins = vx_aabb_mins[~hit_mask] nivx_aabb_maxs = vx_aabb_maxs[~hit_mask]  hit_x, hit_y, hit_z = generate_batch_wireframe(ivx_aabb_mins, ivx_aabb_maxs) miss_x, miss_y, miss_z = generate_batch_wireframe(nivx_aabb_mins, nivx_aabb_maxs)  if len(miss_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=miss_x, y=miss_y, z=miss_z,         mode='lines',         line=dict(color='rgba(0, 0, 255, 0.3)', width=2), # Lower opacity blue         name='Missed Voxels'     ))  # Add Hit Voxels (Green) if len(hit_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=hit_x, y=hit_y, z=hit_z,         mode='lines',         line=dict(color='rgba(0, 255, 0, 0.8)', width=4), # Bright green         name='Hit Voxels'     ))  # Update layout for a better view of the grid fig.update_layout(     title=\"Gaussian vs. Voxel Grid Intersection\",     width=800,     height=800,     scene=dict(         aspectmode='data',         xaxis_title='X',         yaxis_title='Y',         zaxis_title='Z'     ) )  # fig.show() <pre>Voxel AABB Mins: torch.Size([512, 3])\nVoxel AABB Maxs: torch.Size([512, 3])\nHit Mask: torch.Size([512])\nOutput Voxel IDs: torch.Size([111])\nOutput Gaussian IDs: torch.Size([111])\nCentroids: torch.Size([111, 3])\nDensities: torch.Size([111])\nNum Hits: 111\nVoxel ID: 149, Gaussian ID: 0, Centroid: [ 0.75       -0.74826854 -0.75      ], Density: 0.004231208469718695\nVoxel ID: 157, Gaussian ID: 0, Centroid: [ 0.75 -0.25 -0.75], Density: 0.001079017180018127\nVoxel ID: 212, Gaussian ID: 0, Centroid: [ 0.25       -0.74826854 -0.25      ], Density: 0.001083784969523549\nVoxel ID: 220, Gaussian ID: 0, Centroid: [ 0.25 -0.25 -0.25], Density: 0.06575526297092438\nVoxel ID: 221, Gaussian ID: 0, Centroid: [ 0.75 -0.25 -0.25], Density: 0.0667421892285347\nVoxel ID: 166, Gaussian ID: 0, Centroid: [ 1.25  0.25 -0.75], Density: 3.0994892767921556e-06\nVoxel ID: 167, Gaussian ID: 0, Centroid: [ 1.75  0.25 -0.75], Density: 8.822304516797885e-05\nVoxel ID: 229, Gaussian ID: 0, Centroid: [ 0.75  0.25 -0.25], Density: 0.008606134913861752\nVoxel ID: 150, Gaussian ID: 0, Centroid: [ 1.25       -0.74826854 -0.75      ], Density: 0.00011295817967038602\nVoxel ID: 165, Gaussian ID: 0, Centroid: [ 0.75  0.25 -0.75], Density: 0.0\nVoxel ID: 219, Gaussian ID: 0, Centroid: [-0.25 -0.25 -0.25], Density: 0.001656220993027091\nVoxel ID: 222, Gaussian ID: 0, Centroid: [ 1.25 -0.25 -0.25], Density: 0.0017319099279120564\nVoxel ID: 230, Gaussian ID: 0, Centroid: [ 1.25  0.25 -0.25], Density: 0.04602918028831482\nVoxel ID: 231, Gaussian ID: 0, Centroid: [ 1.75  0.25 -0.25], Density: 0.006293813232332468\nVoxel ID: 156, Gaussian ID: 0, Centroid: [ 0.25 -0.25 -0.75], Density: 5.106769094709307e-06\nVoxel ID: 158, Gaussian ID: 0, Centroid: [ 1.25 -0.25 -0.75], Density: 0.005828602239489555\nVoxel ID: 155, Gaussian ID: 0, Centroid: [-0.25 -0.25 -0.75], Density: 0.0\nVoxel ID: 213, Gaussian ID: 0, Centroid: [ 0.75       -0.74826854 -0.25      ], Density: 5.436573246697662e-06\nVoxel ID: 214, Gaussian ID: 0, Centroid: [ 1.25       -0.74826854 -0.25      ], Density: 0.0\nVoxel ID: 223, Gaussian ID: 0, Centroid: [ 1.75 -0.25 -0.25], Density: 1.1489698863442754e-06\nVoxel ID: 228, Gaussian ID: 0, Centroid: [ 0.25  0.25 -0.25], Density: 4.113774048164487e-05\nVoxel ID: 238, Gaussian ID: 0, Centroid: [ 1.25  0.75 -0.25], Density: 1.2190110282972455e-05\nVoxel ID: 239, Gaussian ID: 0, Centroid: [ 1.75  0.75 -0.25], Density: 0.0003435484250076115\nVoxel ID: 284, Gaussian ID: 0, Centroid: [ 0.25 -0.25  0.25], Density: 0.00847888458520174\nVoxel ID: 292, Gaussian ID: 0, Centroid: [0.25 0.25 0.25], Density: 0.26511290669441223\nVoxel ID: 293, Gaussian ID: 0, Centroid: [0.75 0.25 0.25], Density: 0.266432523727417\nVoxel ID: 300, Gaussian ID: 0, Centroid: [0.25 0.75 0.25], Density: 8.260201866505668e-05\nVoxel ID: 301, Gaussian ID: 0, Centroid: [0.75 0.75 0.25], Density: 0.0171098243445158\nVoxel ID: 302, Gaussian ID: 0, Centroid: [1.25 0.75 0.25], Density: 0.09060607105493546\nVoxel ID: 303, Gaussian ID: 0, Centroid: [1.75 0.75 0.25], Density: 0.01226664800196886\nVoxel ID: 353, Gaussian ID: 0, Centroid: [-1.0459862  0.25       0.75     ], Density: 0.0006673026946373284\nVoxel ID: 354, Gaussian ID: 0, Centroid: [-0.75  0.25  0.75], Density: 0.01226664800196886\nVoxel ID: 355, Gaussian ID: 0, Centroid: [-0.25  0.25  0.75], Density: 0.09060607105493546\nVoxel ID: 356, Gaussian ID: 0, Centroid: [0.25 0.25 0.75], Density: 0.0171098243445158\nVoxel ID: 357, Gaussian ID: 0, Centroid: [0.75 0.25 0.75], Density: 8.260201866505668e-05\nVoxel ID: 364, Gaussian ID: 0, Centroid: [0.25 0.75 0.75], Density: 0.266432523727417\nVoxel ID: 365, Gaussian ID: 0, Centroid: [0.75 0.75 0.75], Density: 0.26511290669441223\nVoxel ID: 373, Gaussian ID: 0, Centroid: [0.75 1.25 0.75], Density: 0.00847888458520174\nVoxel ID: 428, Gaussian ID: 0, Centroid: [0.25 0.75 1.25], Density: 0.008606134913861752\nVoxel ID: 436, Gaussian ID: 0, Centroid: [0.25 1.25 1.25], Density: 0.0667421892285347\nVoxel ID: 437, Gaussian ID: 0, Centroid: [0.75 1.25 1.25], Density: 0.06575526297092438\nVoxel ID: 445, Gaussian ID: 0, Centroid: [0.75      1.7482686 1.25     ], Density: 0.001083784969523549\nVoxel ID: 500, Gaussian ID: 0, Centroid: [0.25 1.25 1.75], Density: 0.001079017180018127\nVoxel ID: 508, Gaussian ID: 0, Centroid: [0.25      1.7482686 1.75     ], Density: 0.004231208469718695\nVoxel ID: 148, Gaussian ID: 0, Centroid: [ 0.25       -0.74826854 -0.75      ], Density: 0.004052004776895046\nVoxel ID: 159, Gaussian ID: 0, Centroid: [ 1.75 -0.25 -0.75], Density: 0.0008049329626373947\nVoxel ID: 346, Gaussian ID: 0, Centroid: [-0.75 -0.25  0.75], Density: 0.00033346397685818374\nVoxel ID: 347, Gaussian ID: 0, Centroid: [-0.25 -0.25  0.75], Density: 1.195038748846855e-05\nVoxel ID: 348, Gaussian ID: 0, Centroid: [ 0.25 -0.25  0.75], Density: 0.0\nVoxel ID: 218, Gaussian ID: 0, Centroid: [-0.75 -0.25 -0.25], Density: 1.0665036143109319e-06\nVoxel ID: 227, Gaussian ID: 0, Centroid: [-0.25  0.25 -0.25], Density: 0.0\nVoxel ID: 237, Gaussian ID: 0, Centroid: [ 0.75  0.75 -0.25], Density: 0.0\nVoxel ID: 282, Gaussian ID: 0, Centroid: [-0.75 -0.25  0.25], Density: 0.005959277506917715\nVoxel ID: 283, Gaussian ID: 0, Centroid: [-0.25 -0.25  0.25], Density: 0.04445676878094673\nVoxel ID: 291, Gaussian ID: 0, Centroid: [-0.25  0.25  0.25], Density: 0.006744212005287409\nVoxel ID: 294, Gaussian ID: 0, Centroid: [1.25 0.25 0.25], Density: 0.006845428608357906\nVoxel ID: 363, Gaussian ID: 0, Centroid: [-0.25  0.75  0.75], Density: 0.006845428608357906\nVoxel ID: 366, Gaussian ID: 0, Centroid: [1.25 0.75 0.75], Density: 0.006744212005287409\nVoxel ID: 374, Gaussian ID: 0, Centroid: [1.25 1.25 0.75], Density: 0.04445676878094673\nVoxel ID: 375, Gaussian ID: 0, Centroid: [1.75 1.25 0.75], Density: 0.005959277506917715\nVoxel ID: 498, Gaussian ID: 0, Centroid: [-0.75  1.25  1.75], Density: 0.0008049329626373947\nVoxel ID: 499, Gaussian ID: 0, Centroid: [-0.25  1.25  1.75], Density: 0.005828602239489555\nVoxel ID: 507, Gaussian ID: 0, Centroid: [-0.25       1.7482686  1.75     ], Density: 0.00011295817967038602\nVoxel ID: 509, Gaussian ID: 0, Centroid: [0.75      1.7482686 1.75     ], Density: 0.004052004776895046\nVoxel ID: 510, Gaussian ID: 0, Centroid: [1.25      1.7482686 1.75     ], Density: 9.920441516442224e-05\nVoxel ID: 426, Gaussian ID: 0, Centroid: [-0.75  0.75  1.25], Density: 0.006293813232332468\nVoxel ID: 427, Gaussian ID: 0, Centroid: [-0.25  0.75  1.25], Density: 0.04602918028831482\nVoxel ID: 435, Gaussian ID: 0, Centroid: [-0.25  1.25  1.25], Density: 0.0017319099279120564\nVoxel ID: 438, Gaussian ID: 0, Centroid: [1.25 1.25 1.25], Density: 0.001656220993027091\nVoxel ID: 446, Gaussian ID: 0, Centroid: [1.25      1.7482686 1.25     ], Density: 0.005523511674255133\nVoxel ID: 447, Gaussian ID: 0, Centroid: [1.75      1.7482686 1.25     ], Density: 0.00071968964766711\nVoxel ID: 211, Gaussian ID: 0, Centroid: [-0.25       -0.74826854 -0.25      ], Density: 0.005523511674255133\nVoxel ID: 275, Gaussian ID: 0, Centroid: [-0.25       -0.74826854  0.25      ], Density: 3.0798094030615175e-06\nVoxel ID: 276, Gaussian ID: 0, Centroid: [ 0.25       -0.74826854  0.25      ], Density: 0.0\nVoxel ID: 285, Gaussian ID: 0, Centroid: [ 0.75 -0.25  0.25], Density: 4.134252594667487e-05\nVoxel ID: 286, Gaussian ID: 0, Centroid: [ 1.25 -0.25  0.25], Density: 0.0\nVoxel ID: 147, Gaussian ID: 0, Centroid: [-0.25       -0.74826854 -0.75      ], Density: 9.920441516442224e-05\nVoxel ID: 295, Gaussian ID: 0, Centroid: [1.75 0.25 0.25], Density: 4.496447218116373e-06\nVoxel ID: 358, Gaussian ID: 0, Centroid: [1.25 0.25 0.75], Density: 0.0\nVoxel ID: 367, Gaussian ID: 0, Centroid: [1.75 0.75 0.75], Density: 4.386196906125406e-06\nVoxel ID: 418, Gaussian ID: 0, Centroid: [-0.75  0.25  1.25], Density: 0.0003435484250076115\nVoxel ID: 419, Gaussian ID: 0, Centroid: [-0.25  0.25  1.25], Density: 1.2190110282972455e-05\nVoxel ID: 420, Gaussian ID: 0, Centroid: [0.25 0.25 1.25], Density: 0.0\nVoxel ID: 429, Gaussian ID: 0, Centroid: [0.75 0.75 1.25], Density: 4.113774048164487e-05\nVoxel ID: 430, Gaussian ID: 0, Centroid: [1.25 0.75 1.25], Density: 0.0\nVoxel ID: 439, Gaussian ID: 0, Centroid: [1.75 1.25 1.25], Density: 1.0665036143109319e-06\nVoxel ID: 491, Gaussian ID: 0, Centroid: [-0.25  0.75  1.75], Density: 3.0994892767921556e-06\nVoxel ID: 492, Gaussian ID: 0, Centroid: [0.25 0.75 1.75], Density: 0.0\nVoxel ID: 501, Gaussian ID: 0, Centroid: [0.75 1.25 1.75], Density: 5.106769094709307e-06\nVoxel ID: 502, Gaussian ID: 0, Centroid: [1.25 1.25 1.75], Density: 0.0\nVoxel ID: 345, Gaussian ID: 0, Centroid: [-1.0459862 -0.25       0.75     ], Density: 0.00042513737571425736\nVoxel ID: 310, Gaussian ID: 0, Centroid: [1.25 1.25 0.25], Density: 1.195038748846855e-05\nVoxel ID: 311, Gaussian ID: 0, Centroid: [1.75 1.25 0.25], Density: 0.00033346397685818374\nVoxel ID: 372, Gaussian ID: 0, Centroid: [0.25 1.25 0.75], Density: 4.134252594667487e-05\nVoxel ID: 382, Gaussian ID: 0, Centroid: [1.25      1.7482686 0.75     ], Density: 3.0798094030615175e-06\nVoxel ID: 383, Gaussian ID: 0, Centroid: [1.75      1.7482686 0.75     ], Density: 8.35344399092719e-05\nVoxel ID: 444, Gaussian ID: 0, Centroid: [0.25      1.7482686 1.25     ], Density: 5.436573246697662e-06\nVoxel ID: 210, Gaussian ID: 0, Centroid: [-0.75       -0.74826854 -0.25      ], Density: 0.00071968964766711\nVoxel ID: 274, Gaussian ID: 0, Centroid: [-0.75       -0.74826854  0.25      ], Density: 8.35344399092719e-05\nVoxel ID: 290, Gaussian ID: 0, Centroid: [-0.75  0.25  0.25], Density: 4.386196906125406e-06\nVoxel ID: 299, Gaussian ID: 0, Centroid: [-0.25  0.75  0.25], Density: 0.0\nVoxel ID: 309, Gaussian ID: 0, Centroid: [0.75 1.25 0.25], Density: 0.0\nVoxel ID: 434, Gaussian ID: 0, Centroid: [-0.75  1.25  1.25], Density: 1.1489698863442754e-06\nVoxel ID: 443, Gaussian ID: 0, Centroid: [-0.25       1.7482686  1.25     ], Density: 0.0\nVoxel ID: 362, Gaussian ID: 0, Centroid: [-0.75  0.75  0.75], Density: 4.496447218116373e-06\nVoxel ID: 371, Gaussian ID: 0, Centroid: [-0.25  1.25  0.75], Density: 0.0\nVoxel ID: 381, Gaussian ID: 0, Centroid: [0.75      1.7482686 0.75     ], Density: 0.0\nVoxel ID: 490, Gaussian ID: 0, Centroid: [-0.75  0.75  1.75], Density: 8.822304516797885e-05\nVoxel ID: 281, Gaussian ID: 0, Centroid: [-1.0459862 -0.25       0.25     ], Density: 0.0003222834784537554\nVoxel ID: 417, Gaussian ID: 0, Centroid: [-1.0459862  0.25       1.25     ], Density: 0.00044057564809918404\nVoxel ID: 425, Gaussian ID: 0, Centroid: [-1.0459862  0.75       1.25     ], Density: 0.00034440099261701107\n</pre> In\u00a0[5]: Copied! <pre>for name in [\"Missed Voxels\", \"Contact Points\", \"AABB\"]:\n    fig.update_traces(visible=False, selector=dict(name=name))\n\nhit_vx_mins = vx_aabb_mins[out_voxel_ids]\nhit_vx_maxs = vx_aabb_maxs[out_voxel_ids]\nhit_gs_mins = aabb_min[out_gaus_ids]\nhit_gs_maxs = aabb_max[out_gaus_ids]\n\noverlap_mins = torch.max(hit_vx_mins, hit_gs_mins)\noverlap_maxs = torch.min(hit_vx_maxs, hit_gs_maxs)\n\noverlap_x, overlap_y, overlap_z = generate_batch_wireframe(overlap_mins, overlap_maxs)\n\nif len(overlap_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=overlap_x, y=overlap_y, z=overlap_z,\n        mode='lines',\n        line=dict(color='rgba(255, 165, 0, 1.0)', width=5), # Thick Orange\n        name='Overlap Boxes'\n    ))\n\nif return_centroids and centroids is not None and len(centroids) &gt; 0:\n    centroids_np = centroids.cpu().numpy()\n    densities_np = densities.cpu().numpy()\n    \n    fig.add_trace(go.Scatter3d(\n        x=centroids_np[:, 0],\n        y=centroids_np[:, 1],\n        z=centroids_np[:, 2],\n        mode='markers',\n        marker=dict(\n            color=densities_np,       # Map the color to the exact density value!\n            colorscale='Plasma',      # 'Plasma' goes from very dark purple (0.0) to bright yellow (high)\n            cmin=0.0,                 # Force 0.0 to be the darkest color\n            cmax=float(densities_np.max()), # Force the max density to be the brightest color\n            showscale=True,           # Adds a colorbar legend to the right side of the screen\n            colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),\n            size=6,\n            symbol='diamond',\n            line=dict(color='rgba(255, 255, 255, 0.5)', width=1) # Slight white outline\n        ),\n        name='Approximated Centroids'\n    ))\n\nfig.show()\n</pre> for name in [\"Missed Voxels\", \"Contact Points\", \"AABB\"]:     fig.update_traces(visible=False, selector=dict(name=name))  hit_vx_mins = vx_aabb_mins[out_voxel_ids] hit_vx_maxs = vx_aabb_maxs[out_voxel_ids] hit_gs_mins = aabb_min[out_gaus_ids] hit_gs_maxs = aabb_max[out_gaus_ids]  overlap_mins = torch.max(hit_vx_mins, hit_gs_mins) overlap_maxs = torch.min(hit_vx_maxs, hit_gs_maxs)  overlap_x, overlap_y, overlap_z = generate_batch_wireframe(overlap_mins, overlap_maxs)  if len(overlap_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=overlap_x, y=overlap_y, z=overlap_z,         mode='lines',         line=dict(color='rgba(255, 165, 0, 1.0)', width=5), # Thick Orange         name='Overlap Boxes'     ))  if return_centroids and centroids is not None and len(centroids) &gt; 0:     centroids_np = centroids.cpu().numpy()     densities_np = densities.cpu().numpy()          fig.add_trace(go.Scatter3d(         x=centroids_np[:, 0],         y=centroids_np[:, 1],         z=centroids_np[:, 2],         mode='markers',         marker=dict(             color=densities_np,       # Map the color to the exact density value!             colorscale='Plasma',      # 'Plasma' goes from very dark purple (0.0) to bright yellow (high)             cmin=0.0,                 # Force 0.0 to be the darkest color             cmax=float(densities_np.max()), # Force the max density to be the brightest color             showscale=True,           # Adds a colorbar legend to the right side of the screen             colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),             size=6,             symbol='diamond',             line=dict(color='rgba(255, 255, 255, 0.5)', width=1) # Slight white outline         ),         name='Approximated Centroids'     ))  fig.show() In\u00a0[6]: Copied! <pre>for name in [\"Approximated Centroids\", \"Hit Voxels\"]:\n    fig.update_traces(visible=False, selector=dict(name=name))\n\nif return_centroids and centroids is not None and len(centroids) &gt; 0:\n    centroids_np = centroids.cpu().numpy()\n    densities_np = densities.cpu().numpy()\n    \n    # Create the filter mask\n    valid_mask = densities_np &gt; 0.001\n    \n    # Apply the mask to BOTH arrays to keep the coordinates and colors perfectly aligned\n    meaningful_centroids = centroids_np[valid_mask]\n    meaningful_densities = densities_np[valid_mask]\n    \n    # Ensure there are still points left to plot after filtering!\n    if len(meaningful_centroids) &gt; 0:\n        fig.add_trace(go.Scatter3d(\n            x=meaningful_centroids[:, 0],\n            y=meaningful_centroids[:, 1],\n            z=meaningful_centroids[:, 2],\n            mode='markers',\n            marker=dict(\n                color=meaningful_densities,       \n                colorscale='Plasma',      \n                # Keeping cmin at 0.0 keeps the color scale visually consistent with your previous plots\n                cmin=0.0,                 \n                cmax=float(meaningful_densities.max()), \n                showscale=True,           \n                colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),\n                size=6,\n                symbol='diamond',\n                line=dict(color='rgba(255, 255, 255, 0.5)', width=1) \n            ),\n            name='Approximated Centroids (&gt;0.001)'\n        ))\n\nfig.show()\n</pre> for name in [\"Approximated Centroids\", \"Hit Voxels\"]:     fig.update_traces(visible=False, selector=dict(name=name))  if return_centroids and centroids is not None and len(centroids) &gt; 0:     centroids_np = centroids.cpu().numpy()     densities_np = densities.cpu().numpy()          # Create the filter mask     valid_mask = densities_np &gt; 0.001          # Apply the mask to BOTH arrays to keep the coordinates and colors perfectly aligned     meaningful_centroids = centroids_np[valid_mask]     meaningful_densities = densities_np[valid_mask]          # Ensure there are still points left to plot after filtering!     if len(meaningful_centroids) &gt; 0:         fig.add_trace(go.Scatter3d(             x=meaningful_centroids[:, 0],             y=meaningful_centroids[:, 1],             z=meaningful_centroids[:, 2],             mode='markers',             marker=dict(                 color=meaningful_densities,                        colorscale='Plasma',                       # Keeping cmin at 0.0 keeps the color scale visually consistent with your previous plots                 cmin=0.0,                                  cmax=float(meaningful_densities.max()),                  showscale=True,                            colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),                 size=6,                 symbol='diamond',                 line=dict(color='rgba(255, 255, 255, 0.5)', width=1)              ),             name='Approximated Centroids (&gt;0.001)'         ))  fig.show() In\u00a0[7]: Copied! <pre>if return_centroids and densities is not None and len(densities) &gt; 0:\n    densities_np = densities.cpu().numpy()\n\n    # 2. Create an interactive Histogram\n    fig_hist = go.Figure(data=[go.Histogram(\n        x=densities_np,\n        nbinsx=100, # Bins the data into 100 vertical bars\n        marker_color='rgba(255, 165, 0, 0.8)', # Orange to match your overlap boxes\n        opacity=0.75\n    )])\n\n    # 3. Update the layout for clarity\n    fig_hist.update_layout(\n        title_text='Distribution of Gaussian Intersection Densities', # title of plot\n        xaxis_title_text='Calculated Density (Alpha)', # xaxis label\n        yaxis_title_text='Frequency (Number of Intersections)', # yaxis label\n        bargap=0.1, # Gap between bars so it looks clean\n        template='plotly_dark' # Optional: makes it look like a sleek terminal UI\n    )\n\n    fig_hist.show()\nelse:\n    print(\"Densities tensor is empty or return_centroids was set to False.\")\n</pre> if return_centroids and densities is not None and len(densities) &gt; 0:     densities_np = densities.cpu().numpy()      # 2. Create an interactive Histogram     fig_hist = go.Figure(data=[go.Histogram(         x=densities_np,         nbinsx=100, # Bins the data into 100 vertical bars         marker_color='rgba(255, 165, 0, 0.8)', # Orange to match your overlap boxes         opacity=0.75     )])      # 3. Update the layout for clarity     fig_hist.update_layout(         title_text='Distribution of Gaussian Intersection Densities', # title of plot         xaxis_title_text='Calculated Density (Alpha)', # xaxis label         yaxis_title_text='Frequency (Number of Intersections)', # yaxis label         bargap=0.1, # Gap between bars so it looks clean         template='plotly_dark' # Optional: makes it look like a sleek terminal UI     )      fig_hist.show() else:     print(\"Densities tensor is empty or return_centroids was set to False.\") In\u00a0[8]: Copied! <pre>for name in [\"Approximated Centroids (&gt;0.001)\", \"Overlap Boxes\"]:\n    fig.update_traces(visible=False, selector=dict(name=name))\n\ndef generate_unique_aabb_edges(\n    mins: torch.Tensor, \n    maxs: torch.Tensor, \n    return_mapping: bool = True\n) -&gt; Tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor]]:\n    \"\"\"\n    Generates the unique wireframe edges for a batch of AABBs, \n    stripping out any overlapping edges shared by adjacent voxels.\n    \"\"\"\n    # 1. Extract X, Y, Z components\n    x0, y0, z0 = mins[:, 0:1], mins[:, 1:2], mins[:, 2:3]\n    x1, y1, z1 = maxs[:, 0:1], maxs[:, 1:2], maxs[:, 2:3]\n    \n    # 2. Define the 8 corners for all M boxes\n    c000 = torch.cat([x0, y0, z0], dim=-1).unsqueeze(1)\n    c100 = torch.cat([x1, y0, z0], dim=-1).unsqueeze(1)\n    c010 = torch.cat([x0, y1, z0], dim=-1).unsqueeze(1)\n    c110 = torch.cat([x1, y1, z0], dim=-1).unsqueeze(1)\n    c001 = torch.cat([x0, y0, z1], dim=-1).unsqueeze(1)\n    c101 = torch.cat([x1, y0, z1], dim=-1).unsqueeze(1)\n    c011 = torch.cat([x0, y1, z1], dim=-1).unsqueeze(1)\n    c111 = torch.cat([x1, y1, z1], dim=-1).unsqueeze(1)\n    \n    # 3. Build the Edges\n    sx = torch.cat([c000, c010, c001, c011], dim=1)\n    ex = torch.cat([c100, c110, c101, c111], dim=1)\n    sy = torch.cat([c000, c100, c001, c101], dim=1)\n    ey = torch.cat([c010, c110, c011, c111], dim=1)\n    sz = torch.cat([c000, c100, c010, c110], dim=1)\n    ez = torch.cat([c001, c101, c011, c111], dim=1)\n    \n    # 4. Concatenate and flatten\n    edge_starts_flat = torch.cat([sx, sy, sz], dim=1).view(-1, 3)\n    edge_ends_flat = torch.cat([ex, ey, ez], dim=1).view(-1, 3)\n    \n    # 5. Create a 6D Signature for each edge (start_x, start_y, start_z, end_x, end_y, end_z)\n    edge_signatures = torch.cat([edge_starts_flat, edge_ends_flat], dim=-1)\n    \n    # 6. Filter for Uniqueness!\n    if return_mapping:\n        # inverse_indices maps our compressed unique list back to the original naive list\n        unique_edges, inverse_indices = torch.unique(edge_signatures, dim=0, return_inverse=True)\n        unique_starts = unique_edges[:, 0:3].contiguous()\n        unique_ends = unique_edges[:, 3:6].contiguous()\n        return unique_starts, unique_ends, inverse_indices\n    else:\n        unique_edges = torch.unique(edge_signatures, dim=0)\n        unique_starts = unique_edges[:, 0:3].contiguous()\n        unique_ends = unique_edges[:, 3:6].contiguous()\n        return unique_starts, unique_ends, None\n    \ndef build_plotly_lines(starts, ends):\n    starts_np = starts.cpu().numpy()\n    ends_np = ends.cpu().numpy()\n    \n    x_lines, y_lines, z_lines = [], [], []\n    for s, e in zip(starts_np, ends_np):\n        x_lines.extend([s[0], e[0], None])\n        y_lines.extend([s[1], e[1], None])\n        z_lines.extend([s[2], e[2], None])\n    return x_lines, y_lines, z_lines\n\nedge_starts, edge_ends, inverse_indices = generate_unique_aabb_edges(\n    vx_aabb_mins, vx_aabb_maxs, return_mapping=True\n)\n\nprint(\"Edge Starts Shape:\", edge_starts.shape)\nprint(\"Edge Ends Shape:\", edge_ends.shape)\n\nhit_mask, out_edge_ids, out_gaus_ids = gsbvh.query_edge_pair(\n    edge_starts,\n    edge_ends,\n    means,\n    covis,\n    iso\n)\n\nprint(\"Hit Mask:\", hit_mask.shape)\nprint(\"Output Edge IDs:\", out_edge_ids.shape)\nprint(\"Output Gaussian IDs:\", out_gaus_ids.shape)\nprint(f\"Num Edge Hits: {hit_mask.sum().item()}\")\n\nhit_edge_indices = torch.unique(out_edge_ids)\n\nnum_total_edges = edge_starts.shape[0]\nis_hit_mask = torch.zeros(num_total_edges, dtype=torch.bool, device=edge_starts.device)\n\nif hit_edge_indices.numel() &gt; 0:\n    is_hit_mask[hit_edge_indices] = True\n    \nhit_starts = edge_starts[is_hit_mask]\nhit_ends = edge_ends[is_hit_mask]\n\nmiss_starts = edge_starts[~is_hit_mask]\nmiss_ends = edge_ends[~is_hit_mask]\n\nhit_x, hit_y, hit_z = build_plotly_lines(hit_starts, hit_ends)\nmiss_x, miss_y, miss_z = build_plotly_lines(miss_starts, miss_ends)\n\n# if len(miss_x) &gt; 0:\n#     fig.add_trace(go.Scatter3d(\n#         x=miss_x, y=miss_y, z=miss_z,\n#         mode='lines',\n#         line=dict(color='rgba(0, 0, 255, 0.2)', width=2), # Low opacity blue so it doesn't clutter\n#         name='Missed Edges'\n#     ))\n\n# Add Hit Edges (Green)\nif len(hit_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=hit_x, y=hit_y, z=hit_z,\n        mode='lines',\n        line=dict(color='rgba(0, 255, 0, 1.0)', width=5), # Bright, thick green\n        name='Hit Edges'\n    ))\n\n# Keep the layout clean\nfig.update_layout(\n    title=\"Gaussian Wireframe Edge Intersections\",\n    width=800,\n    height=800,\n    scene=dict(\n        aspectmode='data',\n        xaxis_title='X',\n        yaxis_title='Y',\n        zaxis_title='Z',\n        # bgcolor='rgb(20, 20, 20)' # Dark background makes the glowing lines pop\n    ),\n    # template='plotly_dark'\n)\n\nfig.show()\n</pre> for name in [\"Approximated Centroids (&gt;0.001)\", \"Overlap Boxes\"]:     fig.update_traces(visible=False, selector=dict(name=name))  def generate_unique_aabb_edges(     mins: torch.Tensor,      maxs: torch.Tensor,      return_mapping: bool = True ) -&gt; Tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor]]:     \"\"\"     Generates the unique wireframe edges for a batch of AABBs,      stripping out any overlapping edges shared by adjacent voxels.     \"\"\"     # 1. Extract X, Y, Z components     x0, y0, z0 = mins[:, 0:1], mins[:, 1:2], mins[:, 2:3]     x1, y1, z1 = maxs[:, 0:1], maxs[:, 1:2], maxs[:, 2:3]          # 2. Define the 8 corners for all M boxes     c000 = torch.cat([x0, y0, z0], dim=-1).unsqueeze(1)     c100 = torch.cat([x1, y0, z0], dim=-1).unsqueeze(1)     c010 = torch.cat([x0, y1, z0], dim=-1).unsqueeze(1)     c110 = torch.cat([x1, y1, z0], dim=-1).unsqueeze(1)     c001 = torch.cat([x0, y0, z1], dim=-1).unsqueeze(1)     c101 = torch.cat([x1, y0, z1], dim=-1).unsqueeze(1)     c011 = torch.cat([x0, y1, z1], dim=-1).unsqueeze(1)     c111 = torch.cat([x1, y1, z1], dim=-1).unsqueeze(1)          # 3. Build the Edges     sx = torch.cat([c000, c010, c001, c011], dim=1)     ex = torch.cat([c100, c110, c101, c111], dim=1)     sy = torch.cat([c000, c100, c001, c101], dim=1)     ey = torch.cat([c010, c110, c011, c111], dim=1)     sz = torch.cat([c000, c100, c010, c110], dim=1)     ez = torch.cat([c001, c101, c011, c111], dim=1)          # 4. Concatenate and flatten     edge_starts_flat = torch.cat([sx, sy, sz], dim=1).view(-1, 3)     edge_ends_flat = torch.cat([ex, ey, ez], dim=1).view(-1, 3)          # 5. Create a 6D Signature for each edge (start_x, start_y, start_z, end_x, end_y, end_z)     edge_signatures = torch.cat([edge_starts_flat, edge_ends_flat], dim=-1)          # 6. Filter for Uniqueness!     if return_mapping:         # inverse_indices maps our compressed unique list back to the original naive list         unique_edges, inverse_indices = torch.unique(edge_signatures, dim=0, return_inverse=True)         unique_starts = unique_edges[:, 0:3].contiguous()         unique_ends = unique_edges[:, 3:6].contiguous()         return unique_starts, unique_ends, inverse_indices     else:         unique_edges = torch.unique(edge_signatures, dim=0)         unique_starts = unique_edges[:, 0:3].contiguous()         unique_ends = unique_edges[:, 3:6].contiguous()         return unique_starts, unique_ends, None      def build_plotly_lines(starts, ends):     starts_np = starts.cpu().numpy()     ends_np = ends.cpu().numpy()          x_lines, y_lines, z_lines = [], [], []     for s, e in zip(starts_np, ends_np):         x_lines.extend([s[0], e[0], None])         y_lines.extend([s[1], e[1], None])         z_lines.extend([s[2], e[2], None])     return x_lines, y_lines, z_lines  edge_starts, edge_ends, inverse_indices = generate_unique_aabb_edges(     vx_aabb_mins, vx_aabb_maxs, return_mapping=True )  print(\"Edge Starts Shape:\", edge_starts.shape) print(\"Edge Ends Shape:\", edge_ends.shape)  hit_mask, out_edge_ids, out_gaus_ids = gsbvh.query_edge_pair(     edge_starts,     edge_ends,     means,     covis,     iso )  print(\"Hit Mask:\", hit_mask.shape) print(\"Output Edge IDs:\", out_edge_ids.shape) print(\"Output Gaussian IDs:\", out_gaus_ids.shape) print(f\"Num Edge Hits: {hit_mask.sum().item()}\")  hit_edge_indices = torch.unique(out_edge_ids)  num_total_edges = edge_starts.shape[0] is_hit_mask = torch.zeros(num_total_edges, dtype=torch.bool, device=edge_starts.device)  if hit_edge_indices.numel() &gt; 0:     is_hit_mask[hit_edge_indices] = True      hit_starts = edge_starts[is_hit_mask] hit_ends = edge_ends[is_hit_mask]  miss_starts = edge_starts[~is_hit_mask] miss_ends = edge_ends[~is_hit_mask]  hit_x, hit_y, hit_z = build_plotly_lines(hit_starts, hit_ends) miss_x, miss_y, miss_z = build_plotly_lines(miss_starts, miss_ends)  # if len(miss_x) &gt; 0: #     fig.add_trace(go.Scatter3d( #         x=miss_x, y=miss_y, z=miss_z, #         mode='lines', #         line=dict(color='rgba(0, 0, 255, 0.2)', width=2), # Low opacity blue so it doesn't clutter #         name='Missed Edges' #     ))  # Add Hit Edges (Green) if len(hit_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=hit_x, y=hit_y, z=hit_z,         mode='lines',         line=dict(color='rgba(0, 255, 0, 1.0)', width=5), # Bright, thick green         name='Hit Edges'     ))  # Keep the layout clean fig.update_layout(     title=\"Gaussian Wireframe Edge Intersections\",     width=800,     height=800,     scene=dict(         aspectmode='data',         xaxis_title='X',         yaxis_title='Y',         zaxis_title='Z',         # bgcolor='rgb(20, 20, 20)' # Dark background makes the glowing lines pop     ),     # template='plotly_dark' )  fig.show() <pre>Edge Starts Shape: torch.Size([1944, 3])\nEdge Ends Shape: torch.Size([1944, 3])\nHit Mask: torch.Size([1944])\nOutput Edge IDs: torch.Size([162])\nOutput Gaussian IDs: torch.Size([162])\nNum Edge Hits: 162\n</pre>"},{"location":"examples/gaussians/gs_aabb/#gaussian-splatting-aabb-and-intersection-tests","title":"Gaussian Splatting AABB and Intersection Tests\u00b6","text":"<p>This notebook demonstrates how to compute Axis-Aligned Bounding Boxes (AABB) for 3D Gaussian Splats and test intersections against voxel grids and edges using <code>geocutool</code>.</p>"},{"location":"examples/gaussians/gs_aabb/#1-setup-and-imports","title":"1. Setup and Imports\u00b6","text":"<p>We start by importing PyTorch, NumPy, Plotly, and the necessary functions from <code>geocutool</code>.</p>"},{"location":"examples/gaussians/gs_aabb/#2-utility-functions","title":"2. Utility Functions\u00b6","text":"<p>Here we define helper functions for generating an $n \\times n \\times n$ voxel grid and for building batch wireframes to plot bounding boxes efficiently.</p>"},{"location":"examples/gaussians/gs_aabb/#3-gaussian-splatting-aabb-calculation","title":"3. Gaussian Splatting AABB Calculation\u00b6","text":"<p>We define the properties of a 3D Gaussian (mean, rotation, scale, opacity) and calculate its Axis-Aligned Bounding Box (AABB) using <code>compute_gs_aabb_w_covi</code>. We visualize the ellipsoid and its computed AABB.</p>"},{"location":"examples/gaussians/gs_aabb/#4-voxel-grid-intersection","title":"4. Voxel Grid Intersection\u00b6","text":"<p>We construct a voxel grid and use <code>geocutool</code> to perform a highly optimized broad-phase collision detection between the voxel grid and the Gaussian AABB.</p>"},{"location":"examples/gaussians/gs_aabb/#5-visualizing-overlapping-regions","title":"5. Visualizing Overlapping Regions\u00b6","text":"<p>We extract the bounding boxes of the intersected voxels and plot the overlapping intersection boxes between the voxels and the Gaussian.</p>"},{"location":"examples/gaussians/gs_aabb/#6-visualizing-approximated-centroids","title":"6. Visualizing Approximated Centroids\u00b6","text":"<p>Here, we filter the computed intersection centroids, keeping only those with a density strictly greater than 0.001, and map their colors according to density.</p>"},{"location":"examples/gaussians/gs_aabb/#7-density-distribution-histogram","title":"7. Density Distribution Histogram\u00b6","text":"<p>We plot a histogram of the calculated densities to analyze the density distribution across all intersected voxels.</p>"},{"location":"examples/gaussians/gs_aabb/#8-edge-pair-intersection-queries","title":"8. Edge Pair Intersection Queries\u00b6","text":"<p>Finally, we generate unique AABB wireframe edges for our voxel grid and perform an edge intersection query to determine exactly which edges intersect with the 3D Gaussian.</p>"},{"location":"examples/gaussians/pgs_aabb/","title":"Projected Gaussian Splatting AABB and Intersection Tests","text":"In\u00a0[5]: Copied! <pre>import torch\nimport numpy as np\nimport plotly.graph_objects as go\nfrom typing import Tuple, Optional\nfrom conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb\nfrom conquer3d.data_structure import PGSBVH\n\ndef generate_nxnxn_voxel_grid(n=3, voxel_size=0.1, center=(0.0, 0.0, 0.0), device='cuda'):\n    \"\"\"\n    Generates an n x n x n grid of voxel AABBs centered at a specific coordinate.\n    Returns vx_aabb_mins, vx_aabb_maxs of shape (n**3, 3).\n    \"\"\"\n    # Create dynamic steps to keep the grid perfectly centered.\n    # For n=3 -&gt; [-1.0,  0.0,  1.0]\n    # For n=4 -&gt; [-1.5, -0.5,  0.5,  1.5]\n    steps = torch.arange(n, dtype=torch.float32, device=device) - (n - 1) / 2.0\n    \n    # Create a 3D meshgrid\n    Z, Y, X = torch.meshgrid(steps, steps, steps, indexing='ij')\n    \n    # Flatten the grid and scale by voxel size to get the center coordinates of each voxel\n    centers = torch.stack(\n        [\n            X.flatten(), \n            Y.flatten(), \n            Z.flatten()\n        ], dim=-1\n    ) * voxel_size\n    \n    # Shift the entire grid to the requested center\n    center_tensor = torch.tensor(center, dtype=torch.float32, device=device)\n    centers += center_tensor\n    \n    # Calculate AABB min and max by subtracting/adding half the voxel size\n    half_size = voxel_size / 2.0\n    vx_aabb_mins = centers - half_size\n    vx_aabb_maxs = centers + half_size\n    \n    return vx_aabb_mins.contiguous(), vx_aabb_maxs.contiguous()\n\ndef generate_batch_wireframe(mins, maxs):\n    \"\"\"\n    Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords\n    with None separators to draw all boxes in a single Plotly trace.\n    \"\"\"\n    x_lines, y_lines, z_lines = [], [], []\n    \n    # Convert to numpy if they are tensors\n    mins_np = mins.cpu().numpy()\n    maxs_np = maxs.cpu().numpy()\n    \n    for min_pt, max_pt in zip(mins_np, maxs_np):\n        # 8 corners of the voxel\n        xc = [\n            min_pt[0], max_pt[0], max_pt[0], min_pt[0], \n            min_pt[0], max_pt[0], max_pt[0], min_pt[0]]\n        yc = [\n            min_pt[1], min_pt[1], max_pt[1], max_pt[1], \n            min_pt[1], min_pt[1], max_pt[1], max_pt[1]]\n        zc = [\n            min_pt[2], min_pt[2], min_pt[2], min_pt[2], \n            max_pt[2], max_pt[2], max_pt[2], max_pt[2]]\n\n        # The 12 edge drawing path (with Nones to lift the pen)\n        x_lines.extend([\n            xc[0], xc[1], xc[2], xc[3], xc[0], None, \n            xc[4], xc[5], xc[6], xc[7], xc[4], None, \n            xc[0], xc[4], None, xc[1], xc[5], None, \n            xc[2], xc[6], None, xc[3], xc[7], None])\n        y_lines.extend([\n            yc[0], yc[1], yc[2], yc[3], yc[0], None, \n            yc[4], yc[5], yc[6], yc[7], yc[4], None, \n            yc[0], yc[4], None, yc[1], yc[5], None, \n            yc[2], yc[6], None, yc[3], yc[7], None])\n        z_lines.extend([\n            zc[0], zc[1], zc[2], zc[3], zc[0], None, \n            zc[4], zc[5], zc[6], zc[7], zc[4], None, \n            zc[0], zc[4], None, zc[1], zc[5], None, \n            zc[2], zc[6], None, zc[3], zc[7], None])\n        \n    return x_lines, y_lines, z_lines\n\ndevice = torch.device(\"cuda:0\" if torch.cuda.is_available() else \"cpu\")\n\nmeans = torch.tensor([[0.5, 0.5, 0.5]]).to(device)\nrotations = torch.tensor([[0.5, 0.2, 0.7, 0.3]]).to(device)\nscales = torch.tensor([[0.1, 0.6, 0.5]]).to(device)\nopacities = torch.tensor([[0.5]]).to(device)\nnormals = torch.tensor([[0.5, -1.5, 2.0]]).to(device)\n\nprint(\"Means:\", means.shape)\nprint(\"Rotations:\", rotations.shape)\nprint(\"Scales:\", scales.shape)\nprint(\"Opacities:\", opacities.shape)\nprint(\"Normals:\", normals.shape)\nlevel = 8\niso = 11.345\ntol = 1. / 8.\nrotnorm = True\n\ncovis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm)\naabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, iso, tol)\n\nprint(\"AABB Min:\", aabb_min.shape)\nprint(\"AABB Max:\", aabb_max.shape)\nprint(\"Contact Points:\", contact_points.shape)\nprint(\"Covariance Inverses:\", covis.shape)\n\nmean_np = means[0].cpu().numpy()\nrot_np = rotations[0].cpu().numpy()\nscale_np = scales[0].cpu().numpy()\nnormal_np = normals[0].cpu().numpy()\n\nmin_pt = aabb_min[0].cpu().numpy()\nmax_pt = aabb_max[0].cpu().numpy()\n\ncontact_points_np = contact_points[0].cpu().numpy()\ncovis_np = covis[0].cpu().numpy()\n\nall_contact_points = np.concatenate([contact_points_np, -contact_points_np], axis=0)\n\nvoxelSize = 2.0 / (1 &lt;&lt; level)\nmin_scale = tol * voxelSize\nscale_np = np.maximum(scale_np, min_scale)\n\nu = np.linspace(0, 2 * np.pi, 50)\nv = np.linspace(0, np.pi, 50)\nx_sphere = np.outer(np.cos(u), np.sin(v))\ny_sphere = np.outer(np.sin(u), np.sin(v))\nz_sphere = np.outer(np.ones(np.size(u)), np.cos(v))\n\nrx, ry, rz = scale_np * np.sqrt(iso)\nx_ellipsoid = x_sphere * rx\ny_ellipsoid = y_sphere * ry\nz_ellipsoid = z_sphere * rz\n\npoints = np.vstack(\n    [\n        x_ellipsoid.flatten(), \n        y_ellipsoid.flatten(), \n        z_ellipsoid.flatten()\n    ]\n)\n\nr, x, y, z = rot_np[0], rot_np[1], rot_np[2], rot_np[3]\nif rotnorm:\n    inv_norm = 1.0 / np.sqrt(r*r + x*x + y*y + z*z)\n    r *= inv_norm; x *= inv_norm; y *= inv_norm; z *= inv_norm\n\nR_mat = np.array([\n    [1 - 2*(y*y + z*z), 2*(x*y - r*z), 2*(x*z + r*y)],\n    [2*(x*y + r*z), 1 - 2*(x*x + z*z), 2*(y*z - r*x)],\n    [2*(x*z - r*y), 2*(y*z + r*x), 1 - 2*(x*x + y*y)]\n])\n\nrotated_points = R_mat @ points\nx_final = rotated_points[0, :].reshape(x_sphere.shape) + mean_np[0]\ny_final = rotated_points[1, :].reshape(y_sphere.shape) + mean_np[1]\nz_final = rotated_points[2, :].reshape(z_sphere.shape) + mean_np[2]\n\nx_lines, y_lines, z_lines = generate_batch_wireframe(aabb_min, aabb_max)\n\nfig = go.Figure()\n\nfig.add_trace(go.Surface(\n    x=x_final, y=y_final, z=z_final, \n    opacity=0.6, \n    colorscale='Plasma', \n    showscale=False,\n    name='Gaussian'\n))\n\nfig.add_trace(go.Scatter3d(\n    x=x_lines, y=y_lines, z=z_lines,\n    mode='lines',\n    line=dict(color='red', width=4),\n    name='AABB'\n))\n\nfig.add_trace(go.Scatter3d(\n    x=all_contact_points[0::3] + mean_np[0],\n    y=all_contact_points[1::3] + mean_np[1],\n    z=all_contact_points[2::3] + mean_np[2],\n    mode='markers',\n    marker=dict(size=5, color='green'),\n    name='Contact Points'\n))\n\nfig.add_trace(go.Cone(\n    x=[mean_np[0] + normal_np[0]], y=[mean_np[1] + normal_np[1]], z=[mean_np[2] + normal_np[2]],\n    u=[normal_np[0]], v=[normal_np[1]], w=[normal_np[2]],\n    sizemode=\"absolute\",\n    sizeref=0.5,\n    anchor=\"tip\",\n    colorscale=[[0, 'blue'], [1, 'blue']],\n    showscale=False,\n    name='Normal Arrow'\n))\n\nfig.add_trace(go.Scatter3d(\n    x=[mean_np[0], mean_np[0] + normal_np[0]],\n    y=[mean_np[1], mean_np[1] + normal_np[1]],\n    z=[mean_np[2], mean_np[2] + normal_np[2]],\n    mode='lines',\n    line=dict(color='blue', width=6),\n    name='Normal Line'\n))\n\nfig.update_layout(\n    scene=dict(aspectmode='data'),\n    scene_camera=dict(projection=dict(type=\"orthographic\")),\n    title=\"3D Gaussian and Computed AABB\",\n    # margin=dict(l=0, r=0, b=0, t=40)\n)\n\nfig.show()\n</pre> import torch import numpy as np import plotly.graph_objects as go from typing import Tuple, Optional from conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb from conquer3d.data_structure import PGSBVH  def generate_nxnxn_voxel_grid(n=3, voxel_size=0.1, center=(0.0, 0.0, 0.0), device='cuda'):     \"\"\"     Generates an n x n x n grid of voxel AABBs centered at a specific coordinate.     Returns vx_aabb_mins, vx_aabb_maxs of shape (n**3, 3).     \"\"\"     # Create dynamic steps to keep the grid perfectly centered.     # For n=3 -&gt; [-1.0,  0.0,  1.0]     # For n=4 -&gt; [-1.5, -0.5,  0.5,  1.5]     steps = torch.arange(n, dtype=torch.float32, device=device) - (n - 1) / 2.0          # Create a 3D meshgrid     Z, Y, X = torch.meshgrid(steps, steps, steps, indexing='ij')          # Flatten the grid and scale by voxel size to get the center coordinates of each voxel     centers = torch.stack(         [             X.flatten(),              Y.flatten(),              Z.flatten()         ], dim=-1     ) * voxel_size          # Shift the entire grid to the requested center     center_tensor = torch.tensor(center, dtype=torch.float32, device=device)     centers += center_tensor          # Calculate AABB min and max by subtracting/adding half the voxel size     half_size = voxel_size / 2.0     vx_aabb_mins = centers - half_size     vx_aabb_maxs = centers + half_size          return vx_aabb_mins.contiguous(), vx_aabb_maxs.contiguous()  def generate_batch_wireframe(mins, maxs):     \"\"\"     Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords     with None separators to draw all boxes in a single Plotly trace.     \"\"\"     x_lines, y_lines, z_lines = [], [], []          # Convert to numpy if they are tensors     mins_np = mins.cpu().numpy()     maxs_np = maxs.cpu().numpy()          for min_pt, max_pt in zip(mins_np, maxs_np):         # 8 corners of the voxel         xc = [             min_pt[0], max_pt[0], max_pt[0], min_pt[0],              min_pt[0], max_pt[0], max_pt[0], min_pt[0]]         yc = [             min_pt[1], min_pt[1], max_pt[1], max_pt[1],              min_pt[1], min_pt[1], max_pt[1], max_pt[1]]         zc = [             min_pt[2], min_pt[2], min_pt[2], min_pt[2],              max_pt[2], max_pt[2], max_pt[2], max_pt[2]]          # The 12 edge drawing path (with Nones to lift the pen)         x_lines.extend([             xc[0], xc[1], xc[2], xc[3], xc[0], None,              xc[4], xc[5], xc[6], xc[7], xc[4], None,              xc[0], xc[4], None, xc[1], xc[5], None,              xc[2], xc[6], None, xc[3], xc[7], None])         y_lines.extend([             yc[0], yc[1], yc[2], yc[3], yc[0], None,              yc[4], yc[5], yc[6], yc[7], yc[4], None,              yc[0], yc[4], None, yc[1], yc[5], None,              yc[2], yc[6], None, yc[3], yc[7], None])         z_lines.extend([             zc[0], zc[1], zc[2], zc[3], zc[0], None,              zc[4], zc[5], zc[6], zc[7], zc[4], None,              zc[0], zc[4], None, zc[1], zc[5], None,              zc[2], zc[6], None, zc[3], zc[7], None])              return x_lines, y_lines, z_lines  device = torch.device(\"cuda:0\" if torch.cuda.is_available() else \"cpu\")  means = torch.tensor([[0.5, 0.5, 0.5]]).to(device) rotations = torch.tensor([[0.5, 0.2, 0.7, 0.3]]).to(device) scales = torch.tensor([[0.1, 0.6, 0.5]]).to(device) opacities = torch.tensor([[0.5]]).to(device) normals = torch.tensor([[0.5, -1.5, 2.0]]).to(device)  print(\"Means:\", means.shape) print(\"Rotations:\", rotations.shape) print(\"Scales:\", scales.shape) print(\"Opacities:\", opacities.shape) print(\"Normals:\", normals.shape) level = 8 iso = 11.345 tol = 1. / 8. rotnorm = True  covis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm) aabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, iso, tol)  print(\"AABB Min:\", aabb_min.shape) print(\"AABB Max:\", aabb_max.shape) print(\"Contact Points:\", contact_points.shape) print(\"Covariance Inverses:\", covis.shape)  mean_np = means[0].cpu().numpy() rot_np = rotations[0].cpu().numpy() scale_np = scales[0].cpu().numpy() normal_np = normals[0].cpu().numpy()  min_pt = aabb_min[0].cpu().numpy() max_pt = aabb_max[0].cpu().numpy()  contact_points_np = contact_points[0].cpu().numpy() covis_np = covis[0].cpu().numpy()  all_contact_points = np.concatenate([contact_points_np, -contact_points_np], axis=0)  voxelSize = 2.0 / (1 &lt;&lt; level) min_scale = tol * voxelSize scale_np = np.maximum(scale_np, min_scale)  u = np.linspace(0, 2 * np.pi, 50) v = np.linspace(0, np.pi, 50) x_sphere = np.outer(np.cos(u), np.sin(v)) y_sphere = np.outer(np.sin(u), np.sin(v)) z_sphere = np.outer(np.ones(np.size(u)), np.cos(v))  rx, ry, rz = scale_np * np.sqrt(iso) x_ellipsoid = x_sphere * rx y_ellipsoid = y_sphere * ry z_ellipsoid = z_sphere * rz  points = np.vstack(     [         x_ellipsoid.flatten(),          y_ellipsoid.flatten(),          z_ellipsoid.flatten()     ] )  r, x, y, z = rot_np[0], rot_np[1], rot_np[2], rot_np[3] if rotnorm:     inv_norm = 1.0 / np.sqrt(r*r + x*x + y*y + z*z)     r *= inv_norm; x *= inv_norm; y *= inv_norm; z *= inv_norm  R_mat = np.array([     [1 - 2*(y*y + z*z), 2*(x*y - r*z), 2*(x*z + r*y)],     [2*(x*y + r*z), 1 - 2*(x*x + z*z), 2*(y*z - r*x)],     [2*(x*z - r*y), 2*(y*z + r*x), 1 - 2*(x*x + y*y)] ])  rotated_points = R_mat @ points x_final = rotated_points[0, :].reshape(x_sphere.shape) + mean_np[0] y_final = rotated_points[1, :].reshape(y_sphere.shape) + mean_np[1] z_final = rotated_points[2, :].reshape(z_sphere.shape) + mean_np[2]  x_lines, y_lines, z_lines = generate_batch_wireframe(aabb_min, aabb_max)  fig = go.Figure()  fig.add_trace(go.Surface(     x=x_final, y=y_final, z=z_final,      opacity=0.6,      colorscale='Plasma',      showscale=False,     name='Gaussian' ))  fig.add_trace(go.Scatter3d(     x=x_lines, y=y_lines, z=z_lines,     mode='lines',     line=dict(color='red', width=4),     name='AABB' ))  fig.add_trace(go.Scatter3d(     x=all_contact_points[0::3] + mean_np[0],     y=all_contact_points[1::3] + mean_np[1],     z=all_contact_points[2::3] + mean_np[2],     mode='markers',     marker=dict(size=5, color='green'),     name='Contact Points' ))  fig.add_trace(go.Cone(     x=[mean_np[0] + normal_np[0]], y=[mean_np[1] + normal_np[1]], z=[mean_np[2] + normal_np[2]],     u=[normal_np[0]], v=[normal_np[1]], w=[normal_np[2]],     sizemode=\"absolute\",     sizeref=0.5,     anchor=\"tip\",     colorscale=[[0, 'blue'], [1, 'blue']],     showscale=False,     name='Normal Arrow' ))  fig.add_trace(go.Scatter3d(     x=[mean_np[0], mean_np[0] + normal_np[0]],     y=[mean_np[1], mean_np[1] + normal_np[1]],     z=[mean_np[2], mean_np[2] + normal_np[2]],     mode='lines',     line=dict(color='blue', width=6),     name='Normal Line' ))  fig.update_layout(     scene=dict(aspectmode='data'),     scene_camera=dict(projection=dict(type=\"orthographic\")),     title=\"3D Gaussian and Computed AABB\",     # margin=dict(l=0, r=0, b=0, t=40) )  fig.show() <pre>Means: torch.Size([1, 3])\nRotations: torch.Size([1, 4])\nScales: torch.Size([1, 3])\nOpacities: torch.Size([1, 1])\nNormals: torch.Size([1, 3])\nAABB Min: torch.Size([1, 3])\nAABB Max: torch.Size([1, 3])\nContact Points: torch.Size([1, 9])\nCovariance Inverses: torch.Size([1, 6])\n</pre> In\u00a0[6]: Copied! <pre>vx_aabb_mins, vx_aabb_maxs = generate_nxnxn_voxel_grid(\n    n=8,\n    voxel_size=0.5, \n    center=(0.0, 0.0, 0.0), \n    device=device\n)\n\nreturn_centroids = True\nreturn_centroid_densities = True\n\nprint(\"Voxel AABB Mins:\\n\", vx_aabb_mins.shape)\nprint(\"Voxel AABB Maxs:\\n\", vx_aabb_maxs.shape)\n\npgsbvh = PGSBVH(aabb_min, aabb_max)\n\nhit_mask, out_voxel_ids, out_gaus_ids, centroids, densities = pgsbvh.query_voxel_pair(\n    vx_aabb_mins,\n    vx_aabb_maxs,\n    means,\n    normals,\n    covis,\n    aabb_min,\n    aabb_max,\n    iso,\n    return_centroids,\n    return_centroid_densities\n)\n\nprint(\"Hit Mask:\", hit_mask.shape)\nprint(\"Output Voxel IDs:\", out_voxel_ids.shape)\nprint(\"Output Gaussian IDs:\", out_gaus_ids.shape)\nprint(\"Centroids:\", centroids.shape if return_centroids else \"Not Returned\")\nprint(\"Densities:\", densities.shape if return_centroid_densities else \"Not Returned\")\n\nfor i in range(out_voxel_ids.shape[0]):\n    print(f\"Voxel ID: {out_voxel_ids[i].item()}, Gaussian ID: {out_gaus_ids[i].item()}, Centroid: {centroids[i].cpu().numpy() if return_centroids else 'N/A'}\")\n\nivx_aabb_mins = vx_aabb_mins[hit_mask]\nivx_aabb_maxs = vx_aabb_maxs[hit_mask]\n\nnivx_aabb_mins = vx_aabb_mins[~hit_mask]\nnivx_aabb_maxs = vx_aabb_maxs[~hit_mask]\n\nhit_x, hit_y, hit_z = generate_batch_wireframe(ivx_aabb_mins, ivx_aabb_maxs)\nmiss_x, miss_y, miss_z = generate_batch_wireframe(nivx_aabb_mins, nivx_aabb_maxs)\n\nif len(miss_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=miss_x, y=miss_y, z=miss_z,\n        mode='lines',\n        line=dict(color='rgba(0, 0, 255, 0.3)', width=2), # Lower opacity blue\n        name='Missed Voxels'\n    ))\n\n# Add Hit Voxels (Green)\nif len(hit_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=hit_x, y=hit_y, z=hit_z,\n        mode='lines',\n        line=dict(color='rgba(0, 255, 0, 0.8)', width=4), # Bright green\n        name='Hit Voxels'\n    ))\n\n# Update layout for a better view of the grid\nfig.update_layout(\n    title=\"Gaussian vs. Voxel Grid Intersection\",\n    width=800,\n    height=800,\n    scene=dict(\n        aspectmode='data',\n        xaxis_title='X',\n        yaxis_title='Y',\n        zaxis_title='Z'\n    )\n)\n\nfig.show()\n</pre> vx_aabb_mins, vx_aabb_maxs = generate_nxnxn_voxel_grid(     n=8,     voxel_size=0.5,      center=(0.0, 0.0, 0.0),      device=device )  return_centroids = True return_centroid_densities = True  print(\"Voxel AABB Mins:\\n\", vx_aabb_mins.shape) print(\"Voxel AABB Maxs:\\n\", vx_aabb_maxs.shape)  pgsbvh = PGSBVH(aabb_min, aabb_max)  hit_mask, out_voxel_ids, out_gaus_ids, centroids, densities = pgsbvh.query_voxel_pair(     vx_aabb_mins,     vx_aabb_maxs,     means,     normals,     covis,     aabb_min,     aabb_max,     iso,     return_centroids,     return_centroid_densities )  print(\"Hit Mask:\", hit_mask.shape) print(\"Output Voxel IDs:\", out_voxel_ids.shape) print(\"Output Gaussian IDs:\", out_gaus_ids.shape) print(\"Centroids:\", centroids.shape if return_centroids else \"Not Returned\") print(\"Densities:\", densities.shape if return_centroid_densities else \"Not Returned\")  for i in range(out_voxel_ids.shape[0]):     print(f\"Voxel ID: {out_voxel_ids[i].item()}, Gaussian ID: {out_gaus_ids[i].item()}, Centroid: {centroids[i].cpu().numpy() if return_centroids else 'N/A'}\")  ivx_aabb_mins = vx_aabb_mins[hit_mask] ivx_aabb_maxs = vx_aabb_maxs[hit_mask]  nivx_aabb_mins = vx_aabb_mins[~hit_mask] nivx_aabb_maxs = vx_aabb_maxs[~hit_mask]  hit_x, hit_y, hit_z = generate_batch_wireframe(ivx_aabb_mins, ivx_aabb_maxs) miss_x, miss_y, miss_z = generate_batch_wireframe(nivx_aabb_mins, nivx_aabb_maxs)  if len(miss_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=miss_x, y=miss_y, z=miss_z,         mode='lines',         line=dict(color='rgba(0, 0, 255, 0.3)', width=2), # Lower opacity blue         name='Missed Voxels'     ))  # Add Hit Voxels (Green) if len(hit_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=hit_x, y=hit_y, z=hit_z,         mode='lines',         line=dict(color='rgba(0, 255, 0, 0.8)', width=4), # Bright green         name='Hit Voxels'     ))  # Update layout for a better view of the grid fig.update_layout(     title=\"Gaussian vs. Voxel Grid Intersection\",     width=800,     height=800,     scene=dict(         aspectmode='data',         xaxis_title='X',         yaxis_title='Y',         zaxis_title='Z'     ) )  fig.show() <pre>Voxel AABB Mins:\n torch.Size([512, 3])\nVoxel AABB Maxs:\n torch.Size([512, 3])\nHit Mask: torch.Size([512])\nOutput Voxel IDs: torch.Size([53])\nOutput Gaussian IDs: torch.Size([53])\nCentroids: torch.Size([53, 3])\nDensities: torch.Size([53])\nVoxel ID: 230, Gaussian ID: 0, Centroid: [1.125      0.04166667 0.        ]\nVoxel ID: 231, Gaussian ID: 0, Centroid: [1.5        0.16666667 0.        ]\nVoxel ID: 274, Gaussian ID: 0, Centroid: [-0.5 -0.5  0. ]\nVoxel ID: 275, Gaussian ID: 0, Centroid: [-0.5 -0.5  0. ]\nVoxel ID: 282, Gaussian ID: 0, Centroid: [-0.5     -0.375    0.09375]\nVoxel ID: 283, Gaussian ID: 0, Centroid: [-0.33333334 -0.30555555  0.10416667]\nVoxel ID: 284, Gaussian ID: 0, Centroid: [ 0.25       -0.12499999  0.09375   ]\nVoxel ID: 285, Gaussian ID: 0, Centroid: [ 0.8        -0.03333333  0.025     ]\nVoxel ID: 286, Gaussian ID: 0, Centroid: [1. 0. 0.]\nVoxel ID: 290, Gaussian ID: 0, Centroid: [-0.5         0.08333334  0.4375    ]\nVoxel ID: 291, Gaussian ID: 0, Centroid: [-0.25     0.125    0.40625]\nVoxel ID: 292, Gaussian ID: 0, Centroid: [0.33333334 0.30555558 0.39583334]\nVoxel ID: 293, Gaussian ID: 0, Centroid: [0.75 0.25 0.25]\nVoxel ID: 294, Gaussian ID: 0, Centroid: [1.1666667  0.19444446 0.10416667]\nVoxel ID: 295, Gaussian ID: 0, Centroid: [1.5        0.33333334 0.125     ]\nVoxel ID: 301, Gaussian ID: 0, Centroid: [0.7        0.53333336 0.475     ]\nVoxel ID: 302, Gaussian ID: 0, Centroid: [1.25    0.625   0.40625]\nVoxel ID: 303, Gaussian ID: 0, Centroid: [1.5       0.6666667 0.375    ]\nVoxel ID: 210, Gaussian ID: 0, Centroid: [-0.5 -0.5  0. ]\nVoxel ID: 211, Gaussian ID: 0, Centroid: [-0.375   -0.5     -0.03125]\nVoxel ID: 212, Gaussian ID: 0, Centroid: [ 0.25   -0.5    -0.1875]\nVoxel ID: 213, Gaussian ID: 0, Centroid: [ 0.5  -0.5  -0.25]\nVoxel ID: 219, Gaussian ID: 0, Centroid: [-0.3        -0.46666667 -0.025     ]\nVoxel ID: 220, Gaussian ID: 0, Centroid: [ 0.25       -0.37499997 -0.09375   ]\nVoxel ID: 221, Gaussian ID: 0, Centroid: [ 0.8        -0.13333333 -0.05      ]\nVoxel ID: 222, Gaussian ID: 0, Centroid: [1. 0. 0.]\nVoxel ID: 426, Gaussian ID: 0, Centroid: [-0.5        0.8333334  1.       ]\nVoxel ID: 427, Gaussian ID: 0, Centroid: [-0.125      0.9583334  1.       ]\nVoxel ID: 428, Gaussian ID: 0, Centroid: [0. 1. 1.]\nVoxel ID: 435, Gaussian ID: 0, Centroid: [0. 1. 1.]\nVoxel ID: 436, Gaussian ID: 0, Centroid: [0.2       1.1333333 1.0500001]\nVoxel ID: 437, Gaussian ID: 0, Centroid: [0.75    1.375   1.09375]\nVoxel ID: 438, Gaussian ID: 0, Centroid: [1.3000001 1.4666667 1.025    ]\nVoxel ID: 439, Gaussian ID: 0, Centroid: [1.5 1.5 1. ]\nVoxel ID: 444, Gaussian ID: 0, Centroid: [0.5  1.5  1.25]\nVoxel ID: 445, Gaussian ID: 0, Centroid: [0.75   1.5    1.1875]\nVoxel ID: 446, Gaussian ID: 0, Centroid: [1.375   1.5     1.03125]\nVoxel ID: 447, Gaussian ID: 0, Centroid: [1.5 1.5 1. ]\nVoxel ID: 354, Gaussian ID: 0, Centroid: [-0.5         0.33333334  0.625     ]\nVoxel ID: 355, Gaussian ID: 0, Centroid: [-0.25     0.375    0.59375]\nVoxel ID: 356, Gaussian ID: 0, Centroid: [0.3        0.4666667  0.52500004]\nVoxel ID: 357, Gaussian ID: 0, Centroid: [0.5 0.5 0.5]\nVoxel ID: 362, Gaussian ID: 0, Centroid: [-0.5        0.6666667  0.875    ]\nVoxel ID: 363, Gaussian ID: 0, Centroid: [-0.16666667  0.8055556   0.8958334 ]\nVoxel ID: 364, Gaussian ID: 0, Centroid: [0.25 0.75 0.75]\nVoxel ID: 365, Gaussian ID: 0, Centroid: [0.6666667  0.69444454 0.6041667 ]\nVoxel ID: 366, Gaussian ID: 0, Centroid: [1.25    0.875   0.59375]\nVoxel ID: 367, Gaussian ID: 0, Centroid: [1.5       0.9166667 0.5625   ]\nVoxel ID: 372, Gaussian ID: 0, Centroid: [0.2       1.0333333 0.975    ]\nVoxel ID: 373, Gaussian ID: 0, Centroid: [0.75    1.125   0.90625]\nVoxel ID: 374, Gaussian ID: 0, Centroid: [1.3333334 1.3055556 0.8958334]\nVoxel ID: 375, Gaussian ID: 0, Centroid: [1.5     1.375   0.90625]\nVoxel ID: 383, Gaussian ID: 0, Centroid: [1.5 1.5 1. ]\n</pre> In\u00a0[7]: Copied! <pre>for name in [\"Missed Voxels\", \"Hit Voxels\", \"Contact Points\", \"AABB\"]:\n    fig.update_traces(visible=False, selector=dict(name=name))\n\nhit_vx_mins = vx_aabb_mins[out_voxel_ids]\nhit_vx_maxs = vx_aabb_maxs[out_voxel_ids]\nhit_gs_mins = aabb_min[out_gaus_ids]\nhit_gs_maxs = aabb_max[out_gaus_ids]\n\noverlap_mins = torch.max(hit_vx_mins, hit_gs_mins)\noverlap_maxs = torch.min(hit_vx_maxs, hit_gs_maxs)\n\noverlap_x, overlap_y, overlap_z = generate_batch_wireframe(overlap_mins, overlap_maxs)\n\nif len(overlap_x) &gt; 0:\n    fig.add_trace(go.Scatter3d(\n        x=overlap_x, y=overlap_y, z=overlap_z,\n        mode='lines',\n        line=dict(color='rgba(255, 165, 0, 1.0)', width=5), # Thick Orange\n        name='Overlap Boxes'\n    ))\n\nif return_centroids and centroids is not None and len(centroids) &gt; 0:\n    centroids_np = centroids.cpu().numpy()\n    densities_np = densities.cpu().numpy() if return_centroid_densities else np.zeros(centroids_np.shape[0])\n    \n    fig.add_trace(go.Scatter3d(\n        x=centroids_np[:, 0],\n        y=centroids_np[:, 1],\n        z=centroids_np[:, 2],\n        mode='markers',\n        marker=dict(\n            color=densities_np,       # Map the color to the exact density value!\n            colorscale='Plasma',      # 'Plasma' goes from very dark purple (0.0) to bright yellow (high)\n            cmin=0.0,                 # Force 0.0 to be the darkest color\n            cmax=float(densities_np.max()), # Force the max density to be the brightest color\n            showscale=True,           # Adds a colorbar legend to the right side of the screen\n            colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),\n            size=6,\n            symbol='diamond',\n            line=dict(color='rgba(255, 255, 255, 0.5)', width=1)\n        ),\n        name='Approximated Centroids'\n    ))\n\nfig.show()\n</pre> for name in [\"Missed Voxels\", \"Hit Voxels\", \"Contact Points\", \"AABB\"]:     fig.update_traces(visible=False, selector=dict(name=name))  hit_vx_mins = vx_aabb_mins[out_voxel_ids] hit_vx_maxs = vx_aabb_maxs[out_voxel_ids] hit_gs_mins = aabb_min[out_gaus_ids] hit_gs_maxs = aabb_max[out_gaus_ids]  overlap_mins = torch.max(hit_vx_mins, hit_gs_mins) overlap_maxs = torch.min(hit_vx_maxs, hit_gs_maxs)  overlap_x, overlap_y, overlap_z = generate_batch_wireframe(overlap_mins, overlap_maxs)  if len(overlap_x) &gt; 0:     fig.add_trace(go.Scatter3d(         x=overlap_x, y=overlap_y, z=overlap_z,         mode='lines',         line=dict(color='rgba(255, 165, 0, 1.0)', width=5), # Thick Orange         name='Overlap Boxes'     ))  if return_centroids and centroids is not None and len(centroids) &gt; 0:     centroids_np = centroids.cpu().numpy()     densities_np = densities.cpu().numpy() if return_centroid_densities else np.zeros(centroids_np.shape[0])          fig.add_trace(go.Scatter3d(         x=centroids_np[:, 0],         y=centroids_np[:, 1],         z=centroids_np[:, 2],         mode='markers',         marker=dict(             color=densities_np,       # Map the color to the exact density value!             colorscale='Plasma',      # 'Plasma' goes from very dark purple (0.0) to bright yellow (high)             cmin=0.0,                 # Force 0.0 to be the darkest color             cmax=float(densities_np.max()), # Force the max density to be the brightest color             showscale=True,           # Adds a colorbar legend to the right side of the screen             colorbar=dict(title=\"Density\", thickness=15, len=0.5, xpad=50),             size=6,             symbol='diamond',             line=dict(color='rgba(255, 255, 255, 0.5)', width=1)         ),         name='Approximated Centroids'     ))  fig.show() In\u00a0[8]: Copied! <pre>def generate_unique_aabb_edges(\n    mins: torch.Tensor, \n    maxs: torch.Tensor, \n    return_mapping: bool = True\n) -&gt; Tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor]]:\n    \"\"\"\n    Generates the unique wireframe edges for a batch of AABBs, \n    stripping out any overlapping edges shared by adjacent voxels.\n    \"\"\"\n    # 1. Extract X, Y, Z components\n    x0, y0, z0 = mins[:, 0:1], mins[:, 1:2], mins[:, 2:3]\n    x1, y1, z1 = maxs[:, 0:1], maxs[:, 1:2], maxs[:, 2:3]\n    \n    # 2. Define the 8 corners for all M boxes\n    c000 = torch.cat([x0, y0, z0], dim=-1).unsqueeze(1)\n    c100 = torch.cat([x1, y0, z0], dim=-1).unsqueeze(1)\n    c010 = torch.cat([x0, y1, z0], dim=-1).unsqueeze(1)\n    c110 = torch.cat([x1, y1, z0], dim=-1).unsqueeze(1)\n    c001 = torch.cat([x0, y0, z1], dim=-1).unsqueeze(1)\n    c101 = torch.cat([x1, y0, z1], dim=-1).unsqueeze(1)\n    c011 = torch.cat([x0, y1, z1], dim=-1).unsqueeze(1)\n    c111 = torch.cat([x1, y1, z1], dim=-1).unsqueeze(1)\n    \n    # 3. Build the Edges\n    sx = torch.cat([c000, c010, c001, c011], dim=1)\n    ex = torch.cat([c100, c110, c101, c111], dim=1)\n    sy = torch.cat([c000, c100, c001, c101], dim=1)\n    ey = torch.cat([c010, c110, c011, c111], dim=1)\n    sz = torch.cat([c000, c100, c010, c110], dim=1)\n    ez = torch.cat([c001, c101, c011, c111], dim=1)\n    \n    # 4. Concatenate and flatten\n    edge_starts_flat = torch.cat([sx, sy, sz], dim=1).view(-1, 3)\n    edge_ends_flat = torch.cat([ex, ey, ez], dim=1).view(-1, 3)\n    \n    # 5. Create a 6D Signature for each edge (start_x, start_y, start_z, end_x, end_y, end_z)\n    edge_signatures = torch.cat([edge_starts_flat, edge_ends_flat], dim=-1)\n    \n    # 6. Filter for Uniqueness!\n    if return_mapping:\n        # inverse_indices maps our compressed unique list back to the original naive list\n        unique_edges, inverse_indices = torch.unique(edge_signatures, dim=0, return_inverse=True)\n        unique_starts = unique_edges[:, 0:3].contiguous()\n        unique_ends = unique_edges[:, 3:6].contiguous()\n        return unique_starts, unique_ends, inverse_indices\n    else:\n        unique_edges = torch.unique(edge_signatures, dim=0)\n        unique_starts = unique_edges[:, 0:3].contiguous()\n        unique_ends = unique_edges[:, 3:6].contiguous()\n        return unique_starts, unique_ends, None\n\nedge_starts, edge_ends, inverse_indices = generate_unique_aabb_edges(\n    vx_aabb_mins, vx_aabb_maxs, return_mapping=True\n)\n\nhit_mask, out_gaus_ids = pgsbvh.query_edge(\n    edge_starts,\n    edge_ends,\n    means,\n    normals,\n    opacities,\n    covis,\n    iso\n)\n\nprint(\"Hit Mask:\", hit_mask.shape)\nprint(\"Output Gaussian IDs:\", out_gaus_ids.shape)\nprint(f\"Num Edge Hits: {hit_mask.sum().item()}\")\nprint(f\"Out Gaussian IDs unique: {torch.unique(out_gaus_ids)}\")\nprint((torch.where(hit_mask)[0] == torch.where(out_gaus_ids != -1)[0]).all())\n</pre> def generate_unique_aabb_edges(     mins: torch.Tensor,      maxs: torch.Tensor,      return_mapping: bool = True ) -&gt; Tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor]]:     \"\"\"     Generates the unique wireframe edges for a batch of AABBs,      stripping out any overlapping edges shared by adjacent voxels.     \"\"\"     # 1. Extract X, Y, Z components     x0, y0, z0 = mins[:, 0:1], mins[:, 1:2], mins[:, 2:3]     x1, y1, z1 = maxs[:, 0:1], maxs[:, 1:2], maxs[:, 2:3]          # 2. Define the 8 corners for all M boxes     c000 = torch.cat([x0, y0, z0], dim=-1).unsqueeze(1)     c100 = torch.cat([x1, y0, z0], dim=-1).unsqueeze(1)     c010 = torch.cat([x0, y1, z0], dim=-1).unsqueeze(1)     c110 = torch.cat([x1, y1, z0], dim=-1).unsqueeze(1)     c001 = torch.cat([x0, y0, z1], dim=-1).unsqueeze(1)     c101 = torch.cat([x1, y0, z1], dim=-1).unsqueeze(1)     c011 = torch.cat([x0, y1, z1], dim=-1).unsqueeze(1)     c111 = torch.cat([x1, y1, z1], dim=-1).unsqueeze(1)          # 3. Build the Edges     sx = torch.cat([c000, c010, c001, c011], dim=1)     ex = torch.cat([c100, c110, c101, c111], dim=1)     sy = torch.cat([c000, c100, c001, c101], dim=1)     ey = torch.cat([c010, c110, c011, c111], dim=1)     sz = torch.cat([c000, c100, c010, c110], dim=1)     ez = torch.cat([c001, c101, c011, c111], dim=1)          # 4. Concatenate and flatten     edge_starts_flat = torch.cat([sx, sy, sz], dim=1).view(-1, 3)     edge_ends_flat = torch.cat([ex, ey, ez], dim=1).view(-1, 3)          # 5. Create a 6D Signature for each edge (start_x, start_y, start_z, end_x, end_y, end_z)     edge_signatures = torch.cat([edge_starts_flat, edge_ends_flat], dim=-1)          # 6. Filter for Uniqueness!     if return_mapping:         # inverse_indices maps our compressed unique list back to the original naive list         unique_edges, inverse_indices = torch.unique(edge_signatures, dim=0, return_inverse=True)         unique_starts = unique_edges[:, 0:3].contiguous()         unique_ends = unique_edges[:, 3:6].contiguous()         return unique_starts, unique_ends, inverse_indices     else:         unique_edges = torch.unique(edge_signatures, dim=0)         unique_starts = unique_edges[:, 0:3].contiguous()         unique_ends = unique_edges[:, 3:6].contiguous()         return unique_starts, unique_ends, None  edge_starts, edge_ends, inverse_indices = generate_unique_aabb_edges(     vx_aabb_mins, vx_aabb_maxs, return_mapping=True )  hit_mask, out_gaus_ids = pgsbvh.query_edge(     edge_starts,     edge_ends,     means,     normals,     opacities,     covis,     iso )  print(\"Hit Mask:\", hit_mask.shape) print(\"Output Gaussian IDs:\", out_gaus_ids.shape) print(f\"Num Edge Hits: {hit_mask.sum().item()}\") print(f\"Out Gaussian IDs unique: {torch.unique(out_gaus_ids)}\") print((torch.where(hit_mask)[0] == torch.where(out_gaus_ids != -1)[0]).all()) <pre>Hit Mask: torch.Size([1944])\nOutput Gaussian IDs: torch.Size([1944])\nNum Edge Hits: 54\nOut Gaussian IDs unique: tensor([-1,  0], device='cuda:0')\ntensor(True, device='cuda:0')\n</pre>"},{"location":"examples/gaussians/pgs_aabb/#projected-gaussian-splatting-aabb-and-intersection-tests","title":"Projected Gaussian Splatting AABB and Intersection Tests\u00b6","text":"<p>This notebook demonstrates how to calculate Axis-Aligned Bounding Boxes (AABB) for Projected Gaussian Splats (PGS) and perform collision detection against voxel grids and edges.</p>"},{"location":"examples/gaussians/pgs_aabb/#1-setup-and-initialization","title":"1. Setup and Initialization\u00b6","text":"<p>We import the necessary libraries including PyTorch, Plotly, and <code>geocutool</code>. We define the parameters of our 3D Gaussians and build utility functions for plotting.</p>"},{"location":"examples/gaussians/pgs_aabb/#2-voxel-grid-intersections","title":"2. Voxel Grid Intersections\u00b6","text":"<p>We generate a uniform $n \\times n \\times n$ voxel grid and query intersections between these voxels and the Projected Gaussians. The output reveals exactly which voxel intersects with which Gaussian.</p>"},{"location":"examples/gaussians/pgs_aabb/#3-visualizing-intersections","title":"3. Visualizing Intersections\u00b6","text":"<p>We extract the bounding boxes of the intersecting voxels and overlay them with the Gaussians' bounding boxes. This visualizes the regions of overlap.</p>"},{"location":"examples/gaussians/pgs_aabb/#4-edge-intersection-queries","title":"4. Edge Intersection Queries\u00b6","text":"<p>To perform more precise collision tests, we extract the unique wireframe edges of our voxel grid and query exactly which edges penetrate the Projected Gaussians.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/","title":"PGS Tangency Radii","text":"In\u00a0[1]: Copied! <pre>import torch\nimport numpy as np\nimport plotly.graph_objects as go\nimport math\nimport trimesh\nfrom conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb\nfrom conquer3d.primitive.pgs import solve_pgs_cluster_tangency_radius\n</pre> import torch import numpy as np import plotly.graph_objects as go import math import trimesh from conquer3d.primitive.gs import compute_gs_covi, compute_gs_aabb from conquer3d.primitive.pgs import solve_pgs_cluster_tangency_radius In\u00a0[2]: Copied! <pre>def generate_batch_wireframe(mins, maxs):\n    \"\"\"\n    Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords\n    with None separators to draw all boxes in a single Plotly trace.\n    \"\"\"\n    x_lines, y_lines, z_lines = [], [], []\n    \n    # Convert to numpy if they are tensors\n    mins_np = mins.cpu().numpy()\n    maxs_np = maxs.cpu().numpy()\n    \n    for min_pt, max_pt in zip(mins_np, maxs_np):\n        # 8 corners of the voxel\n        xc = [\n            min_pt[0], max_pt[0], max_pt[0], min_pt[0], \n            min_pt[0], max_pt[0], max_pt[0], min_pt[0]]\n        yc = [\n            min_pt[1], min_pt[1], max_pt[1], max_pt[1], \n            min_pt[1], min_pt[1], max_pt[1], max_pt[1]]\n        zc = [\n            min_pt[2], min_pt[2], min_pt[2], min_pt[2], \n            max_pt[2], max_pt[2], max_pt[2], max_pt[2]]\n\n        # The 12 edge drawing path (with Nones to lift the pen)\n        x_lines.extend([\n            xc[0], xc[1], xc[2], xc[3], xc[0], None, \n            xc[4], xc[5], xc[6], xc[7], xc[4], None, \n            xc[0], xc[4], None, xc[1], xc[5], None, \n            xc[2], xc[6], None, xc[3], xc[7], None])\n        y_lines.extend([\n            yc[0], yc[1], yc[2], yc[3], yc[0], None, \n            yc[4], yc[5], yc[6], yc[7], yc[4], None, \n            yc[0], yc[4], None, yc[1], yc[5], None, \n            yc[2], yc[6], None, yc[3], yc[7], None])\n        z_lines.extend([\n            zc[0], zc[1], zc[2], zc[3], zc[0], None, \n            zc[4], zc[5], zc[6], zc[7], zc[4], None, \n            zc[0], zc[4], None, zc[1], zc[5], None, \n            zc[2], zc[6], None, zc[3], zc[7], None])\n        \n    return x_lines, y_lines, z_lines\n</pre> def generate_batch_wireframe(mins, maxs):     \"\"\"     Takes N voxel min/max tensors, returns single flattened lists of x, y, z coords     with None separators to draw all boxes in a single Plotly trace.     \"\"\"     x_lines, y_lines, z_lines = [], [], []          # Convert to numpy if they are tensors     mins_np = mins.cpu().numpy()     maxs_np = maxs.cpu().numpy()          for min_pt, max_pt in zip(mins_np, maxs_np):         # 8 corners of the voxel         xc = [             min_pt[0], max_pt[0], max_pt[0], min_pt[0],              min_pt[0], max_pt[0], max_pt[0], min_pt[0]]         yc = [             min_pt[1], min_pt[1], max_pt[1], max_pt[1],              min_pt[1], min_pt[1], max_pt[1], max_pt[1]]         zc = [             min_pt[2], min_pt[2], min_pt[2], min_pt[2],              max_pt[2], max_pt[2], max_pt[2], max_pt[2]]          # The 12 edge drawing path (with Nones to lift the pen)         x_lines.extend([             xc[0], xc[1], xc[2], xc[3], xc[0], None,              xc[4], xc[5], xc[6], xc[7], xc[4], None,              xc[0], xc[4], None, xc[1], xc[5], None,              xc[2], xc[6], None, xc[3], xc[7], None])         y_lines.extend([             yc[0], yc[1], yc[2], yc[3], yc[0], None,              yc[4], yc[5], yc[6], yc[7], yc[4], None,              yc[0], yc[4], None, yc[1], yc[5], None,              yc[2], yc[6], None, yc[3], yc[7], None])         z_lines.extend([             zc[0], zc[1], zc[2], zc[3], zc[0], None,              zc[4], zc[5], zc[6], zc[7], zc[4], None,              zc[0], zc[4], None, zc[1], zc[5], None,              zc[2], zc[6], None, zc[3], zc[7], None])              return x_lines, y_lines, z_lines In\u00a0[3]: Copied! <pre>vertices = [\n    [-1, -1, -0.8],\n    [-1, 1, -0.8],\n    [1, -1, -0.8],\n    [1, 1, -0.8],\n    [-1, 0, 0],\n    [1, 0, 0]\n]\n\nfaces = [\n    [0, 1, 2],\n    [1, 3, 2],\n    [0, 5, 4],\n    [0, 2, 5],\n    [4, 3, 1],\n    [4, 5, 3],\n    [2, 3, 5],\n    [0, 4, 1],\n]\n\nvertices = np.array(vertices)\n\nnum_points = 10000\n\nmesh = trimesh.Trimesh(vertices=vertices, faces=faces)\n\npoints, face_index = trimesh.sample.sample_surface(mesh, num_points)\nnormals = mesh.face_normals[face_index]\n\ndevice = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\n\nmeans = torch.from_numpy(points).float().to(device)\ngs_normals = torch.from_numpy(normals).float().to(device)\nrotations = torch.rand(means.shape[0], 4).float().to(device)\nscales = torch.ones(means.shape[0], 3).float().to(device) * 0.05\nopacities = torch.rand(means.shape[0]).float().to(device)\n\nprint(\"Means shape:\", means.shape)\nprint(\"Normals shape:\", gs_normals.shape)\nprint(\"Rotations shape:\", rotations.shape)\nprint(\"Scales shape:\", scales.shape)\nprint(f\"Scales mean: {scales.mean().item():.4f}, std: {scales.std().item():.4f}\")\nprint(\"Opacities shape:\", opacities.shape)\n</pre> vertices = [     [-1, -1, -0.8],     [-1, 1, -0.8],     [1, -1, -0.8],     [1, 1, -0.8],     [-1, 0, 0],     [1, 0, 0] ]  faces = [     [0, 1, 2],     [1, 3, 2],     [0, 5, 4],     [0, 2, 5],     [4, 3, 1],     [4, 5, 3],     [2, 3, 5],     [0, 4, 1], ]  vertices = np.array(vertices)  num_points = 10000  mesh = trimesh.Trimesh(vertices=vertices, faces=faces)  points, face_index = trimesh.sample.sample_surface(mesh, num_points) normals = mesh.face_normals[face_index]  device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')  means = torch.from_numpy(points).float().to(device) gs_normals = torch.from_numpy(normals).float().to(device) rotations = torch.rand(means.shape[0], 4).float().to(device) scales = torch.ones(means.shape[0], 3).float().to(device) * 0.05 opacities = torch.rand(means.shape[0]).float().to(device)  print(\"Means shape:\", means.shape) print(\"Normals shape:\", gs_normals.shape) print(\"Rotations shape:\", rotations.shape) print(\"Scales shape:\", scales.shape) print(f\"Scales mean: {scales.mean().item():.4f}, std: {scales.std().item():.4f}\") print(\"Opacities shape:\", opacities.shape) <pre>Means shape: torch.Size([10000, 3])\nNormals shape: torch.Size([10000, 3])\nRotations shape: torch.Size([10000, 4])\nScales shape: torch.Size([10000, 3])\nScales mean: 0.0500, std: 0.0000\nOpacities shape: torch.Size([10000])\n</pre> In\u00a0[4]: Copied! <pre>res = 16\n\nmax_level = int(math.log2(res))\nmin_level = min(4, max(1, max_level - 1))\n\nlevel = max_level\niso = 0.1\ntol = 1. / 8.\nrotnorm = True\nk=25\n\ncovis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm)\nprint(\"Covariance Inverses shape:\", covis.shape)\n\nisos, invalid_mask = solve_pgs_cluster_tangency_radius(means, gs_normals, covis, k)\nprint(\"Tangency Radii shape:\", isos.shape)\nprint(\"Invalid Mask shape:\", invalid_mask.shape)\nprint(f\"Num invalid: {invalid_mask.sum().item()}, valid: {(~invalid_mask).sum().item()}\")\n\nisos_min = isos[~invalid_mask].min()\nisos_mean = isos[~invalid_mask].mean()\nisos_max = isos[~invalid_mask].max()\n\nprint(f\"Isos mean: {isos_mean.item():.6f}, min: {isos_min.item():.6f}, max: {isos_max.item():.6f}\")\n\nisos[invalid_mask] = isos_max\n\nprint(f\"Isos mean: {isos.mean().item():.6f}, min: {isos.min().item():.6f}, max: {isos.max().item():.6f}\")\n\naabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, isos, tol)\n\nprint(\"AABB Min:\", aabb_min.shape)\nprint(\"AABB Max:\", aabb_max.shape)\nprint(\"Contact Points:\", contact_points.shape)\nprint(\"Gaussian Normals:\", gs_normals.shape)\n\nmeans_np = means.cpu().numpy()\ncontact_points_np = contact_points.cpu().numpy()\nrot_np = rotations.cpu().numpy()\nscales_np = scales.cpu().numpy()\nnormals_np = gs_normals.cpu().numpy()\ncovis_np = covis.cpu().numpy()\nisos_np = isos.cpu().numpy()\n</pre> res = 16  max_level = int(math.log2(res)) min_level = min(4, max(1, max_level - 1))  level = max_level iso = 0.1 tol = 1. / 8. rotnorm = True k=25  covis = compute_gs_covi(means, rotations, scales, level, tol, rotnorm) print(\"Covariance Inverses shape:\", covis.shape)  isos, invalid_mask = solve_pgs_cluster_tangency_radius(means, gs_normals, covis, k) print(\"Tangency Radii shape:\", isos.shape) print(\"Invalid Mask shape:\", invalid_mask.shape) print(f\"Num invalid: {invalid_mask.sum().item()}, valid: {(~invalid_mask).sum().item()}\")  isos_min = isos[~invalid_mask].min() isos_mean = isos[~invalid_mask].mean() isos_max = isos[~invalid_mask].max()  print(f\"Isos mean: {isos_mean.item():.6f}, min: {isos_min.item():.6f}, max: {isos_max.item():.6f}\")  isos[invalid_mask] = isos_max  print(f\"Isos mean: {isos.mean().item():.6f}, min: {isos.min().item():.6f}, max: {isos.max().item():.6f}\")  aabb_min, aabb_max, contact_points = compute_gs_aabb(means, scales, covis, level, isos, tol)  print(\"AABB Min:\", aabb_min.shape) print(\"AABB Max:\", aabb_max.shape) print(\"Contact Points:\", contact_points.shape) print(\"Gaussian Normals:\", gs_normals.shape)  means_np = means.cpu().numpy() contact_points_np = contact_points.cpu().numpy() rot_np = rotations.cpu().numpy() scales_np = scales.cpu().numpy() normals_np = gs_normals.cpu().numpy() covis_np = covis.cpu().numpy() isos_np = isos.cpu().numpy() <pre>Covariance Inverses shape: torch.Size([10000, 6])\nTangency Radii shape: torch.Size([10000])\nInvalid Mask shape: torch.Size([10000])\nNum invalid: 7350, valid: 2650\nIsos mean: 1.693535, min: 0.000000, max: 13.774948\nIsos mean: 10.573373, min: 0.000000, max: 13.774948\nAABB Min: torch.Size([10000, 3])\nAABB Max: torch.Size([10000, 3])\nContact Points: torch.Size([10000, 9])\nGaussian Normals: torch.Size([10000, 3])\n</pre> In\u00a0[5]: Copied! <pre>def quat_to_rotmat(q):\n    \"\"\"Converts a quaternion [w, x, y, z] to a 3x3 rotation matrix.\"\"\"\n    w, x, y, z = q[0], q[1], q[2], q[3]\n    n = np.sqrt(x*x + y*y + z*z + w*w)\n    w, x, y, z = w/n, x/n, y/n, z/n\n    \n    R = np.array([\n        [1 - 2*(y**2 + z**2), 2*(x*y - w*z),     2*(x*z + w*y)],\n        [2*(x*y + w*z),     1 - 2*(x**2 + z**2), 2*(y*z - w*x)],\n        [2*(x*z - w*y),     2*(y*z + w*x),     1 - 2*(x**2 + y**2)]\n    ])\n    return R\n\ndef get_ellipsoid_surface(mean, rot_quat, scale, iso_sq, resolution=15):\n    \"\"\"Generates X, Y, Z grid for a 3D ellipsoid surface.\"\"\"\n    # The mathematical distance multiplier is the square root of the squared ISO\n    multiplier = np.sqrt(iso_sq)\n    \n    # 1. Base Unit Sphere\n    u = np.linspace(0, 2 * np.pi, resolution)\n    v = np.linspace(0, np.pi, resolution)\n    x = np.outer(np.cos(u), np.sin(v))\n    y = np.outer(np.sin(u), np.sin(v))\n    z = np.outer(np.ones_like(u), np.cos(v))\n    \n    # 2. Scale the sphere by (scale * sqrt(iso))\n    x *= scale[0] * multiplier\n    y *= scale[1] * multiplier\n    z *= scale[2] * multiplier\n    \n    # 3. Rotate the scaled points\n    R = quat_to_rotmat(rot_quat)\n    points = np.stack([x.flatten(), y.flatten(), z.flatten()], axis=1)\n    points_rot = points @ R.T  # Apply rotation\n    \n    # 4. Translate to the Gaussian mean\n    x_rot = points_rot[:, 0].reshape(resolution, resolution) + mean[0]\n    y_rot = points_rot[:, 1].reshape(resolution, resolution) + mean[1]\n    z_rot = points_rot[:, 2].reshape(resolution, resolution) + mean[2]\n    \n    return x_rot, y_rot, z_rot\n</pre> def quat_to_rotmat(q):     \"\"\"Converts a quaternion [w, x, y, z] to a 3x3 rotation matrix.\"\"\"     w, x, y, z = q[0], q[1], q[2], q[3]     n = np.sqrt(x*x + y*y + z*z + w*w)     w, x, y, z = w/n, x/n, y/n, z/n          R = np.array([         [1 - 2*(y**2 + z**2), 2*(x*y - w*z),     2*(x*z + w*y)],         [2*(x*y + w*z),     1 - 2*(x**2 + z**2), 2*(y*z - w*x)],         [2*(x*z - w*y),     2*(y*z + w*x),     1 - 2*(x**2 + y**2)]     ])     return R  def get_ellipsoid_surface(mean, rot_quat, scale, iso_sq, resolution=15):     \"\"\"Generates X, Y, Z grid for a 3D ellipsoid surface.\"\"\"     # The mathematical distance multiplier is the square root of the squared ISO     multiplier = np.sqrt(iso_sq)          # 1. Base Unit Sphere     u = np.linspace(0, 2 * np.pi, resolution)     v = np.linspace(0, np.pi, resolution)     x = np.outer(np.cos(u), np.sin(v))     y = np.outer(np.sin(u), np.sin(v))     z = np.outer(np.ones_like(u), np.cos(v))          # 2. Scale the sphere by (scale * sqrt(iso))     x *= scale[0] * multiplier     y *= scale[1] * multiplier     z *= scale[2] * multiplier          # 3. Rotate the scaled points     R = quat_to_rotmat(rot_quat)     points = np.stack([x.flatten(), y.flatten(), z.flatten()], axis=1)     points_rot = points @ R.T  # Apply rotation          # 4. Translate to the Gaussian mean     x_rot = points_rot[:, 0].reshape(resolution, resolution) + mean[0]     y_rot = points_rot[:, 1].reshape(resolution, resolution) + mean[1]     z_rot = points_rot[:, 2].reshape(resolution, resolution) + mean[2]          return x_rot, y_rot, z_rot In\u00a0[6]: Copied! <pre>num_to_plot = 100\n\nplot_mins = aabb_min[:num_to_plot]\nplot_maxs = aabb_max[:num_to_plot]\nplot_means = means_np[:num_to_plot]\nplot_normals = normals_np[:num_to_plot]\nplot_scales = scales_np[:num_to_plot]\nplot_rots = rot_np[:num_to_plot]\nplot_isos = isos_np[:num_to_plot]\n\n# Reshape contact points to standard (X, Y, Z) array\nplot_contacts = contact_points_np[:num_to_plot].reshape(-1, 3)\n\n# Generate Wireframes\nx_lines, y_lines, z_lines = generate_batch_wireframe(plot_mins, plot_maxs)\n\n# Mesh\nvertices = mesh.vertices\nfaces = mesh.faces\n\n# Normals\nnx_lines, ny_lines, nz_lines = [], [], []\nnormal_length = 0.5\n\nfor i in range(num_to_plot):\n    nx_lines.extend([plot_means[i, 0], plot_means[i, 0] + plot_normals[i, 0] * normal_length, None])\n    ny_lines.extend([plot_means[i, 1], plot_means[i, 1] + plot_normals[i, 1] * normal_length, None])\n    nz_lines.extend([plot_means[i, 2], plot_means[i, 2] + plot_normals[i, 2] * normal_length, None])\n\n# Start adding traces to the figure\nfig = go.Figure()\n\n# Mesh\nfig.add_trace(go.Mesh3d(\n    x=vertices[:, 0],\n    y=vertices[:, 1],\n    z=vertices[:, 2],\n    i=faces[:, 0],\n    j=faces[:, 1],\n    k=faces[:, 2],\n    color='lightblue',\n    opacity=0.8,\n    name='Original Mesh'\n))\n\n# Add Gaussian Normals\nfig.add_trace(go.Scatter3d(\n    x=nx_lines, y=ny_lines, z=nz_lines,\n    mode='lines',\n    line=dict(color='purple', width=4),\n    name='Gaussian Normals'\n))\n\n# Add AABB Wireframes\n# fig.add_trace(go.Scatter3d(\n#     x=x_lines, y=y_lines, z=z_lines,\n#     mode='lines', line=dict(color='lightgreen', width=2),\n#     name='AABBs'\n# ))\n\n# Add Gaussian Centers\n# fig.add_trace(go.Scatter3d(\n#     x=plot_means[:, 0], y=plot_means[:, 1], z=plot_means[:, 2],\n#     mode='markers', marker=dict(size=4, color='blue', symbol='circle'),\n#     name='Gaussian Centers'\n# ))\n\n# Add Contact Points (Should lie perfectly on the wireframe and the ellipsoid!)\n# fig.add_trace(go.Scatter3d(\n#     x=plot_contacts[:, 0], y=plot_contacts[:, 1], z=plot_contacts[:, 2],\n#     mode='markers', marker=dict(size=4, color='red', symbol='diamond'),\n#     name='Contact Points'\n# ))\n\n# --- Loop through to generate and plot the 3D Ellipsoids ---\nfor i in range(num_to_plot):\n    ex, ey, ez = get_ellipsoid_surface(\n        mean=plot_means[i], \n        rot_quat=plot_rots[i], \n        scale=plot_scales[i], \n        iso_sq=plot_isos[i]\n    )\n    \n    fig.add_trace(go.Surface(\n        x=ex, y=ey, z=ez,\n        opacity=0.2,\n        colorscale='Viridis',\n        showscale=False,\n        hoverinfo='skip'\n    ))\n\n# Render\nfig.update_layout(\n    title=f\"First {num_to_plot} Gaussians: Ellipsoids, AABBs &amp; Contact Points\",\n    scene=dict(\n        aspectmode='data', # Critical: Ensures X/Y/Z scaling is exactly 1:1:1\n        xaxis=dict(title='X'),\n        yaxis=dict(title='Y'),\n        zaxis=dict(title='Z')\n    )\n)\nfig.show()\n</pre> num_to_plot = 100  plot_mins = aabb_min[:num_to_plot] plot_maxs = aabb_max[:num_to_plot] plot_means = means_np[:num_to_plot] plot_normals = normals_np[:num_to_plot] plot_scales = scales_np[:num_to_plot] plot_rots = rot_np[:num_to_plot] plot_isos = isos_np[:num_to_plot]  # Reshape contact points to standard (X, Y, Z) array plot_contacts = contact_points_np[:num_to_plot].reshape(-1, 3)  # Generate Wireframes x_lines, y_lines, z_lines = generate_batch_wireframe(plot_mins, plot_maxs)  # Mesh vertices = mesh.vertices faces = mesh.faces  # Normals nx_lines, ny_lines, nz_lines = [], [], [] normal_length = 0.5  for i in range(num_to_plot):     nx_lines.extend([plot_means[i, 0], plot_means[i, 0] + plot_normals[i, 0] * normal_length, None])     ny_lines.extend([plot_means[i, 1], plot_means[i, 1] + plot_normals[i, 1] * normal_length, None])     nz_lines.extend([plot_means[i, 2], plot_means[i, 2] + plot_normals[i, 2] * normal_length, None])  # Start adding traces to the figure fig = go.Figure()  # Mesh fig.add_trace(go.Mesh3d(     x=vertices[:, 0],     y=vertices[:, 1],     z=vertices[:, 2],     i=faces[:, 0],     j=faces[:, 1],     k=faces[:, 2],     color='lightblue',     opacity=0.8,     name='Original Mesh' ))  # Add Gaussian Normals fig.add_trace(go.Scatter3d(     x=nx_lines, y=ny_lines, z=nz_lines,     mode='lines',     line=dict(color='purple', width=4),     name='Gaussian Normals' ))  # Add AABB Wireframes # fig.add_trace(go.Scatter3d( #     x=x_lines, y=y_lines, z=z_lines, #     mode='lines', line=dict(color='lightgreen', width=2), #     name='AABBs' # ))  # Add Gaussian Centers # fig.add_trace(go.Scatter3d( #     x=plot_means[:, 0], y=plot_means[:, 1], z=plot_means[:, 2], #     mode='markers', marker=dict(size=4, color='blue', symbol='circle'), #     name='Gaussian Centers' # ))  # Add Contact Points (Should lie perfectly on the wireframe and the ellipsoid!) # fig.add_trace(go.Scatter3d( #     x=plot_contacts[:, 0], y=plot_contacts[:, 1], z=plot_contacts[:, 2], #     mode='markers', marker=dict(size=4, color='red', symbol='diamond'), #     name='Contact Points' # ))  # --- Loop through to generate and plot the 3D Ellipsoids --- for i in range(num_to_plot):     ex, ey, ez = get_ellipsoid_surface(         mean=plot_means[i],          rot_quat=plot_rots[i],          scale=plot_scales[i],          iso_sq=plot_isos[i]     )          fig.add_trace(go.Surface(         x=ex, y=ey, z=ez,         opacity=0.2,         colorscale='Viridis',         showscale=False,         hoverinfo='skip'     ))  # Render fig.update_layout(     title=f\"First {num_to_plot} Gaussians: Ellipsoids, AABBs &amp; Contact Points\",     scene=dict(         aspectmode='data', # Critical: Ensures X/Y/Z scaling is exactly 1:1:1         xaxis=dict(title='X'),         yaxis=dict(title='Y'),         zaxis=dict(title='Z')     ) ) fig.show()"},{"location":"examples/gaussians/pgs_tangency_radii/#pgs-tangency-radii","title":"PGS Tangency Radii\u00b6","text":"<p>This notebook shows how to calculate the tangency radii for Gaussians with normals over a mesh surface.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/#1-visualization-helpers","title":"1. Visualization Helpers\u00b6","text":"<p>Define helper functions to generate wireframes for voxel bounding boxes.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/#2-mesh-definition","title":"2. Mesh Definition\u00b6","text":"<p>Define the mesh geometry (vertices and faces) where Gaussians are sampled.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/#3-mesh-voxelization","title":"3. Mesh Voxelization\u00b6","text":"<p>Create an Octree representation of the mesh to efficiently query surface locations.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/#4-quaternion-utilities","title":"4. Quaternion Utilities\u00b6","text":"<p>Define helper functions for converting quaternions into rotation matrices.</p>"},{"location":"examples/gaussians/pgs_tangency_radii/#5-radii-visualization","title":"5. Radii Visualization\u00b6","text":"<p>Visualize the projected bounding boxes on the mesh surface, using the computed tangency radii.</p>"},{"location":"examples/mesh/01_basic_mesh_operations/","title":"TriangleMesh Example","text":"In\u00a0[1]: Copied! <pre>from conquer3d.data_structure import TriangleMesh\n\nimport torch\nimport plotly.graph_objects as go\nimport conquer3d\n</pre> from conquer3d.data_structure import TriangleMesh  import torch import plotly.graph_objects as go import conquer3d In\u00a0[2]: Copied! <pre>vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0)\nprint(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\")\n</pre> vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0) print(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\") <pre>Vertices: torch.Size([482, 3]), Faces: torch.Size([960, 3])\n</pre> In\u00a0[3]: Copied! <pre>mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda'))\nis_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True)\nis_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)\n\nprint(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\")\nprint(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")\n\nis_vertex_manifold = mesh.is_vertex_manifold()\nprint(f\"Is Vertex Manifold: {is_vertex_manifold}\")\n\n# Compute areas and normals\nareas = mesh.triangle_areas\nnormals = mesh.triangle_normals\nsurface_area = mesh.surface_area\nprint(f'Total Surface Area: {surface_area.item():.4f}')\n\nimport plotly.graph_objects as go\nimport numpy as np\n\n# Compute face centers for normals\nv_np = mesh.vertices.cpu().numpy()\nf_np = mesh.triangles.cpu().numpy()\ncenters = np.mean(v_np[f_np], axis=1)\nnorms_np = normals.cpu().numpy()\nareas_np = areas.cpu().numpy()\n\n# Determine scaling factor for normals\nscale = 0.1\ntips = centers + norms_np * scale\n\n# Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace\nlines_x = np.empty((len(centers) * 3,))\nlines_y = np.empty((len(centers) * 3,))\nlines_z = np.empty((len(centers) * 3,))\nlines_x[0::3] = centers[:, 0]\nlines_x[1::3] = tips[:, 0]\nlines_x[2::3] = None\nlines_y[0::3] = centers[:, 1]\nlines_y[1::3] = tips[:, 1]\nlines_y[2::3] = None\nlines_z[0::3] = centers[:, 2]\nlines_z[1::3] = tips[:, 2]\nlines_z[2::3] = None\n\nfig = go.Figure(data=[\n    go.Mesh3d(\n        x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n        i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],\n        intensity=areas_np, intensitymode='cell', colorscale='Viridis',\n        opacity=0.8, showscale=True, name='Mesh'\n    ),\n    go.Scatter3d(\n        x=lines_x, y=lines_y, z=lines_z,\n        mode='lines', line=dict(color='black', width=2), showlegend=False\n    ),\n    go.Cone(\n        x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],\n        u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],\n        sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'\n    )\n])\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\")\nfig.show()\n</pre> mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda')) is_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True) is_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)  print(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\") print(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")  is_vertex_manifold = mesh.is_vertex_manifold() print(f\"Is Vertex Manifold: {is_vertex_manifold}\")  # Compute areas and normals areas = mesh.triangle_areas normals = mesh.triangle_normals surface_area = mesh.surface_area print(f'Total Surface Area: {surface_area.item():.4f}')  import plotly.graph_objects as go import numpy as np  # Compute face centers for normals v_np = mesh.vertices.cpu().numpy() f_np = mesh.triangles.cpu().numpy() centers = np.mean(v_np[f_np], axis=1) norms_np = normals.cpu().numpy() areas_np = areas.cpu().numpy()  # Determine scaling factor for normals scale = 0.1 tips = centers + norms_np * scale  # Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace lines_x = np.empty((len(centers) * 3,)) lines_y = np.empty((len(centers) * 3,)) lines_z = np.empty((len(centers) * 3,)) lines_x[0::3] = centers[:, 0] lines_x[1::3] = tips[:, 0] lines_x[2::3] = None lines_y[0::3] = centers[:, 1] lines_y[1::3] = tips[:, 1] lines_y[2::3] = None lines_z[0::3] = centers[:, 2] lines_z[1::3] = tips[:, 2] lines_z[2::3] = None  fig = go.Figure(data=[     go.Mesh3d(         x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],         i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],         intensity=areas_np, intensitymode='cell', colorscale='Viridis',         opacity=0.8, showscale=True, name='Mesh'     ),     go.Scatter3d(         x=lines_x, y=lines_y, z=lines_z,         mode='lines', line=dict(color='black', width=2), showlegend=False     ),     go.Cone(         x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],         u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],         sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'     ) ])  fig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\") fig.show() <pre>Is Open Manifold (&lt;=2 triangles per edge): True\nIs Closed Manifold (==2 triangles per edge): True\nIs Vertex Manifold: True\nTotal Surface Area: 12.4657\n</pre> In\u00a0[4]: Copied! <pre>def plot_mesh(V_cpu, F_cpu, E_cpu, title):\n    # Create wireframe lines by connecting edge vertices with None in between to break the line\n    import numpy as np\n    x_lines = []\n    y_lines = []\n    z_lines = []\n    for edge in E_cpu:\n        x_lines.extend([V_cpu[edge[0], 0], V_cpu[edge[1], 0], None])\n        y_lines.extend([V_cpu[edge[0], 1], V_cpu[edge[1], 1], None])\n        z_lines.extend([V_cpu[edge[0], 2], V_cpu[edge[1], 2], None])\n        \n    wireframe = go.Scatter3d(\n        x=x_lines, y=y_lines, z=z_lines,\n        mode='lines',\n        line=dict(color='black', width=2),\n        showlegend=False\n    )\n    \n    mesh3d = go.Mesh3d(\n        x=V_cpu[:, 0], y=V_cpu[:, 1], z=V_cpu[:, 2],\n        i=F_cpu[:, 0], j=F_cpu[:, 1], k=F_cpu[:, 2],\n        opacity=0.8,\n        color='lightblue',\n        flatshading=True,\n        showscale=False\n    )\n\n    fig = go.Figure(data=[mesh3d, wireframe])\n    fig.update_layout(title=title, scene=dict(aspectmode='data'))\n    fig.show()\n\nplot_mesh(vertices.numpy(), faces.numpy(), mesh.edges.cpu().numpy(), \"Original Sphere\")\n</pre> def plot_mesh(V_cpu, F_cpu, E_cpu, title):     # Create wireframe lines by connecting edge vertices with None in between to break the line     import numpy as np     x_lines = []     y_lines = []     z_lines = []     for edge in E_cpu:         x_lines.extend([V_cpu[edge[0], 0], V_cpu[edge[1], 0], None])         y_lines.extend([V_cpu[edge[0], 1], V_cpu[edge[1], 1], None])         z_lines.extend([V_cpu[edge[0], 2], V_cpu[edge[1], 2], None])              wireframe = go.Scatter3d(         x=x_lines, y=y_lines, z=z_lines,         mode='lines',         line=dict(color='black', width=2),         showlegend=False     )          mesh3d = go.Mesh3d(         x=V_cpu[:, 0], y=V_cpu[:, 1], z=V_cpu[:, 2],         i=F_cpu[:, 0], j=F_cpu[:, 1], k=F_cpu[:, 2],         opacity=0.8,         color='lightblue',         flatshading=True,         showscale=False     )      fig = go.Figure(data=[mesh3d, wireframe])     fig.update_layout(title=title, scene=dict(aspectmode='data'))     fig.show()  plot_mesh(vertices.numpy(), faces.numpy(), mesh.edges.cpu().numpy(), \"Original Sphere\") In\u00a0[5]: Copied! <pre># Create a random boolean mask keeping 80% of faces\ntorch.manual_seed(42)\nkeep_mask = torch.rand(mesh.num_triangles, device='cuda') &gt; 0.2\nmesh.remove_triangles_by_mask(keep_mask)\n\nprint(f\"Remaining Faces: {mesh.num_triangles}\")\nis_manifold_open_new = mesh.is_edge_manifold(allow_boundary_edge=True)\nis_manifold_closed_new = mesh.is_edge_manifold(allow_boundary_edge=False)\n\nprint(f\"Is Open Manifold After Removal: {is_manifold_open_new}\")\nprint(f\"Is Closed Manifold After Removal: {is_manifold_closed_new}\")\n</pre> # Create a random boolean mask keeping 80% of faces torch.manual_seed(42) keep_mask = torch.rand(mesh.num_triangles, device='cuda') &gt; 0.2 mesh.remove_triangles_by_mask(keep_mask)  print(f\"Remaining Faces: {mesh.num_triangles}\") is_manifold_open_new = mesh.is_edge_manifold(allow_boundary_edge=True) is_manifold_closed_new = mesh.is_edge_manifold(allow_boundary_edge=False)  print(f\"Is Open Manifold After Removal: {is_manifold_open_new}\") print(f\"Is Closed Manifold After Removal: {is_manifold_closed_new}\") <pre>Remaining Faces: 771\nIs Open Manifold After Removal: True\nIs Closed Manifold After Removal: False\n</pre> In\u00a0[6]: Copied! <pre>plot_mesh(mesh.vertices.cpu().numpy(), mesh.triangles.cpu().numpy(), mesh.edges.cpu().numpy(), \"Decimated Sphere\")\n</pre> plot_mesh(mesh.vertices.cpu().numpy(), mesh.triangles.cpu().numpy(), mesh.edges.cpu().numpy(), \"Decimated Sphere\")"},{"location":"examples/mesh/01_basic_mesh_operations/#trianglemesh-example","title":"TriangleMesh Example\u00b6","text":"<p>This example demonstrates how to create a sphere, instantiate a TriangleMesh, check its edge manifold properties, and safely remove triangles from it.</p>"},{"location":"examples/mesh/01_basic_mesh_operations/#1-create-a-sphere","title":"1. Create a Sphere\u00b6","text":""},{"location":"examples/mesh/01_basic_mesh_operations/#2-create-triangle-mesh-and-check-manifold","title":"2. Create Triangle Mesh and Check Manifold\u00b6","text":"<p>A closed sphere should be a perfect closed manifold (every edge has exactly 2 triangles).</p>"},{"location":"examples/mesh/01_basic_mesh_operations/#3-visualize-original-mesh-with-wireframe","title":"3. Visualize Original Mesh (with Wireframe)\u00b6","text":""},{"location":"examples/mesh/01_basic_mesh_operations/#4-remove-random-faces","title":"4. Remove Random Faces\u00b6","text":"<p>We create a boolean mask to randomly drop 20% of the triangles. This will create holes in the mesh!</p>"},{"location":"examples/mesh/01_basic_mesh_operations/#5-visualize-decimated-mesh","title":"5. Visualize Decimated Mesh\u00b6","text":"<p>Notice how the mesh is no longer a closed manifold because we introduced boundary edges (holes) by removing triangles.</p>"},{"location":"examples/mesh/02_find_non_manifold_vertices/","title":"Find Non-Manifold Vertices","text":"In\u00a0[1]: Copied! <pre>from conquer3d.data_structure import TriangleMesh\n\nimport torch\nimport plotly.graph_objects as go\nimport conquer3d\nimport numpy as np\n</pre> from conquer3d.data_structure import TriangleMesh  import torch import plotly.graph_objects as go import conquer3d import numpy as np In\u00a0[2]: Copied! <pre>vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0)\nprint(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\")\n</pre> vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0) print(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\") <pre>Vertices: torch.Size([482, 3]), Faces: torch.Size([960, 3])\n</pre> In\u00a0[3]: Copied! <pre>mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda'))\nis_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True)\nis_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)\n\nprint(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\")\nprint(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")\n\nis_vertex_manifold = mesh.is_vertex_manifold()\nprint(f\"Is Vertex Manifold: {is_vertex_manifold}\")\n\n# Compute areas and normals\nareas = mesh.triangle_areas\nnormals = mesh.triangle_normals\nsurface_area = mesh.surface_area\nprint(f'Total Surface Area: {surface_area.item():.4f}')\n\n# Compute face centers for normals\nv_np = mesh.vertices.cpu().numpy()\nf_np = mesh.triangles.cpu().numpy()\ncenters = np.mean(v_np[f_np], axis=1)\nnorms_np = normals.cpu().numpy()\nareas_np = areas.cpu().numpy()\n\n# Determine scaling factor for normals\nscale = 0.1\ntips = centers + norms_np * scale\n\n# Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace\nlines_x = np.empty((len(centers) * 3,))\nlines_y = np.empty((len(centers) * 3,))\nlines_z = np.empty((len(centers) * 3,))\nlines_x[0::3] = centers[:, 0]\nlines_x[1::3] = tips[:, 0]\nlines_x[2::3] = None\nlines_y[0::3] = centers[:, 1]\nlines_y[1::3] = tips[:, 1]\nlines_y[2::3] = None\nlines_z[0::3] = centers[:, 2]\nlines_z[1::3] = tips[:, 2]\nlines_z[2::3] = None\n\nfig = go.Figure(data=[\n    go.Mesh3d(\n        x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n        i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],\n        intensity=areas_np, intensitymode='cell', colorscale='Viridis',\n        opacity=0.8, showscale=True, name='Mesh'\n    ),\n    go.Scatter3d(\n        x=lines_x, y=lines_y, z=lines_z,\n        mode='lines', line=dict(color='black', width=2), showlegend=False\n    ),\n    go.Cone(\n        x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],\n        u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],\n        sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'\n    )\n])\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\")\nfig.show()\n</pre> mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda')) is_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True) is_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)  print(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\") print(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")  is_vertex_manifold = mesh.is_vertex_manifold() print(f\"Is Vertex Manifold: {is_vertex_manifold}\")  # Compute areas and normals areas = mesh.triangle_areas normals = mesh.triangle_normals surface_area = mesh.surface_area print(f'Total Surface Area: {surface_area.item():.4f}')  # Compute face centers for normals v_np = mesh.vertices.cpu().numpy() f_np = mesh.triangles.cpu().numpy() centers = np.mean(v_np[f_np], axis=1) norms_np = normals.cpu().numpy() areas_np = areas.cpu().numpy()  # Determine scaling factor for normals scale = 0.1 tips = centers + norms_np * scale  # Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace lines_x = np.empty((len(centers) * 3,)) lines_y = np.empty((len(centers) * 3,)) lines_z = np.empty((len(centers) * 3,)) lines_x[0::3] = centers[:, 0] lines_x[1::3] = tips[:, 0] lines_x[2::3] = None lines_y[0::3] = centers[:, 1] lines_y[1::3] = tips[:, 1] lines_y[2::3] = None lines_z[0::3] = centers[:, 2] lines_z[1::3] = tips[:, 2] lines_z[2::3] = None  fig = go.Figure(data=[     go.Mesh3d(         x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],         i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],         intensity=areas_np, intensitymode='cell', colorscale='Viridis',         opacity=0.8, showscale=True, name='Mesh'     ),     go.Scatter3d(         x=lines_x, y=lines_y, z=lines_z,         mode='lines', line=dict(color='black', width=2), showlegend=False     ),     go.Cone(         x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],         u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],         sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'     ) ])  fig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\") fig.show() <pre>Is Open Manifold (&lt;=2 triangles per edge): True\nIs Closed Manifold (==2 triangles per edge): True\nIs Vertex Manifold: True\nTotal Surface Area: 12.4657\n</pre> In\u00a0[4]: Copied! <pre>torch.manual_seed(42)\n\n# Drop ~20% of the triangles randomly\nkeep_mask = torch.rand(mesh.num_triangles, device='cuda') &gt; 0.2\n\n# Remove the triangles (this automatically clears all our C++ caches)\nmesh.remove_triangles_by_mask(keep_mask)\n\nprint(f\"Remaining triangles: {mesh.num_triangles}\")\nis_edge_manifold = mesh.is_edge_manifold(allow_boundary_edge=True)\nprint(f\"Is Edge Manifold (Open): {is_edge_manifold}\")\n\nis_vertex_manifold = mesh.is_vertex_manifold()\nprint(f\"Is Vertex Manifold: {is_vertex_manifold}\")\n\n# Detect the non-manifold (bowtie) vertices we just accidentally created!\nnon_manifold_vertices = mesh.get_non_manifold_vertices()\nprint(f\"Number of Non-Manifold Vertices found: {len(non_manifold_vertices)}\")\n</pre> torch.manual_seed(42)  # Drop ~20% of the triangles randomly keep_mask = torch.rand(mesh.num_triangles, device='cuda') &gt; 0.2  # Remove the triangles (this automatically clears all our C++ caches) mesh.remove_triangles_by_mask(keep_mask)  print(f\"Remaining triangles: {mesh.num_triangles}\") is_edge_manifold = mesh.is_edge_manifold(allow_boundary_edge=True) print(f\"Is Edge Manifold (Open): {is_edge_manifold}\")  is_vertex_manifold = mesh.is_vertex_manifold() print(f\"Is Vertex Manifold: {is_vertex_manifold}\")  # Detect the non-manifold (bowtie) vertices we just accidentally created! non_manifold_vertices = mesh.get_non_manifold_vertices() print(f\"Number of Non-Manifold Vertices found: {len(non_manifold_vertices)}\")  <pre>Remaining triangles: 771\nIs Edge Manifold (Open): True\nIs Vertex Manifold: False\nNumber of Non-Manifold Vertices found: 106\n</pre> In\u00a0[5]: Copied! <pre># Let's visualize the broken mesh and explicitly highlight the non-manifold vertices!\nvertices_cpu = mesh.vertices.cpu().numpy()\ntriangles_cpu = mesh.triangles.cpu().numpy()\n\n# Main mesh surface\nmesh_plot = go.Mesh3d(\n    x=vertices_cpu[:, 0],\n    y=vertices_cpu[:, 1],\n    z=vertices_cpu[:, 2],\n    i=triangles_cpu[:, 0],\n    j=triangles_cpu[:, 1],\n    k=triangles_cpu[:, 2],\n    color='lightblue',\n    opacity=0.8,\n    flatshading=True\n)\n\n# Wireframe of the remaining edges\nedges = mesh.edges.cpu().numpy()\nedge_x = []\nedge_y = []\nedge_z = []\nfor edge in edges:\n    edge_x.extend([vertices_cpu[edge[0], 0], vertices_cpu[edge[1], 0], None])\n    edge_y.extend([vertices_cpu[edge[0], 1], vertices_cpu[edge[1], 1], None])\n    edge_z.extend([vertices_cpu[edge[0], 2], vertices_cpu[edge[1], 2], None])\n\nwireframe = go.Scatter3d(\n    x=edge_x, y=edge_y, z=edge_z,\n    mode='lines',\n    line=dict(color='black', width=2),\n    showlegend=False\n)\n\n# Non-manifold vertices highlighted in RED\nnm_idx = non_manifold_vertices.cpu().numpy()\nnm_points = go.Scatter3d(\n    x=vertices_cpu[nm_idx, 0],\n    y=vertices_cpu[nm_idx, 1],\n    z=vertices_cpu[nm_idx, 2],\n    mode='markers',\n    marker=dict(color='red', size=6, symbol='diamond'),\n    name='Non-Manifold (Bowtie) Vertices'\n)\n\nfig = go.Figure(data=[mesh_plot, wireframe, nm_points])\nfig.update_layout(\n    title=\"Swiss Cheese Sphere (Red = Bowtie Vertices)\",\n    scene=dict(\n        xaxis=dict(visible=False),\n        yaxis=dict(visible=False),\n        zaxis=dict(visible=False)\n    ),\n    margin=dict(l=0, r=0, b=0, t=40)\n)\nfig.show()\n</pre> # Let's visualize the broken mesh and explicitly highlight the non-manifold vertices! vertices_cpu = mesh.vertices.cpu().numpy() triangles_cpu = mesh.triangles.cpu().numpy()  # Main mesh surface mesh_plot = go.Mesh3d(     x=vertices_cpu[:, 0],     y=vertices_cpu[:, 1],     z=vertices_cpu[:, 2],     i=triangles_cpu[:, 0],     j=triangles_cpu[:, 1],     k=triangles_cpu[:, 2],     color='lightblue',     opacity=0.8,     flatshading=True )  # Wireframe of the remaining edges edges = mesh.edges.cpu().numpy() edge_x = [] edge_y = [] edge_z = [] for edge in edges:     edge_x.extend([vertices_cpu[edge[0], 0], vertices_cpu[edge[1], 0], None])     edge_y.extend([vertices_cpu[edge[0], 1], vertices_cpu[edge[1], 1], None])     edge_z.extend([vertices_cpu[edge[0], 2], vertices_cpu[edge[1], 2], None])  wireframe = go.Scatter3d(     x=edge_x, y=edge_y, z=edge_z,     mode='lines',     line=dict(color='black', width=2),     showlegend=False )  # Non-manifold vertices highlighted in RED nm_idx = non_manifold_vertices.cpu().numpy() nm_points = go.Scatter3d(     x=vertices_cpu[nm_idx, 0],     y=vertices_cpu[nm_idx, 1],     z=vertices_cpu[nm_idx, 2],     mode='markers',     marker=dict(color='red', size=6, symbol='diamond'),     name='Non-Manifold (Bowtie) Vertices' )  fig = go.Figure(data=[mesh_plot, wireframe, nm_points]) fig.update_layout(     title=\"Swiss Cheese Sphere (Red = Bowtie Vertices)\",     scene=dict(         xaxis=dict(visible=False),         yaxis=dict(visible=False),         zaxis=dict(visible=False)     ),     margin=dict(l=0, r=0, b=0, t=40) ) fig.show()"},{"location":"examples/mesh/02_find_non_manifold_vertices/#1-create-a-sphere","title":"1. Create a Sphere\u00b6","text":""},{"location":"examples/mesh/02_find_non_manifold_vertices/#2-create-triangle-mesh-and-check-manifold","title":"2. Create Triangle Mesh and Check Manifold\u00b6","text":"<p>A closed sphere should be a perfect closed manifold (every edge has exactly 2 triangles).</p>"},{"location":"examples/mesh/02_find_non_manifold_vertices/#3-remove-random-triangles-and-find-non-manifold-vertices","title":"3. Remove Random Triangles and Find Non-Manifold Vertices\u00b6","text":"<p>We drop 20% of the triangles randomly. This will create open boundaries, but mathematically, random dropping on a dense sphere almost guarantees the creation of several 'bowtie' vertices (where two triangles touch at exactly one corner). Let's find them and highlight them in red!</p>"},{"location":"examples/mesh/03_self_intersection/","title":"MeshBVH and Self-Intersection","text":"In\u00a0[1]: Copied! <pre>import torch\nimport plotly.graph_objects as go\nimport conquer3d\n\n# Create random triangle soup with both intersecting and non-intersecting triangles\nN_soup = 20\ntorch.manual_seed(42)\n\n# Generate random centers in a large space so most are isolated\ncenters = torch.rand((N_soup, 1, 3), dtype=torch.float32, device=\"cuda\") * 3\n\n# Force some triangles to be near each other to guarantee intersections\ncenters[1] = centers[0] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5\ncenters[3] = centers[2] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5\ncenters[5] = centers[4] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5\n\n# Add small random offsets to create the 3 vertices of each triangle\noffsets = torch.rand((N_soup, 3, 3), dtype=torch.float32, device=\"cuda\") * 2\nvertices_soup = (centers + offsets).view(N_soup * 3, 3)\ntriangles_soup = torch.arange(N_soup * 3, dtype=torch.int32, device=\"cuda\").view(N_soup, 3)\n\n# Create mesh\nmesh_soup = conquer3d.data_structure.TriangleMesh(vertices_soup, triangles_soup)\n\n# Check self intersection directly from mesh\nhas_intersection = mesh_soup.is_self_intersection()\nintersecting_pairs = mesh_soup.get_self_intersection()\n\nprint(f\"Has self intersection: {has_intersection}\")\nprint(f\"Found {intersecting_pairs.size(0)} intersecting pairs!\")\n\n# Visualize\nfig = go.Figure()\nv0 = vertices_soup[triangles_soup[:, 0]].cpu().numpy()\nv1 = vertices_soup[triangles_soup[:, 1]].cpu().numpy()\nv2 = vertices_soup[triangles_soup[:, 2]].cpu().numpy()\n\nintersecting_indices = set()\nif intersecting_pairs.size(0) &gt; 0:\n    intersecting_indices = set(intersecting_pairs.cpu().numpy().flatten())\n\nfor i in range(N_soup):\n    x = [v0[i, 0], v1[i, 0], v2[i, 0], v0[i, 0]]\n    y = [v0[i, 1], v1[i, 1], v2[i, 1], v0[i, 1]]\n    z = [v0[i, 2], v1[i, 2], v2[i, 2], v0[i, 2]]\n    \n    is_intersecting = i in intersecting_indices\n    color = 'red' if is_intersecting else 'blue'\n    width = 5 if is_intersecting else 2\n    name = f\"Intersecting Triangle {i}\" if is_intersecting else f\"Triangle {i}\"\n    \n    fig.add_trace(go.Scatter3d(x=x, y=y, z=z, mode='lines', line=dict(color=color, width=width), name=name))\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"MeshBVH Self Intersection on Triangle Soup\")\nfig.show()\n</pre> import torch import plotly.graph_objects as go import conquer3d  # Create random triangle soup with both intersecting and non-intersecting triangles N_soup = 20 torch.manual_seed(42)  # Generate random centers in a large space so most are isolated centers = torch.rand((N_soup, 1, 3), dtype=torch.float32, device=\"cuda\") * 3  # Force some triangles to be near each other to guarantee intersections centers[1] = centers[0] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5 centers[3] = centers[2] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5 centers[5] = centers[4] + torch.rand((1, 1, 3), device=\"cuda\") * 0.5  # Add small random offsets to create the 3 vertices of each triangle offsets = torch.rand((N_soup, 3, 3), dtype=torch.float32, device=\"cuda\") * 2 vertices_soup = (centers + offsets).view(N_soup * 3, 3) triangles_soup = torch.arange(N_soup * 3, dtype=torch.int32, device=\"cuda\").view(N_soup, 3)  # Create mesh mesh_soup = conquer3d.data_structure.TriangleMesh(vertices_soup, triangles_soup)  # Check self intersection directly from mesh has_intersection = mesh_soup.is_self_intersection() intersecting_pairs = mesh_soup.get_self_intersection()  print(f\"Has self intersection: {has_intersection}\") print(f\"Found {intersecting_pairs.size(0)} intersecting pairs!\")  # Visualize fig = go.Figure() v0 = vertices_soup[triangles_soup[:, 0]].cpu().numpy() v1 = vertices_soup[triangles_soup[:, 1]].cpu().numpy() v2 = vertices_soup[triangles_soup[:, 2]].cpu().numpy()  intersecting_indices = set() if intersecting_pairs.size(0) &gt; 0:     intersecting_indices = set(intersecting_pairs.cpu().numpy().flatten())  for i in range(N_soup):     x = [v0[i, 0], v1[i, 0], v2[i, 0], v0[i, 0]]     y = [v0[i, 1], v1[i, 1], v2[i, 1], v0[i, 1]]     z = [v0[i, 2], v1[i, 2], v2[i, 2], v0[i, 2]]          is_intersecting = i in intersecting_indices     color = 'red' if is_intersecting else 'blue'     width = 5 if is_intersecting else 2     name = f\"Intersecting Triangle {i}\" if is_intersecting else f\"Triangle {i}\"          fig.add_trace(go.Scatter3d(x=x, y=y, z=z, mode='lines', line=dict(color=color, width=width), name=name))  fig.update_layout(scene=dict(aspectmode='data'), title=\"MeshBVH Self Intersection on Triangle Soup\") fig.show() <pre>Has self intersection: True\nFound 5 intersecting pairs!\n</pre>"},{"location":"examples/mesh/03_self_intersection/#meshbvh-and-self-intersection","title":"MeshBVH and Self-Intersection\u00b6","text":"<p>In this section, we create a completely random triangle soup, construct a <code>TriangleMesh</code>, and quickly identify all self-intersecting triangles directly using the mesh's built-in BVH methods.</p>"},{"location":"examples/mesh/04_ray_intersection/","title":"Ray Intersection","text":"In\u00a0[1]: Copied! <pre>from conquer3d.data_structure import TriangleMesh\n\nimport torch\nimport plotly.graph_objects as go\nimport conquer3d\nimport numpy as np\n</pre> from conquer3d.data_structure import TriangleMesh  import torch import plotly.graph_objects as go import conquer3d import numpy as np In\u00a0[2]: Copied! <pre>vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0)\nprint(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\")\n</pre> vertices, faces = conquer3d.creation.create_sphere(sectors=32, stacks=16, radius=1.0) print(f\"Vertices: {vertices.shape}, Faces: {faces.shape}\") <pre>Vertices: torch.Size([482, 3]), Faces: torch.Size([960, 3])\n</pre> In\u00a0[3]: Copied! <pre>mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda'))\nis_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True)\nis_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)\n\nprint(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\")\nprint(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")\n\nis_vertex_manifold = mesh.is_vertex_manifold()\nprint(f\"Is Vertex Manifold: {is_vertex_manifold}\")\n\n# Compute areas and normals\nareas = mesh.triangle_areas\nnormals = mesh.triangle_normals\nsurface_area = mesh.surface_area\nprint(f'Total Surface Area: {surface_area.item():.4f}')\n\n# Compute face centers for normals\nv_np = mesh.vertices.cpu().numpy()\nf_np = mesh.triangles.cpu().numpy()\ncenters = np.mean(v_np[f_np], axis=1)\nnorms_np = normals.cpu().numpy()\nareas_np = areas.cpu().numpy()\n\n# Determine scaling factor for normals\nscale = 0.1\ntips = centers + norms_np * scale\n\n# Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace\nlines_x = np.empty((len(centers) * 3,))\nlines_y = np.empty((len(centers) * 3,))\nlines_z = np.empty((len(centers) * 3,))\nlines_x[0::3] = centers[:, 0]\nlines_x[1::3] = tips[:, 0]\nlines_x[2::3] = None\nlines_y[0::3] = centers[:, 1]\nlines_y[1::3] = tips[:, 1]\nlines_y[2::3] = None\nlines_z[0::3] = centers[:, 2]\nlines_z[1::3] = tips[:, 2]\nlines_z[2::3] = None\n\nfig = go.Figure(data=[\n    go.Mesh3d(\n        x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n        i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],\n        intensity=areas_np, intensitymode='cell', colorscale='Viridis',\n        opacity=0.8, showscale=True, name='Mesh'\n    ),\n    go.Scatter3d(\n        x=lines_x, y=lines_y, z=lines_z,\n        mode='lines', line=dict(color='black', width=2), showlegend=False\n    ),\n    go.Cone(\n        x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],\n        u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],\n        sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'\n    )\n])\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\")\nfig.show()\n</pre> mesh: TriangleMesh = conquer3d.data_structure.TriangleMesh(vertices.to('cuda'), faces.to('cuda')) is_manifold_open = mesh.is_edge_manifold(allow_boundary_edge=True) is_manifold_closed = mesh.is_edge_manifold(allow_boundary_edge=False)  print(f\"Is Open Manifold (&lt;=2 triangles per edge): {is_manifold_open}\") print(f\"Is Closed Manifold (==2 triangles per edge): {is_manifold_closed}\")  is_vertex_manifold = mesh.is_vertex_manifold() print(f\"Is Vertex Manifold: {is_vertex_manifold}\")  # Compute areas and normals areas = mesh.triangle_areas normals = mesh.triangle_normals surface_area = mesh.surface_area print(f'Total Surface Area: {surface_area.item():.4f}')  # Compute face centers for normals v_np = mesh.vertices.cpu().numpy() f_np = mesh.triangles.cpu().numpy() centers = np.mean(v_np[f_np], axis=1) norms_np = normals.cpu().numpy() areas_np = areas.cpu().numpy()  # Determine scaling factor for normals scale = 0.1 tips = centers + norms_np * scale  # Build lines for normals. We interleave [center, tip, None] to draw multiple disconnected lines in one trace lines_x = np.empty((len(centers) * 3,)) lines_y = np.empty((len(centers) * 3,)) lines_z = np.empty((len(centers) * 3,)) lines_x[0::3] = centers[:, 0] lines_x[1::3] = tips[:, 0] lines_x[2::3] = None lines_y[0::3] = centers[:, 1] lines_y[1::3] = tips[:, 1] lines_y[2::3] = None lines_z[0::3] = centers[:, 2] lines_z[1::3] = tips[:, 2] lines_z[2::3] = None  fig = go.Figure(data=[     go.Mesh3d(         x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],         i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],         intensity=areas_np, intensitymode='cell', colorscale='Viridis',         opacity=0.8, showscale=True, name='Mesh'     ),     go.Scatter3d(         x=lines_x, y=lines_y, z=lines_z,         mode='lines', line=dict(color='black', width=2), showlegend=False     ),     go.Cone(         x=tips[:, 0], y=tips[:, 1], z=tips[:, 2],         u=norms_np[:, 0], v=norms_np[:, 1], w=norms_np[:, 2],         sizemode=\"absolute\", sizeref=scale*0.3, showscale=False, colorscale='Greys', anchor='tip'     ) ])  fig.update_layout(scene=dict(aspectmode='data'), title=\"Mesh with Areas and Normals\") fig.show() <pre>Is Open Manifold (&lt;=2 triangles per edge): True\nIs Closed Manifold (==2 triangles per edge): True\nIs Vertex Manifold: True\nTotal Surface Area: 12.4657\n</pre> In\u00a0[4]: Copied! <pre># Point inside (e.g., origin) and outside (e.g., [1, 1, 1])\ninside_pt = torch.tensor([[0.0, 0.0, 0.0]], device='cuda', dtype=torch.float32).repeat(5, 1)\noutside_pt = torch.tensor([[0.8, 0.8, 0.8]], device='cuda', dtype=torch.float32).repeat(20, 1)\n\nray_origins = torch.cat([inside_pt, outside_pt], dim=0)\n\n# Random directions\ntorch.manual_seed(32)\nray_dirs = torch.randn(ray_origins.size(0), 3, device='cuda', dtype=torch.float32)\nray_dirs = torch.nn.functional.normalize(ray_dirs, dim=1)\n\n# Perform Ray-Triangle Intersection\nray_ids, tri_ids, int_pts, distances = mesh.get_ray_intersection(ray_origins, ray_dirs, return_distance=True)\n\n# Fetch vertices and faces for visualization\nv_np = mesh.vertices.cpu().numpy()\nf_np = mesh.triangles.cpu().numpy()\n\n# Base mesh\nfig = go.Figure(data=[\n    go.Mesh3d(\n        x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n        i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],\n        color='lightgray', opacity=1, showscale=False\n    )\n])\n\n# Inside point rays (Indices 0 to 4) -&gt; Blue\ninside_mask = ray_ids &lt; 5\ninside_tris = tri_ids[inside_mask].cpu().numpy()\nif len(inside_tris) &gt; 0:\n    for tri_idx in inside_tris:\n        face = f_np[tri_idx]\n        pts = v_np[face]\n        fig.add_trace(go.Mesh3d(\n            x=pts[:, 0], y=pts[:, 1], z=pts[:, 2],\n            i=[0], j=[1], k=[2],\n            color='blue', opacity=1.0\n        ))\n\n# Outside point rays (Indices 5 to 9) -&gt; Red\noutside_mask = ray_ids &gt;= 5\noutside_tris = tri_ids[outside_mask].cpu().numpy()\nif len(outside_tris) &gt; 0:\n    for tri_idx in outside_tris:\n        face = f_np[tri_idx]\n        pts = v_np[face]\n        fig.add_trace(go.Mesh3d(\n            x=pts[:, 0], y=pts[:, 1], z=pts[:, 2],\n            i=[0], j=[1], k=[2],\n            color='red', opacity=1.0\n        ))\n\n# Draw the rays as lines\nfor i in range(ray_origins.size(0)):\n    start = ray_origins[i].cpu().numpy()\n    end = start + ray_dirs[i].cpu().numpy() * 1.5  # length 1.5\n    color = 'blue' if i &lt; 5 else 'red'\n    fig.add_trace(go.Scatter3d(\n        x=[start[0], end[0]], y=[start[1], end[1]], z=[start[2], end[2]],\n        mode='lines', line=dict(color=color, width=3)\n    ))\n\n# Draw the intersection points with distances\nint_pts_np = int_pts.cpu().numpy()\ndist_np = distances.cpu().numpy()\nfig.add_trace(go.Scatter3d(\n    x=int_pts_np[:, 0], y=int_pts_np[:, 1], z=int_pts_np[:, 2],\n    mode='markers+text',\n    marker=dict(color='yellow', size=8, symbol='diamond'),\n    text=[f\"{d:.2f}\" for d in dist_np],\n    textposition=\"top center\",\n    textfont=dict(color='black', size=12),\n    name='Intersections'\n))\n\n# Draw the points\nfig.add_trace(go.Scatter3d(\n    x=[0.0, 0.8], y=[0.0, 0.8], z=[0.0, 0.8],\n    mode='markers', marker=dict(color=['blue', 'red'], size=6)\n))\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"Ray-Triangle Intersection\")\nfig.show()\n</pre> # Point inside (e.g., origin) and outside (e.g., [1, 1, 1]) inside_pt = torch.tensor([[0.0, 0.0, 0.0]], device='cuda', dtype=torch.float32).repeat(5, 1) outside_pt = torch.tensor([[0.8, 0.8, 0.8]], device='cuda', dtype=torch.float32).repeat(20, 1)  ray_origins = torch.cat([inside_pt, outside_pt], dim=0)  # Random directions torch.manual_seed(32) ray_dirs = torch.randn(ray_origins.size(0), 3, device='cuda', dtype=torch.float32) ray_dirs = torch.nn.functional.normalize(ray_dirs, dim=1)  # Perform Ray-Triangle Intersection ray_ids, tri_ids, int_pts, distances = mesh.get_ray_intersection(ray_origins, ray_dirs, return_distance=True)  # Fetch vertices and faces for visualization v_np = mesh.vertices.cpu().numpy() f_np = mesh.triangles.cpu().numpy()  # Base mesh fig = go.Figure(data=[     go.Mesh3d(         x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],         i=f_np[:, 0], j=f_np[:, 1], k=f_np[:, 2],         color='lightgray', opacity=1, showscale=False     ) ])  # Inside point rays (Indices 0 to 4) -&gt; Blue inside_mask = ray_ids &lt; 5 inside_tris = tri_ids[inside_mask].cpu().numpy() if len(inside_tris) &gt; 0:     for tri_idx in inside_tris:         face = f_np[tri_idx]         pts = v_np[face]         fig.add_trace(go.Mesh3d(             x=pts[:, 0], y=pts[:, 1], z=pts[:, 2],             i=[0], j=[1], k=[2],             color='blue', opacity=1.0         ))  # Outside point rays (Indices 5 to 9) -&gt; Red outside_mask = ray_ids &gt;= 5 outside_tris = tri_ids[outside_mask].cpu().numpy() if len(outside_tris) &gt; 0:     for tri_idx in outside_tris:         face = f_np[tri_idx]         pts = v_np[face]         fig.add_trace(go.Mesh3d(             x=pts[:, 0], y=pts[:, 1], z=pts[:, 2],             i=[0], j=[1], k=[2],             color='red', opacity=1.0         ))  # Draw the rays as lines for i in range(ray_origins.size(0)):     start = ray_origins[i].cpu().numpy()     end = start + ray_dirs[i].cpu().numpy() * 1.5  # length 1.5     color = 'blue' if i &lt; 5 else 'red'     fig.add_trace(go.Scatter3d(         x=[start[0], end[0]], y=[start[1], end[1]], z=[start[2], end[2]],         mode='lines', line=dict(color=color, width=3)     ))  # Draw the intersection points with distances int_pts_np = int_pts.cpu().numpy() dist_np = distances.cpu().numpy() fig.add_trace(go.Scatter3d(     x=int_pts_np[:, 0], y=int_pts_np[:, 1], z=int_pts_np[:, 2],     mode='markers+text',     marker=dict(color='yellow', size=8, symbol='diamond'),     text=[f\"{d:.2f}\" for d in dist_np],     textposition=\"top center\",     textfont=dict(color='black', size=12),     name='Intersections' ))  # Draw the points fig.add_trace(go.Scatter3d(     x=[0.0, 0.8], y=[0.0, 0.8], z=[0.0, 0.8],     mode='markers', marker=dict(color=['blue', 'red'], size=6) ))  fig.update_layout(scene=dict(aspectmode='data'), title=\"Ray-Triangle Intersection\") fig.show()"},{"location":"examples/mesh/04_ray_intersection/#1-create-a-sphere","title":"1. Create a Sphere\u00b6","text":""},{"location":"examples/mesh/04_ray_intersection/#2-create-triangle-mesh-and-check-manifold","title":"2. Create Triangle Mesh and Check Manifold\u00b6","text":"<p>A closed sphere should be a perfect closed manifold (every edge has exactly 2 triangles).</p>"},{"location":"examples/mesh/04_ray_intersection/#3-ray-intersection","title":"3. Ray Intersection\u00b6","text":"<p>Cast 5 random rays from a point inside and a point outside the sphere, and highlight intersected triangles.</p>"},{"location":"examples/mesh/05_query_point_sdf/","title":"Query Point SDF","text":"In\u00a0[1]: Copied! <pre>import open3d as o3d\nimport torch\nimport conquer3d as c3d\nimport plotly.graph_objects as go\nimport numpy as np\nimport random\n\n# 1. Create a manifold sphere mesh\nsphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10)\nsphere.compute_vertex_normals()\nvertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda')\ntriangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')\n\n# 2. Build the TriangleMesh\nmesh = c3d.data_structure.TriangleMesh(vertices, triangles)\n\n# 3. Sample 5 inside points and 5 outside points\ninside_pts = []\nfor _ in range(5):\n    # Inside radius &lt; 1.0\n    r = random.uniform(0.1, 0.8)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    inside_pts.append([x,y,z])\n    \noutside_pts = []\nfor _ in range(5):\n    # Outside radius &gt; 1.0\n    r = random.uniform(1.2, 2.0)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    outside_pts.append([x,y,z])\n\nquery_points_np = np.array(inside_pts + outside_pts)\nquery_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')\n\n# 4. Query points with SDF and projected points\nq_ids, tri_ids, prj_pts, sdf = mesh.query_points(\n    query_points, \n    return_sdf=True, \n    return_prj_pts=True, \n    sign_mode=0,\n    distance_mode=0\n)\n\nprj_pts_np = prj_pts.cpu().numpy()\nsdf_np = sdf.cpu().numpy()\n\n# 5. Visualize\nfig = go.Figure()\n\n# Plot base mesh\nfig.add_trace(go.Mesh3d(\n    x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),\n    i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),\n    color='lightblue', opacity=0.3, name='Base Mesh'\n))\n\n# Extract edges for wireframe\nverts_np = vertices.cpu().numpy()\ntris_np = triangles.cpu().numpy()\nedge_x = []\nedge_y = []\nedge_z = []\nfor tri in tris_np:\n    v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]\n    edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])\n    edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])\n    edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])\n\nfig.add_trace(go.Scatter3d(\n    x=edge_x, y=edge_y, z=edge_z,\n    mode='lines',\n    line=dict(color='gray', width=1),\n    name='Wireframe',\n    showlegend=False\n))\n\ncolors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']\n\n# Plot pairs of points and text for SDF\nfor i in range(10):\n    qp = query_points_np[i]\n    pp = prj_pts_np[i]\n    dist = sdf_np[i]\n    c = colors[i]\n    \n    # Query point\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0]], y=[qp[1]], z=[qp[2]],\n        mode='markers+text',\n        marker=dict(size=5, color=c),\n        text=[f'SDF: {dist:.3f}'],\n        textposition='top center',\n        name=f'Query {i}'\n    ))\n    \n    # Projected point\n    fig.add_trace(go.Scatter3d(\n        x=[pp[0]], y=[pp[1]], z=[pp[2]],\n        mode='markers',\n        marker=dict(size=4, color=c, symbol='diamond'),\n        name=f'Proj {i}'\n    ))\n    \n    # Line connecting them\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],\n        mode='lines',\n        line=dict(color=c, width=3),\n        showlegend=False\n    ))\n\nfig.update_layout(\n    scene=dict(\n        xaxis=dict(visible=False),\n        yaxis=dict(visible=False),\n        zaxis=dict(visible=False)\n    ),\n    title=\"Point-to-Mesh Projection and SDF\"\n)\nfig.show()\n</pre> import open3d as o3d import torch import conquer3d as c3d import plotly.graph_objects as go import numpy as np import random  # 1. Create a manifold sphere mesh sphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10) sphere.compute_vertex_normals() vertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda') triangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')  # 2. Build the TriangleMesh mesh = c3d.data_structure.TriangleMesh(vertices, triangles)  # 3. Sample 5 inside points and 5 outside points inside_pts = [] for _ in range(5):     # Inside radius &lt; 1.0     r = random.uniform(0.1, 0.8)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     inside_pts.append([x,y,z])      outside_pts = [] for _ in range(5):     # Outside radius &gt; 1.0     r = random.uniform(1.2, 2.0)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     outside_pts.append([x,y,z])  query_points_np = np.array(inside_pts + outside_pts) query_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')  # 4. Query points with SDF and projected points q_ids, tri_ids, prj_pts, sdf = mesh.query_points(     query_points,      return_sdf=True,      return_prj_pts=True,      sign_mode=0,     distance_mode=0 )  prj_pts_np = prj_pts.cpu().numpy() sdf_np = sdf.cpu().numpy()  # 5. Visualize fig = go.Figure()  # Plot base mesh fig.add_trace(go.Mesh3d(     x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),     i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),     color='lightblue', opacity=0.3, name='Base Mesh' ))  # Extract edges for wireframe verts_np = vertices.cpu().numpy() tris_np = triangles.cpu().numpy() edge_x = [] edge_y = [] edge_z = [] for tri in tris_np:     v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]     edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])     edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])     edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])  fig.add_trace(go.Scatter3d(     x=edge_x, y=edge_y, z=edge_z,     mode='lines',     line=dict(color='gray', width=1),     name='Wireframe',     showlegend=False ))  colors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']  # Plot pairs of points and text for SDF for i in range(10):     qp = query_points_np[i]     pp = prj_pts_np[i]     dist = sdf_np[i]     c = colors[i]          # Query point     fig.add_trace(go.Scatter3d(         x=[qp[0]], y=[qp[1]], z=[qp[2]],         mode='markers+text',         marker=dict(size=5, color=c),         text=[f'SDF: {dist:.3f}'],         textposition='top center',         name=f'Query {i}'     ))          # Projected point     fig.add_trace(go.Scatter3d(         x=[pp[0]], y=[pp[1]], z=[pp[2]],         mode='markers',         marker=dict(size=4, color=c, symbol='diamond'),         name=f'Proj {i}'     ))          # Line connecting them     fig.add_trace(go.Scatter3d(         x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],         mode='lines',         line=dict(color=c, width=3),         showlegend=False     ))  fig.update_layout(     scene=dict(         xaxis=dict(visible=False),         yaxis=dict(visible=False),         zaxis=dict(visible=False)     ),     title=\"Point-to-Mesh Projection and SDF\" ) fig.show() <pre>Jupyter environment detected. Enabling Open3D WebVisualizer.\n[Open3D INFO] WebRTC GUI backend enabled.\n[Open3D INFO] WebRTCWindowSystem: HTTP handshake server disabled.\n</pre>"},{"location":"examples/mesh/05_query_point_sdf/#example-7-query-point-sdf-on-a-manifold-sphere","title":"Example 7: Query Point SDF on a Manifold Sphere\u00b6","text":""},{"location":"examples/mesh/06_fast_winding_number/","title":"Fast Winding Number","text":"In\u00a0[1]: Copied! <pre>import open3d as o3d\nimport torch\nimport conquer3d as c3d\nimport plotly.graph_objects as go\nimport numpy as np\nimport random\n\n# 1. Create a manifold sphere mesh\nsphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10)\nsphere.compute_vertex_normals()\nvertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda')\ntriangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')\n\n# 2. Build the TriangleMesh\nmesh = c3d.data_structure.TriangleMesh(vertices, triangles)\n\n# 3. Sample 5 inside points and 5 outside points\ninside_pts = []\nfor _ in range(5):\n    # Inside radius &lt; 1.0\n    r = random.uniform(0.1, 0.8)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    inside_pts.append([x,y,z])\n    \noutside_pts = []\nfor _ in range(5):\n    # Outside radius &gt; 1.0\n    r = random.uniform(1.2, 2.0)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    outside_pts.append([x,y,z])\n\nquery_points_np = np.array(inside_pts + outside_pts)\nquery_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')\n\n# 4. Query points with SDF and projected points\nq_ids, tri_ids, prj_pts, sdf = mesh.query_points(\n    query_points, \n    return_sdf=True, \n    return_prj_pts=True, \n    sign_mode=1,\n    distance_mode=0\n)\n\nprj_pts_np = prj_pts.cpu().numpy()\nsdf_np = sdf.cpu().numpy()\n\n# 5. Visualize\nfig = go.Figure()\n\n# Plot base mesh\nfig.add_trace(go.Mesh3d(\n    x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),\n    i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),\n    color='lightblue', opacity=0.3, name='Base Mesh'\n))\n\n# Extract edges for wireframe\nverts_np = vertices.cpu().numpy()\ntris_np = triangles.cpu().numpy()\nedge_x = []\nedge_y = []\nedge_z = []\nfor tri in tris_np:\n    v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]\n    edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])\n    edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])\n    edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])\n\nfig.add_trace(go.Scatter3d(\n    x=edge_x, y=edge_y, z=edge_z,\n    mode='lines',\n    line=dict(color='gray', width=1),\n    name='Wireframe',\n    showlegend=False\n))\n\ncolors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']\n\n# Plot pairs of points and text for SDF\nfor i in range(10):\n    qp = query_points_np[i]\n    pp = prj_pts_np[i]\n    dist = sdf_np[i]\n    c = colors[i]\n    \n    # Query point\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0]], y=[qp[1]], z=[qp[2]],\n        mode='markers+text',\n        marker=dict(size=5, color=c),\n        text=[f'SDF: {dist:.3f}'],\n        textposition='top center',\n        name=f'Query {i}'\n    ))\n    \n    # Projected point\n    fig.add_trace(go.Scatter3d(\n        x=[pp[0]], y=[pp[1]], z=[pp[2]],\n        mode='markers',\n        marker=dict(size=4, color=c, symbol='diamond'),\n        name=f'Proj {i}'\n    ))\n    \n    # Line connecting them\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],\n        mode='lines',\n        line=dict(color=c, width=3),\n        showlegend=False\n    ))\n\nfig.update_layout(\n    scene=dict(\n        xaxis=dict(visible=False),\n        yaxis=dict(visible=False),\n        zaxis=dict(visible=False)\n    ),\n    title=\"Point-to-Mesh Projection and SDF\"\n)\nfig.show()\n</pre> import open3d as o3d import torch import conquer3d as c3d import plotly.graph_objects as go import numpy as np import random  # 1. Create a manifold sphere mesh sphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10) sphere.compute_vertex_normals() vertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda') triangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')  # 2. Build the TriangleMesh mesh = c3d.data_structure.TriangleMesh(vertices, triangles)  # 3. Sample 5 inside points and 5 outside points inside_pts = [] for _ in range(5):     # Inside radius &lt; 1.0     r = random.uniform(0.1, 0.8)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     inside_pts.append([x,y,z])      outside_pts = [] for _ in range(5):     # Outside radius &gt; 1.0     r = random.uniform(1.2, 2.0)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     outside_pts.append([x,y,z])  query_points_np = np.array(inside_pts + outside_pts) query_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')  # 4. Query points with SDF and projected points q_ids, tri_ids, prj_pts, sdf = mesh.query_points(     query_points,      return_sdf=True,      return_prj_pts=True,      sign_mode=1,     distance_mode=0 )  prj_pts_np = prj_pts.cpu().numpy() sdf_np = sdf.cpu().numpy()  # 5. Visualize fig = go.Figure()  # Plot base mesh fig.add_trace(go.Mesh3d(     x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),     i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),     color='lightblue', opacity=0.3, name='Base Mesh' ))  # Extract edges for wireframe verts_np = vertices.cpu().numpy() tris_np = triangles.cpu().numpy() edge_x = [] edge_y = [] edge_z = [] for tri in tris_np:     v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]     edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])     edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])     edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])  fig.add_trace(go.Scatter3d(     x=edge_x, y=edge_y, z=edge_z,     mode='lines',     line=dict(color='gray', width=1),     name='Wireframe',     showlegend=False ))  colors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']  # Plot pairs of points and text for SDF for i in range(10):     qp = query_points_np[i]     pp = prj_pts_np[i]     dist = sdf_np[i]     c = colors[i]          # Query point     fig.add_trace(go.Scatter3d(         x=[qp[0]], y=[qp[1]], z=[qp[2]],         mode='markers+text',         marker=dict(size=5, color=c),         text=[f'SDF: {dist:.3f}'],         textposition='top center',         name=f'Query {i}'     ))          # Projected point     fig.add_trace(go.Scatter3d(         x=[pp[0]], y=[pp[1]], z=[pp[2]],         mode='markers',         marker=dict(size=4, color=c, symbol='diamond'),         name=f'Proj {i}'     ))          # Line connecting them     fig.add_trace(go.Scatter3d(         x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],         mode='lines',         line=dict(color=c, width=3),         showlegend=False     ))  fig.update_layout(     scene=dict(         xaxis=dict(visible=False),         yaxis=dict(visible=False),         zaxis=dict(visible=False)     ),     title=\"Point-to-Mesh Projection and SDF\" ) fig.show() <pre>Jupyter environment detected. Enabling Open3D WebVisualizer.\n[Open3D INFO] WebRTC GUI backend enabled.\n[Open3D INFO] WebRTCWindowSystem: HTTP handshake server disabled.\n</pre>"},{"location":"examples/mesh/06_fast_winding_number/#example-8-query-point-sdf-on-fast-winding-number","title":"Example 8: Query Point SDF on Fast Winding Number\u00b6","text":""},{"location":"examples/mesh/07_sample_points_with_colors_and_normals/","title":"Sample Points (Colors & Normals)","text":"In\u00a0[1]: Copied! <pre>import open3d as o3d\nimport torch\nimport conquer3d as c3d\nimport plotly.graph_objects as go\nimport numpy as np\nimport random\n\n# 1. Create a manifold sphere mesh\nsphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10)\nsphere.compute_vertex_normals()\nvertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda')\ntriangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')\n\n# 2. Build the TriangleMesh\nmesh = c3d.data_structure.TriangleMesh(vertices, triangles)\n\n# 3. Sample 5 inside points and 5 outside points\ninside_pts = []\nfor _ in range(5):\n    # Inside radius &lt; 1.0\n    r = random.uniform(0.1, 0.8)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    inside_pts.append([x,y,z])\n    \noutside_pts = []\nfor _ in range(5):\n    # Outside radius &gt; 1.0\n    r = random.uniform(1.2, 2.0)\n    theta = random.uniform(0, 2*np.pi)\n    phi = random.uniform(0, np.pi)\n    x = r * np.sin(phi) * np.cos(theta)\n    y = r * np.sin(phi) * np.sin(theta)\n    z = r * np.cos(phi)\n    outside_pts.append([x,y,z])\n\nquery_points_np = np.array(inside_pts + outside_pts)\nquery_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')\n\n# 4. Query points with SDF and projected points\nq_ids, tri_ids, prj_pts, sdf = mesh.query_points(\n    query_points, \n    return_sdf=True, \n    return_prj_pts=True, \n    sign_mode=1,\n    distance_mode=0\n)\n\nprj_pts_np = prj_pts.cpu().numpy()\nsdf_np = sdf.cpu().numpy()\n\n# 5. Visualize\nfig = go.Figure()\n\n# Plot base mesh\nfig.add_trace(go.Mesh3d(\n    x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),\n    i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),\n    color='lightblue', opacity=0.3, name='Base Mesh'\n))\n\n# Extract edges for wireframe\nverts_np = vertices.cpu().numpy()\ntris_np = triangles.cpu().numpy()\nedge_x = []\nedge_y = []\nedge_z = []\nfor tri in tris_np:\n    v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]\n    edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])\n    edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])\n    edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])\n\nfig.add_trace(go.Scatter3d(\n    x=edge_x, y=edge_y, z=edge_z,\n    mode='lines',\n    line=dict(color='gray', width=1),\n    name='Wireframe',\n    showlegend=False\n))\n\ncolors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']\n\n# Plot pairs of points and text for SDF\nfor i in range(10):\n    qp = query_points_np[i]\n    pp = prj_pts_np[i]\n    dist = sdf_np[i]\n    c = colors[i]\n    \n    # Query point\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0]], y=[qp[1]], z=[qp[2]],\n        mode='markers+text',\n        marker=dict(size=5, color=c),\n        text=[f'SDF: {dist:.3f}'],\n        textposition='top center',\n        name=f'Query {i}'\n    ))\n    \n    # Projected point\n    fig.add_trace(go.Scatter3d(\n        x=[pp[0]], y=[pp[1]], z=[pp[2]],\n        mode='markers',\n        marker=dict(size=4, color=c, symbol='diamond'),\n        name=f'Proj {i}'\n    ))\n    \n    # Line connecting them\n    fig.add_trace(go.Scatter3d(\n        x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],\n        mode='lines',\n        line=dict(color=c, width=3),\n        showlegend=False\n    ))\n\nfig.update_layout(\n    scene=dict(\n        xaxis=dict(visible=False),\n        yaxis=dict(visible=False),\n        zaxis=dict(visible=False)\n    ),\n    title=\"Point-to-Mesh Projection and SDF\"\n)\nfig.show()\n</pre> import open3d as o3d import torch import conquer3d as c3d import plotly.graph_objects as go import numpy as np import random  # 1. Create a manifold sphere mesh sphere = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=10) sphere.compute_vertex_normals() vertices = torch.tensor(np.asarray(sphere.vertices), dtype=torch.float32, device='cuda') triangles = torch.tensor(np.asarray(sphere.triangles), dtype=torch.int32, device='cuda')  # 2. Build the TriangleMesh mesh = c3d.data_structure.TriangleMesh(vertices, triangles)  # 3. Sample 5 inside points and 5 outside points inside_pts = [] for _ in range(5):     # Inside radius &lt; 1.0     r = random.uniform(0.1, 0.8)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     inside_pts.append([x,y,z])      outside_pts = [] for _ in range(5):     # Outside radius &gt; 1.0     r = random.uniform(1.2, 2.0)     theta = random.uniform(0, 2*np.pi)     phi = random.uniform(0, np.pi)     x = r * np.sin(phi) * np.cos(theta)     y = r * np.sin(phi) * np.sin(theta)     z = r * np.cos(phi)     outside_pts.append([x,y,z])  query_points_np = np.array(inside_pts + outside_pts) query_points = torch.tensor(query_points_np, dtype=torch.float32, device='cuda')  # 4. Query points with SDF and projected points q_ids, tri_ids, prj_pts, sdf = mesh.query_points(     query_points,      return_sdf=True,      return_prj_pts=True,      sign_mode=1,     distance_mode=0 )  prj_pts_np = prj_pts.cpu().numpy() sdf_np = sdf.cpu().numpy()  # 5. Visualize fig = go.Figure()  # Plot base mesh fig.add_trace(go.Mesh3d(     x=vertices[:, 0].cpu(), y=vertices[:, 1].cpu(), z=vertices[:, 2].cpu(),     i=triangles[:, 0].cpu(), j=triangles[:, 1].cpu(), k=triangles[:, 2].cpu(),     color='lightblue', opacity=0.3, name='Base Mesh' ))  # Extract edges for wireframe verts_np = vertices.cpu().numpy() tris_np = triangles.cpu().numpy() edge_x = [] edge_y = [] edge_z = [] for tri in tris_np:     v0, v1, v2 = verts_np[tri[0]], verts_np[tri[1]], verts_np[tri[2]]     edge_x.extend([v0[0], v1[0], v2[0], v0[0], None])     edge_y.extend([v0[1], v1[1], v2[1], v0[1], None])     edge_z.extend([v0[2], v1[2], v2[2], v0[2], None])  fig.add_trace(go.Scatter3d(     x=edge_x, y=edge_y, z=edge_z,     mode='lines',     line=dict(color='gray', width=1),     name='Wireframe',     showlegend=False ))  colors = ['red', 'green', 'blue', 'orange', 'purple', 'cyan', 'magenta', 'yellow', 'brown', 'pink']  # Plot pairs of points and text for SDF for i in range(10):     qp = query_points_np[i]     pp = prj_pts_np[i]     dist = sdf_np[i]     c = colors[i]          # Query point     fig.add_trace(go.Scatter3d(         x=[qp[0]], y=[qp[1]], z=[qp[2]],         mode='markers+text',         marker=dict(size=5, color=c),         text=[f'SDF: {dist:.3f}'],         textposition='top center',         name=f'Query {i}'     ))          # Projected point     fig.add_trace(go.Scatter3d(         x=[pp[0]], y=[pp[1]], z=[pp[2]],         mode='markers',         marker=dict(size=4, color=c, symbol='diamond'),         name=f'Proj {i}'     ))          # Line connecting them     fig.add_trace(go.Scatter3d(         x=[qp[0], pp[0]], y=[qp[1], pp[1]], z=[qp[2], pp[2]],         mode='lines',         line=dict(color=c, width=3),         showlegend=False     ))  fig.update_layout(     scene=dict(         xaxis=dict(visible=False),         yaxis=dict(visible=False),         zaxis=dict(visible=False)     ),     title=\"Point-to-Mesh Projection and SDF\" ) fig.show() <pre>Jupyter environment detected. Enabling Open3D WebVisualizer.\n[Open3D INFO] WebRTC GUI backend enabled.\n[Open3D INFO] WebRTCWindowSystem: HTTP handshake server disabled.\n</pre> In\u00a0[2]: Copied! <pre>import matplotlib.cm as cm\nimport plotly.subplots as sp\n\n# 1. Create a rainbow vertex color mapped to the Y axis\ny = vertices[:, 1]\ny_norm = (y - y.min()) / (y.max() - y.min())\nrainbow_colors = torch.tensor(cm.rainbow(y_norm.cpu().numpy())[:, :3], dtype=torch.float32, device='cuda')\n\n# Re-instantiate the TriangleMesh with vertex colors\nmesh_colored = c3d.data_structure.TriangleMesh(vertices, triangles, in_vertex_colors=rainbow_colors)\n\n# 2. Sample points with flat normals\npts_flat, _, n_flat, c_flat = mesh_colored.sample_points(\n    num_points=30000, uniform=True, return_normals=True, return_colors=True, use_triangle_normal=True\n)\npts_flat = pts_flat.cpu().numpy()\nc_flat = (c_flat.cpu().numpy() * 255).astype(int)\nn_flat = ((n_flat.cpu().numpy() + 1.0) / 2.0 * 255).astype(int) # map normals to RGB for visualization\n\n# 3. Sample points with smooth normals\npts_smooth, _, n_smooth, c_smooth = mesh_colored.sample_points(\n    num_points=30000, uniform=True, return_normals=True, return_colors=True, use_triangle_normal=False\n)\npts_smooth = pts_smooth.cpu().numpy()\nc_smooth = (c_smooth.cpu().numpy() * 255).astype(int)\nn_smooth = ((n_smooth.cpu().numpy() + 1.0) / 2.0 * 255).astype(int) # map normals to RGB for visualization\n\ndef to_hex(c): return [f'#{r:02x}{g:02x}{b:02x}' for r, g, b in c]\n\n# 4. Plot differences\nfig = sp.make_subplots(rows=2, cols=2, \n                       specs=[[{'type': 'scatter3d'}, {'type': 'scatter3d'}],\n                              [{'type': 'scatter3d'}, {'type': 'scatter3d'}]],\n                       subplot_titles=('Flat Normal - Colors', 'Smooth Normal - Colors',\n                                       'Flat Normal - Normals', 'Smooth Normal - Normals'))\n\n# Top row: Colors\nfig.add_trace(go.Scatter3d(x=pts_flat[:,0], y=pts_flat[:,1], z=pts_flat[:,2], mode='markers', marker=dict(size=2, color=to_hex(c_flat))), row=1, col=1)\nfig.add_trace(go.Scatter3d(x=pts_smooth[:,0], y=pts_smooth[:,1], z=pts_smooth[:,2], mode='markers', marker=dict(size=2, color=to_hex(c_smooth))), row=1, col=2)\n\n# Bottom row: Normals mapped to RGB\nfig.add_trace(go.Scatter3d(x=pts_flat[:,0], y=pts_flat[:,1], z=pts_flat[:,2], mode='markers', marker=dict(size=2, color=to_hex(n_flat))), row=2, col=1)\nfig.add_trace(go.Scatter3d(x=pts_smooth[:,0], y=pts_smooth[:,1], z=pts_smooth[:,2], mode='markers', marker=dict(size=2, color=to_hex(n_smooth))), row=2, col=2)\n\nfig.update_layout(height=800, width=1000, title_text=\"Point Cloud Sampling: Colors and Normals\", showlegend=False)\nfig.show()\n</pre> import matplotlib.cm as cm import plotly.subplots as sp  # 1. Create a rainbow vertex color mapped to the Y axis y = vertices[:, 1] y_norm = (y - y.min()) / (y.max() - y.min()) rainbow_colors = torch.tensor(cm.rainbow(y_norm.cpu().numpy())[:, :3], dtype=torch.float32, device='cuda')  # Re-instantiate the TriangleMesh with vertex colors mesh_colored = c3d.data_structure.TriangleMesh(vertices, triangles, in_vertex_colors=rainbow_colors)  # 2. Sample points with flat normals pts_flat, _, n_flat, c_flat = mesh_colored.sample_points(     num_points=30000, uniform=True, return_normals=True, return_colors=True, use_triangle_normal=True ) pts_flat = pts_flat.cpu().numpy() c_flat = (c_flat.cpu().numpy() * 255).astype(int) n_flat = ((n_flat.cpu().numpy() + 1.0) / 2.0 * 255).astype(int) # map normals to RGB for visualization  # 3. Sample points with smooth normals pts_smooth, _, n_smooth, c_smooth = mesh_colored.sample_points(     num_points=30000, uniform=True, return_normals=True, return_colors=True, use_triangle_normal=False ) pts_smooth = pts_smooth.cpu().numpy() c_smooth = (c_smooth.cpu().numpy() * 255).astype(int) n_smooth = ((n_smooth.cpu().numpy() + 1.0) / 2.0 * 255).astype(int) # map normals to RGB for visualization  def to_hex(c): return [f'#{r:02x}{g:02x}{b:02x}' for r, g, b in c]  # 4. Plot differences fig = sp.make_subplots(rows=2, cols=2,                         specs=[[{'type': 'scatter3d'}, {'type': 'scatter3d'}],                               [{'type': 'scatter3d'}, {'type': 'scatter3d'}]],                        subplot_titles=('Flat Normal - Colors', 'Smooth Normal - Colors',                                        'Flat Normal - Normals', 'Smooth Normal - Normals'))  # Top row: Colors fig.add_trace(go.Scatter3d(x=pts_flat[:,0], y=pts_flat[:,1], z=pts_flat[:,2], mode='markers', marker=dict(size=2, color=to_hex(c_flat))), row=1, col=1) fig.add_trace(go.Scatter3d(x=pts_smooth[:,0], y=pts_smooth[:,1], z=pts_smooth[:,2], mode='markers', marker=dict(size=2, color=to_hex(c_smooth))), row=1, col=2)  # Bottom row: Normals mapped to RGB fig.add_trace(go.Scatter3d(x=pts_flat[:,0], y=pts_flat[:,1], z=pts_flat[:,2], mode='markers', marker=dict(size=2, color=to_hex(n_flat))), row=2, col=1) fig.add_trace(go.Scatter3d(x=pts_smooth[:,0], y=pts_smooth[:,1], z=pts_smooth[:,2], mode='markers', marker=dict(size=2, color=to_hex(n_smooth))), row=2, col=2)  fig.update_layout(height=800, width=1000, title_text=\"Point Cloud Sampling: Colors and Normals\", showlegend=False) fig.show()"},{"location":"examples/mesh/07_sample_points_with_colors_and_normals/#example-8-query-point-sdf-on-fast-winding-number","title":"Example 8: Query Point SDF on Fast Winding Number\u00b6","text":""},{"location":"examples/mesh/07_sample_points_with_colors_and_normals/#9-sample-points-with-colors-and-normals","title":"9. Sample Points with Colors and Normals\u00b6","text":"<p>We assign a rainbow colormap to the mesh based on the Y-coordinate. Then we sample points with <code>return_normals=True</code> and <code>return_colors=True</code>, comparing the flat normals (<code>use_triangle_normal=True</code>) and smoothly interpolated vertex normals (<code>use_triangle_normal=False</code>).</p>"},{"location":"examples/spatial/ds_bvh/","title":"Bounding Volume Hierarchy (BVH) Example","text":"In\u00a0[1]: Copied! <pre>import torch\nimport numpy as np\nimport plotly.graph_objects as go\nimport open3d as o3d\nimport time\n\nfrom conquer3d.data_structure import BVH\n\ndevice = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\nprint(f\"Running on: {device}\")\n\nsphere_mesh = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=20)\n\nvertices = torch.tensor(np.asarray(sphere_mesh.vertices), dtype=torch.float32, device=device)\ntriangles = torch.tensor(np.asarray(sphere_mesh.triangles), dtype=torch.long, device=device)\n\nprint(f\"Mesh contains {triangles.shape[0]} triangles.\")\n\ntri_verts = vertices[triangles]\n\naabb_mins, _ = torch.min(tri_verts, dim=1)\naabb_maxs, _ = torch.max(tri_verts, dim=1)\n\nprint(\"Building GPU BVH...\")\nstart_time = time.perf_counter()\nbvh = BVH(aabb_mins, aabb_maxs)\ntorch.cuda.synchronize()\nprint(f\"BVH Built in {(time.perf_counter() - start_time) * 1000:.2f} ms\")\n\nquery_verts = torch.tensor([\n    [-1.0, 0.5, -0.3],\n    [ 1.0, -0.5, -0.3],\n    [ 0.0, 0.0,  2.0]\n], dtype=torch.float32, device=device)\n\nquery_min, _ = torch.min(query_verts, dim=0, keepdim=True)\nquery_max, _ = torch.max(query_verts, dim=0, keepdim=True)\n\nprint(\"Querying BVH...\")\nstart_time = time.perf_counter()\n\nq_ids, intersected_tri_ids = bvh.query(query_min, query_max)\ntorch.cuda.synchronize()\n\nnum_hits = intersected_tri_ids.size(0)\nprint(f\"Query Completed in {(time.perf_counter() - start_time) * 1000:.2f} ms\")\nprint(f\"Found {num_hits} broad-phase triangle intersections!\")\n\nv_np = vertices.cpu().numpy()\nt_np = triangles.cpu().numpy()\nintersected_idx = intersected_tri_ids.cpu().numpy()\nq_v_np = query_verts.cpu().numpy()\n\nfig = go.Figure()\n\nfig.add_trace(go.Mesh3d(\n    x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n    i=t_np[:, 0], j=t_np[:, 1], k=t_np[:, 2],\n    color='lightgray',\n    opacity=0.5,\n    name='Base Mesh',\n    hoverinfo='skip'\n))\n\nif num_hits &gt; 0:\n    hit_triangles = t_np[intersected_idx]\n    fig.add_trace(go.Mesh3d(\n        x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],\n        i=hit_triangles[:, 0], j=hit_triangles[:, 1], k=hit_triangles[:, 2],\n        color='red',\n        opacity=1.0,\n        name='Intersected AABBs',\n        flatshading=True\n    ))\n    \n    hit_mins = aabb_mins.cpu().numpy()[intersected_idx]\n    hit_maxs = aabb_maxs.cpu().numpy()[intersected_idx]\n\n    box_x, box_y, box_z = [], [], []\n    \n    # Build line segments for the boxes. We insert `None` to break the continuous \n    # Plotly line between different edges/boxes so they don't connect to each other.\n    for min_pt, max_pt in zip(hit_mins, hit_maxs):\n        x0, y0, z0 = min_pt\n        x1, y1, z1 = max_pt\n\n        # Bottom face ring\n        box_x.extend([x0, x1, x1, x0, x0, None])\n        box_y.extend([y0, y0, y1, y1, y0, None])\n        box_z.extend([z0, z0, z0, z0, z0, None])\n\n        # Top face ring\n        box_x.extend([x0, x1, x1, x0, x0, None])\n        box_y.extend([y0, y0, y1, y1, y0, None])\n        box_z.extend([z1, z1, z1, z1, z1, None])\n\n        # 4 Vertical Pillars connecting top and bottom\n        box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])\n        box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])\n        box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])\n\n    fig.add_trace(go.Scatter3d(\n        x=box_x, y=box_y, z=box_z,\n        mode='lines',\n        line=dict(color='orange', width=2),\n        name='AABB Wireframes',\n        hoverinfo='skip'\n    ))\n\nfig.add_trace(go.Mesh3d(\n    x=q_v_np[:, 0], y=q_v_np[:, 1], z=q_v_np[:, 2],\n    i=[0], j=[1], k=[2],\n    color='blue',\n    opacity=0.6,\n    name='Query Triangle'\n))\n\nq_min_np = query_min.cpu().numpy()[0]\nq_max_np = query_max.cpu().numpy()[0]\n\nqbox_x, qbox_y, qbox_z = [], [], []\nqx0, qy0, qz0 = q_min_np\nqx1, qy1, qz1 = q_max_np\n\n# Bottom\nqbox_x.extend([qx0, qx1, qx1, qx0, qx0, None])\nqbox_y.extend([qy0, qy0, qy1, qy1, qy0, None])\nqbox_z.extend([qz0, qz0, qz0, qz0, qz0, None])\n# Top\nqbox_x.extend([qx0, qx1, qx1, qx0, qx0, None])\nqbox_y.extend([qy0, qy0, qy1, qy1, qy0, None])\nqbox_z.extend([qz1, qz1, qz1, qz1, qz1, None])\n# Pillars\nqbox_x.extend([qx0, qx0, None, qx1, qx1, None, qx1, qx1, None, qx0, qx0, None])\nqbox_y.extend([qy0, qy0, None, qy0, qy0, None, qy1, qy1, None, qy1, qy1, None])\nqbox_z.extend([qz0, qz1, None, qz0, qz1, None, qz0, qz1, None, qz0, qz1, None])\n\nfig.add_trace(go.Scatter3d(\n    x=qbox_x, y=qbox_y, z=qbox_z,\n    mode='lines',\n    line=dict(color='cyan', width=4, dash='dash'),\n    name='Query AABB',\n    hoverinfo='skip'\n))\n\nfig.update_layout(\n    title=\"BVH Broad-Phase AABB Intersection\",\n    scene=dict(\n        xaxis=dict(showbackground=False, visible=False),\n        yaxis=dict(showbackground=False, visible=False),\n        zaxis=dict(showbackground=False, visible=False),\n        aspectmode='data'\n    ),\n    margin=dict(l=0, r=0, b=0, t=40)\n)\n\nfig.show()\n</pre> import torch import numpy as np import plotly.graph_objects as go import open3d as o3d import time  from conquer3d.data_structure import BVH  device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\") print(f\"Running on: {device}\")  sphere_mesh = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=20)  vertices = torch.tensor(np.asarray(sphere_mesh.vertices), dtype=torch.float32, device=device) triangles = torch.tensor(np.asarray(sphere_mesh.triangles), dtype=torch.long, device=device)  print(f\"Mesh contains {triangles.shape[0]} triangles.\")  tri_verts = vertices[triangles]  aabb_mins, _ = torch.min(tri_verts, dim=1) aabb_maxs, _ = torch.max(tri_verts, dim=1)  print(\"Building GPU BVH...\") start_time = time.perf_counter() bvh = BVH(aabb_mins, aabb_maxs) torch.cuda.synchronize() print(f\"BVH Built in {(time.perf_counter() - start_time) * 1000:.2f} ms\")  query_verts = torch.tensor([     [-1.0, 0.5, -0.3],     [ 1.0, -0.5, -0.3],     [ 0.0, 0.0,  2.0] ], dtype=torch.float32, device=device)  query_min, _ = torch.min(query_verts, dim=0, keepdim=True) query_max, _ = torch.max(query_verts, dim=0, keepdim=True)  print(\"Querying BVH...\") start_time = time.perf_counter()  q_ids, intersected_tri_ids = bvh.query(query_min, query_max) torch.cuda.synchronize()  num_hits = intersected_tri_ids.size(0) print(f\"Query Completed in {(time.perf_counter() - start_time) * 1000:.2f} ms\") print(f\"Found {num_hits} broad-phase triangle intersections!\")  v_np = vertices.cpu().numpy() t_np = triangles.cpu().numpy() intersected_idx = intersected_tri_ids.cpu().numpy() q_v_np = query_verts.cpu().numpy()  fig = go.Figure()  fig.add_trace(go.Mesh3d(     x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],     i=t_np[:, 0], j=t_np[:, 1], k=t_np[:, 2],     color='lightgray',     opacity=0.5,     name='Base Mesh',     hoverinfo='skip' ))  if num_hits &gt; 0:     hit_triangles = t_np[intersected_idx]     fig.add_trace(go.Mesh3d(         x=v_np[:, 0], y=v_np[:, 1], z=v_np[:, 2],         i=hit_triangles[:, 0], j=hit_triangles[:, 1], k=hit_triangles[:, 2],         color='red',         opacity=1.0,         name='Intersected AABBs',         flatshading=True     ))          hit_mins = aabb_mins.cpu().numpy()[intersected_idx]     hit_maxs = aabb_maxs.cpu().numpy()[intersected_idx]      box_x, box_y, box_z = [], [], []          # Build line segments for the boxes. We insert `None` to break the continuous      # Plotly line between different edges/boxes so they don't connect to each other.     for min_pt, max_pt in zip(hit_mins, hit_maxs):         x0, y0, z0 = min_pt         x1, y1, z1 = max_pt          # Bottom face ring         box_x.extend([x0, x1, x1, x0, x0, None])         box_y.extend([y0, y0, y1, y1, y0, None])         box_z.extend([z0, z0, z0, z0, z0, None])          # Top face ring         box_x.extend([x0, x1, x1, x0, x0, None])         box_y.extend([y0, y0, y1, y1, y0, None])         box_z.extend([z1, z1, z1, z1, z1, None])          # 4 Vertical Pillars connecting top and bottom         box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])         box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])         box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])      fig.add_trace(go.Scatter3d(         x=box_x, y=box_y, z=box_z,         mode='lines',         line=dict(color='orange', width=2),         name='AABB Wireframes',         hoverinfo='skip'     ))  fig.add_trace(go.Mesh3d(     x=q_v_np[:, 0], y=q_v_np[:, 1], z=q_v_np[:, 2],     i=[0], j=[1], k=[2],     color='blue',     opacity=0.6,     name='Query Triangle' ))  q_min_np = query_min.cpu().numpy()[0] q_max_np = query_max.cpu().numpy()[0]  qbox_x, qbox_y, qbox_z = [], [], [] qx0, qy0, qz0 = q_min_np qx1, qy1, qz1 = q_max_np  # Bottom qbox_x.extend([qx0, qx1, qx1, qx0, qx0, None]) qbox_y.extend([qy0, qy0, qy1, qy1, qy0, None]) qbox_z.extend([qz0, qz0, qz0, qz0, qz0, None]) # Top qbox_x.extend([qx0, qx1, qx1, qx0, qx0, None]) qbox_y.extend([qy0, qy0, qy1, qy1, qy0, None]) qbox_z.extend([qz1, qz1, qz1, qz1, qz1, None]) # Pillars qbox_x.extend([qx0, qx0, None, qx1, qx1, None, qx1, qx1, None, qx0, qx0, None]) qbox_y.extend([qy0, qy0, None, qy0, qy0, None, qy1, qy1, None, qy1, qy1, None]) qbox_z.extend([qz0, qz1, None, qz0, qz1, None, qz0, qz1, None, qz0, qz1, None])  fig.add_trace(go.Scatter3d(     x=qbox_x, y=qbox_y, z=qbox_z,     mode='lines',     line=dict(color='cyan', width=4, dash='dash'),     name='Query AABB',     hoverinfo='skip' ))  fig.update_layout(     title=\"BVH Broad-Phase AABB Intersection\",     scene=dict(         xaxis=dict(showbackground=False, visible=False),         yaxis=dict(showbackground=False, visible=False),         zaxis=dict(showbackground=False, visible=False),         aspectmode='data'     ),     margin=dict(l=0, r=0, b=0, t=40) )  fig.show() <pre>Jupyter environment detected. Enabling Open3D WebVisualizer.\n[Open3D INFO] WebRTC GUI backend enabled.\n[Open3D INFO] WebRTCWindowSystem: HTTP handshake server disabled.\nRunning on: cuda\nMesh contains 1520 triangles.\nBuilding GPU BVH...\nBVH Built in 1.21 ms\nQuerying BVH...\nQuery Completed in 2.67 ms\nFound 578 broad-phase triangle intersections!\n</pre> In\u00a0[2]: Copied! <pre>import torch\nimport plotly.graph_objects as go\nimport conquer3d\nimport numpy as np\n\n# Generate N random AABBs\nN = 10\ntorch.manual_seed(42)\n\n# Random centers and half-extents\ncenters = torch.rand((N, 3), dtype=torch.float32, device=\"cuda\") * 10\nextents = torch.rand((N, 3), dtype=torch.float32, device=\"cuda\") * 2 + 0.5\n\naabb_mins = centers - extents\naabb_maxs = centers + extents\n\n# Build BVH directly from AABBs\nbvh = conquer3d.data_structure.BVH(aabb_mins, aabb_maxs)\n\n# Query self intersection\nquery_ids, object_ids = bvh.query_self()\nprint(f\"Found {query_ids.size(0)} intersecting pairs!\")\nif query_ids.size(0) &gt; 0:\n    for q, o in zip(query_ids, object_ids):\n        print(f\"Box {q.item()} intersects with Box {o.item()}\")\n\n# Visualization with Plotly\nfig = go.Figure()\n\nintersecting_indices = set()\nif query_ids.size(0) &gt; 0:\n    intersecting_indices.update(query_ids.cpu().numpy().tolist())\n    intersecting_indices.update(object_ids.cpu().numpy().tolist())\n\nmins_np = aabb_mins.cpu().numpy()\nmaxs_np = aabb_maxs.cpu().numpy()\n\ndef get_aabb_lines(min_pt, max_pt):\n    x0, y0, z0 = min_pt\n    x1, y1, z1 = max_pt\n    # 12 edges of a box\n    x = [x0, x1, x1, x0, x0, None, x0, x1, x1, x0, x0, None, x0, x0, None, x1, x1, None, x1, x1, None, x0, x0]\n    y = [y0, y0, y1, y1, y0, None, y0, y0, y1, y1, y0, None, y0, y0, None, y0, y0, None, y1, y1, None, y1, y1]\n    z = [z0, z0, z0, z0, z0, None, z1, z1, z1, z1, z1, None, z0, z1, None, z0, z1, None, z0, z1, None, z0, z1]\n    return x, y, z\n\nfor i in range(N):\n    x, y, z = get_aabb_lines(mins_np[i], maxs_np[i])\n    \n    is_intersecting = i in intersecting_indices\n    color = 'red' if is_intersecting else 'blue'\n    width = 5 if is_intersecting else 2\n    name = f\"Intersecting Box {i}\" if is_intersecting else f\"Box {i}\"\n    \n    fig.add_trace(go.Scatter3d(\n        x=x, y=y, z=z,\n        mode='lines',\n        line=dict(color=color, width=width),\n        name=name\n    ))\n\nfig.update_layout(scene=dict(aspectmode='data'), title=\"BVH Self Intersection (Red=Intersecting)\")\nfig.show()\n</pre> import torch import plotly.graph_objects as go import conquer3d import numpy as np  # Generate N random AABBs N = 10 torch.manual_seed(42)  # Random centers and half-extents centers = torch.rand((N, 3), dtype=torch.float32, device=\"cuda\") * 10 extents = torch.rand((N, 3), dtype=torch.float32, device=\"cuda\") * 2 + 0.5  aabb_mins = centers - extents aabb_maxs = centers + extents  # Build BVH directly from AABBs bvh = conquer3d.data_structure.BVH(aabb_mins, aabb_maxs)  # Query self intersection query_ids, object_ids = bvh.query_self() print(f\"Found {query_ids.size(0)} intersecting pairs!\") if query_ids.size(0) &gt; 0:     for q, o in zip(query_ids, object_ids):         print(f\"Box {q.item()} intersects with Box {o.item()}\")  # Visualization with Plotly fig = go.Figure()  intersecting_indices = set() if query_ids.size(0) &gt; 0:     intersecting_indices.update(query_ids.cpu().numpy().tolist())     intersecting_indices.update(object_ids.cpu().numpy().tolist())  mins_np = aabb_mins.cpu().numpy() maxs_np = aabb_maxs.cpu().numpy()  def get_aabb_lines(min_pt, max_pt):     x0, y0, z0 = min_pt     x1, y1, z1 = max_pt     # 12 edges of a box     x = [x0, x1, x1, x0, x0, None, x0, x1, x1, x0, x0, None, x0, x0, None, x1, x1, None, x1, x1, None, x0, x0]     y = [y0, y0, y1, y1, y0, None, y0, y0, y1, y1, y0, None, y0, y0, None, y0, y0, None, y1, y1, None, y1, y1]     z = [z0, z0, z0, z0, z0, None, z1, z1, z1, z1, z1, None, z0, z1, None, z0, z1, None, z0, z1, None, z0, z1]     return x, y, z  for i in range(N):     x, y, z = get_aabb_lines(mins_np[i], maxs_np[i])          is_intersecting = i in intersecting_indices     color = 'red' if is_intersecting else 'blue'     width = 5 if is_intersecting else 2     name = f\"Intersecting Box {i}\" if is_intersecting else f\"Box {i}\"          fig.add_trace(go.Scatter3d(         x=x, y=y, z=z,         mode='lines',         line=dict(color=color, width=width),         name=name     ))  fig.update_layout(scene=dict(aspectmode='data'), title=\"BVH Self Intersection (Red=Intersecting)\") fig.show() <pre>Found 2 intersecting pairs!\nBox 0 intersects with Box 4\nBox 6 intersects with Box 8\n</pre> In\u00a0[3]: Copied! <pre># 1. Generate 5 random rays\nnum_rays = 5\nray_origins = torch.rand((num_rays, 3), dtype=torch.float32, device=\"cuda\") * 10\nray_dirs = torch.randn((num_rays, 3), dtype=torch.float32, device=\"cuda\")\nray_dirs = ray_dirs / torch.norm(ray_dirs, dim=-1, keepdim=True)\n\n# 2. Query the BVH\nray_ids, aabb_ids = bvh.query_ray(ray_origins, ray_dirs)\n\nprint(f\"Found {len(ray_ids)} total ray-AABB intersections.\")\n\n# 3. Visualization\nfig = go.Figure()\n\nintersected_aabb_ids = aabb_ids.unique().cpu().numpy().tolist()\n\nfor i in range(N):\n    x, y, z = get_aabb_lines(mins_np[i], maxs_np[i])\n    \n    is_intersecting = i in intersected_aabb_ids\n    color = 'red' if is_intersecting else 'lightgray'\n    width = 4 if is_intersecting else 2\n    name = f\"Intersected Box {i}\" if is_intersecting else f\"Box {i}\"\n    \n    fig.add_trace(go.Scatter3d(\n        x=x, y=y, z=z,\n        mode='lines',\n        line=dict(color=color, width=width),\n        name=name,\n        hoverinfo='none' if not is_intersecting else 'all'\n    ))\n\n# Plot the 5 rays\nray_origins_cpu = ray_origins.cpu()\nray_dirs_cpu = ray_dirs.cpu()\n\nfor i in range(num_rays):\n    ro = ray_origins_cpu[i]\n    rd = ray_dirs_cpu[i]\n    # Draw ray as a line from origin to origin + 15.0 * direction\n    end_pt = ro + rd * 15.0\n    fig.add_trace(go.Scatter3d(\n        x=[ro[0], end_pt[0]],\n        y=[ro[1], end_pt[1]],\n        z=[ro[2], end_pt[2]],\n        mode='lines+markers',\n        marker=dict(size=4, color='blue'),\n        line=dict(color='blue', width=3),\n        name=f'Ray {i}'\n    ))\n\nfig.update_layout(\n    scene=dict(aspectmode='data'),\n    title=\"Ray-AABB Intersection using BVH\",\n    margin=dict(l=0, r=0, b=0, t=40)\n)\nfig.show()\n</pre> # 1. Generate 5 random rays num_rays = 5 ray_origins = torch.rand((num_rays, 3), dtype=torch.float32, device=\"cuda\") * 10 ray_dirs = torch.randn((num_rays, 3), dtype=torch.float32, device=\"cuda\") ray_dirs = ray_dirs / torch.norm(ray_dirs, dim=-1, keepdim=True)  # 2. Query the BVH ray_ids, aabb_ids = bvh.query_ray(ray_origins, ray_dirs)  print(f\"Found {len(ray_ids)} total ray-AABB intersections.\")  # 3. Visualization fig = go.Figure()  intersected_aabb_ids = aabb_ids.unique().cpu().numpy().tolist()  for i in range(N):     x, y, z = get_aabb_lines(mins_np[i], maxs_np[i])          is_intersecting = i in intersected_aabb_ids     color = 'red' if is_intersecting else 'lightgray'     width = 4 if is_intersecting else 2     name = f\"Intersected Box {i}\" if is_intersecting else f\"Box {i}\"          fig.add_trace(go.Scatter3d(         x=x, y=y, z=z,         mode='lines',         line=dict(color=color, width=width),         name=name,         hoverinfo='none' if not is_intersecting else 'all'     ))  # Plot the 5 rays ray_origins_cpu = ray_origins.cpu() ray_dirs_cpu = ray_dirs.cpu()  for i in range(num_rays):     ro = ray_origins_cpu[i]     rd = ray_dirs_cpu[i]     # Draw ray as a line from origin to origin + 15.0 * direction     end_pt = ro + rd * 15.0     fig.add_trace(go.Scatter3d(         x=[ro[0], end_pt[0]],         y=[ro[1], end_pt[1]],         z=[ro[2], end_pt[2]],         mode='lines+markers',         marker=dict(size=4, color='blue'),         line=dict(color='blue', width=3),         name=f'Ray {i}'     ))  fig.update_layout(     scene=dict(aspectmode='data'),     title=\"Ray-AABB Intersection using BVH\",     margin=dict(l=0, r=0, b=0, t=40) ) fig.show()  <pre>Found 3 total ray-AABB intersections.\n</pre> In\u00a0[4]: Copied! <pre>import plotly.graph_objects as go\nimport torch\nimport numpy as np\nimport plotly.express as px\nimport conquer3d\n\n# RE-BUILD THE SPHERE BVH\n# (The 'bvh' variable was overwritten by Example 2 and 3)\ntri_verts = vertices[triangles]\naabb_mins, _ = torch.min(tri_verts, dim=1)\naabb_maxs, _ = torch.max(tri_verts, dim=1)\nbvh = conquer3d.data_structure.BVH(aabb_mins, aabb_maxs)\n\n# 1. Generate 20 random query points\n# The sphere has radius 1.0 at origin, so we generate points uniformly in [-1.5, 1.5]\nquery_points = (torch.rand(20, 3, dtype=torch.float32, device=device) * 3.0) - 1.5\n\n# 2. Perform the closest point BVH query\nquery_ids, obj_ids, distances = bvh.query_point(query_points)\n\n# 3. Visualization\nfig = go.Figure()\n\n# Add the base mesh (semi-transparent)\nvertices_np = vertices.cpu().numpy()\ntriangles_np = triangles.cpu().numpy()\nfig.add_trace(go.Mesh3d(\n    x=vertices_np[:, 0], y=vertices_np[:, 1], z=vertices_np[:, 2],\n    i=triangles_np[:, 0], j=triangles_np[:, 1], k=triangles_np[:, 2],\n    color='lightgray', opacity=0.1, name='Base Mesh', hoverinfo='skip'\n))\n\n# Gather all AABBs into one trace (Default Color: lightgray)\naabb_mins_np = aabb_mins.cpu().numpy()\naabb_maxs_np = aabb_maxs.cpu().numpy()\n\nall_box_x, all_box_y, all_box_z = [], [], []\n\nfor min_pt, max_pt in zip(aabb_mins_np, aabb_maxs_np):\n    x0, y0, z0 = min_pt\n    x1, y1, z1 = max_pt\n    \n    # Bottom\n    all_box_x.extend([x0, x1, x1, x0, x0, None])\n    all_box_y.extend([y0, y0, y1, y1, y0, None])\n    all_box_z.extend([z0, z0, z0, z0, z0, None])\n    # Top\n    all_box_x.extend([x0, x1, x1, x0, x0, None])\n    all_box_y.extend([y0, y0, y1, y1, y0, None])\n    all_box_z.extend([z1, z1, z1, z1, z1, None])\n    # Pillars\n    all_box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])\n    all_box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])\n    all_box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])\n\nfig.add_trace(go.Scatter3d(\n    x=all_box_x, y=all_box_y, z=all_box_z,\n    mode='lines',\n    line=dict(color='lightgray', width=1),\n    name='All AABBs',\n    hoverinfo='skip'\n))\n\n# Generate distinct colors for 20 points\ncolors = px.colors.qualitative.Alphabet\n\n# Add the points and their corresponding closest AABBs\nquery_pts_np = query_points.cpu().numpy()\n\nfor i in range(len(query_ids)):\n    q_id = query_ids[i].item()\n    obj_id = obj_ids[i].item()\n    dist = distances[i].item()\n    color = colors[i % len(colors)]\n    \n    # Plot the query point\n    pt = query_pts_np[q_id]\n    fig.add_trace(go.Scatter3d(\n        x=[pt[0]], y=[pt[1]], z=[pt[2]],\n        mode='markers',\n        marker=dict(color=color, size=6, symbol='circle'),\n        name=f'Query {q_id} (Dist: {dist:.3f})'\n    ))\n    \n    # Get the closest AABB\n    min_pt = aabb_mins_np[obj_id]\n    max_pt = aabb_maxs_np[obj_id]\n    \n    # Create wireframe for AABB\n    x0, y0, z0 = min_pt\n    x1, y1, z1 = max_pt\n    box_x, box_y, box_z = [], [], []\n    \n    # Bottom\n    box_x.extend([x0, x1, x1, x0, x0, None])\n    box_y.extend([y0, y0, y1, y1, y0, None])\n    box_z.extend([z0, z0, z0, z0, z0, None])\n    # Top\n    box_x.extend([x0, x1, x1, x0, x0, None])\n    box_y.extend([y0, y0, y1, y1, y0, None])\n    box_z.extend([z1, z1, z1, z1, z1, None])\n    # Pillars\n    box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])\n    box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])\n    box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])\n               \n    fig.add_trace(go.Scatter3d(\n        x=box_x, y=box_y, z=box_z,\n        mode='lines',\n        line=dict(color=color, width=4),\n        name=f'Closest AABB {obj_id}',\n        hoverinfo='skip'\n    ))\n\nfig.update_layout(\n    title=\"BVH Closest Point Query (Sphere Mesh)\",\n    scene=dict(\n        xaxis=dict(showbackground=False, visible=False),\n        yaxis=dict(showbackground=False, visible=False),\n        zaxis=dict(showbackground=False, visible=False),\n        aspectmode='data'\n    ),\n    margin=dict(l=0, r=0, b=0, t=40)\n)\nfig.show()\n</pre> import plotly.graph_objects as go import torch import numpy as np import plotly.express as px import conquer3d  # RE-BUILD THE SPHERE BVH # (The 'bvh' variable was overwritten by Example 2 and 3) tri_verts = vertices[triangles] aabb_mins, _ = torch.min(tri_verts, dim=1) aabb_maxs, _ = torch.max(tri_verts, dim=1) bvh = conquer3d.data_structure.BVH(aabb_mins, aabb_maxs)  # 1. Generate 20 random query points # The sphere has radius 1.0 at origin, so we generate points uniformly in [-1.5, 1.5] query_points = (torch.rand(20, 3, dtype=torch.float32, device=device) * 3.0) - 1.5  # 2. Perform the closest point BVH query query_ids, obj_ids, distances = bvh.query_point(query_points)  # 3. Visualization fig = go.Figure()  # Add the base mesh (semi-transparent) vertices_np = vertices.cpu().numpy() triangles_np = triangles.cpu().numpy() fig.add_trace(go.Mesh3d(     x=vertices_np[:, 0], y=vertices_np[:, 1], z=vertices_np[:, 2],     i=triangles_np[:, 0], j=triangles_np[:, 1], k=triangles_np[:, 2],     color='lightgray', opacity=0.1, name='Base Mesh', hoverinfo='skip' ))  # Gather all AABBs into one trace (Default Color: lightgray) aabb_mins_np = aabb_mins.cpu().numpy() aabb_maxs_np = aabb_maxs.cpu().numpy()  all_box_x, all_box_y, all_box_z = [], [], []  for min_pt, max_pt in zip(aabb_mins_np, aabb_maxs_np):     x0, y0, z0 = min_pt     x1, y1, z1 = max_pt          # Bottom     all_box_x.extend([x0, x1, x1, x0, x0, None])     all_box_y.extend([y0, y0, y1, y1, y0, None])     all_box_z.extend([z0, z0, z0, z0, z0, None])     # Top     all_box_x.extend([x0, x1, x1, x0, x0, None])     all_box_y.extend([y0, y0, y1, y1, y0, None])     all_box_z.extend([z1, z1, z1, z1, z1, None])     # Pillars     all_box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])     all_box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])     all_box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])  fig.add_trace(go.Scatter3d(     x=all_box_x, y=all_box_y, z=all_box_z,     mode='lines',     line=dict(color='lightgray', width=1),     name='All AABBs',     hoverinfo='skip' ))  # Generate distinct colors for 20 points colors = px.colors.qualitative.Alphabet  # Add the points and their corresponding closest AABBs query_pts_np = query_points.cpu().numpy()  for i in range(len(query_ids)):     q_id = query_ids[i].item()     obj_id = obj_ids[i].item()     dist = distances[i].item()     color = colors[i % len(colors)]          # Plot the query point     pt = query_pts_np[q_id]     fig.add_trace(go.Scatter3d(         x=[pt[0]], y=[pt[1]], z=[pt[2]],         mode='markers',         marker=dict(color=color, size=6, symbol='circle'),         name=f'Query {q_id} (Dist: {dist:.3f})'     ))          # Get the closest AABB     min_pt = aabb_mins_np[obj_id]     max_pt = aabb_maxs_np[obj_id]          # Create wireframe for AABB     x0, y0, z0 = min_pt     x1, y1, z1 = max_pt     box_x, box_y, box_z = [], [], []          # Bottom     box_x.extend([x0, x1, x1, x0, x0, None])     box_y.extend([y0, y0, y1, y1, y0, None])     box_z.extend([z0, z0, z0, z0, z0, None])     # Top     box_x.extend([x0, x1, x1, x0, x0, None])     box_y.extend([y0, y0, y1, y1, y0, None])     box_z.extend([z1, z1, z1, z1, z1, None])     # Pillars     box_x.extend([x0, x0, None, x1, x1, None, x1, x1, None, x0, x0, None])     box_y.extend([y0, y0, None, y0, y0, None, y1, y1, None, y1, y1, None])     box_z.extend([z0, z1, None, z0, z1, None, z0, z1, None, z0, z1, None])                     fig.add_trace(go.Scatter3d(         x=box_x, y=box_y, z=box_z,         mode='lines',         line=dict(color=color, width=4),         name=f'Closest AABB {obj_id}',         hoverinfo='skip'     ))  fig.update_layout(     title=\"BVH Closest Point Query (Sphere Mesh)\",     scene=dict(         xaxis=dict(showbackground=False, visible=False),         yaxis=dict(showbackground=False, visible=False),         zaxis=dict(showbackground=False, visible=False),         aspectmode='data'     ),     margin=dict(l=0, r=0, b=0, t=40) ) fig.show()"},{"location":"examples/spatial/ds_bvh/#bounding-volume-hierarchy-bvh-example","title":"Bounding Volume Hierarchy (BVH) Example\u00b6","text":"<p>This notebook demonstrates how to construct and query a Bounding Volume Hierarchy (BVH) on the GPU using <code>geocutool</code>. A BVH accelerates spatial queries like collision detection and ray tracing by organizing geometric primitives into a tree structure.</p>"},{"location":"examples/spatial/ds_bvh/#steps-in-this-example","title":"Steps in this example:\u00b6","text":"<ol> <li>Mesh Generation: We create a sphere mesh using Open3D and convert it to PyTorch tensors.</li> <li>AABB Computation: We calculate the Axis-Aligned Bounding Box (AABB) for each triangle in the mesh.</li> <li>BVH Construction: We build the BVH over these AABBs using <code>geocutool.data_structure.BVH</code>.</li> <li>Intersection Query: We define a single query triangle, compute its AABB, and perform a broad-phase collision query against the BVH to find all overlapping bounding boxes.</li> <li>Visualization: We render the base mesh, the query triangle, the intersected triangles, and their respective AABB wireframes using Plotly.</li> </ol>"},{"location":"examples/spatial/ds_bvh/#example-2-bvh-self-intersection","title":"Example 2: BVH Self-Intersection\u00b6","text":"<p>In this example, we generate $N=10$ random AABBs (pure bounding boxes, no triangles) and check for self-intersections using the base BVH class.</p>"},{"location":"examples/spatial/ds_bvh/#3-ray-aabb-intersection-queries","title":"3. Ray-AABB Intersection Queries\u00b6","text":"<p>We can also use the BVH to accelerate ray casting! Let's cast 5 random rays into the scene and visualize all the AABBs they intersect.</p>"},{"location":"examples/spatial/ds_bvh/#example-4-closest-aabb-query-query_point","title":"Example 4: Closest AABB Query (<code>query_point</code>)\u00b6","text":"<p>In this example, we generate some query points (some inside the mesh, some outside) and use the <code>query_point</code> method to find the closest leaf AABB in the BVH for each point. We then visualize the query points and their corresponding closest AABBs, matching their colors so you can easily see which AABB belongs to which query point!</p>"},{"location":"examples/spatial/ds_kdtree/","title":"KDTree Data Structure Example","text":"In\u00a0[1]: Copied! <pre>from conquer3d.data_structure import KDTree\n\nimport torch\nimport numpy as np\nimport plotly.graph_objects as go\nimport open3d as o3d\nimport time\n</pre> from conquer3d.data_structure import KDTree  import torch import numpy as np import plotly.graph_objects as go import open3d as o3d import time <pre>Jupyter environment detected. Enabling Open3D WebVisualizer.\n[Open3D INFO] WebRTC GUI backend enabled.\n[Open3D INFO] WebRTCWindowSystem: HTTP handshake server disabled.\n</pre> In\u00a0[2]: Copied! <pre>sphere_mesh = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=20)\npoints = np.asarray(sphere_mesh.vertices, dtype=np.float32)\npoints_tensor = torch.from_numpy(points)\n\nprint(points_tensor.shape)\n</pre> sphere_mesh = o3d.geometry.TriangleMesh.create_sphere(radius=1.0, resolution=20) points = np.asarray(sphere_mesh.vertices, dtype=np.float32) points_tensor = torch.from_numpy(points)  print(points_tensor.shape) <pre>torch.Size([762, 3])\n</pre> In\u00a0[3]: Copied! <pre>points_tensor = points_tensor.to('cuda')\n\nkdtree = KDTree(points_tensor)\n\nquery_index = 250\nquery_point = points_tensor[query_index].unsqueeze(0)\nk = 25\n\nstart_time = time.time()\ndists, indices = kdtree.query(query_point, k, exclude_self=True)\nend_time = time.time()\n\nprint(f\"Query Time: {end_time - start_time:.6f} seconds\")\n\nprint(\"Query Point:\", query_point)\nprint(\"Distances:\", dists)\nprint(\"Indices:\", indices)\n</pre> points_tensor = points_tensor.to('cuda')  kdtree = KDTree(points_tensor)  query_index = 250 query_point = points_tensor[query_index].unsqueeze(0) k = 25  start_time = time.time() dists, indices = kdtree.query(query_point, k, exclude_self=True) end_time = time.time()  print(f\"Query Time: {end_time - start_time:.6f} seconds\")  print(\"Query Point:\", query_point) print(\"Distances:\", dists) print(\"Indices:\", indices) <pre>Query Time: 0.000082 seconds\nQuery Point: tensor([[0.2753, 0.8474, 0.4540]], device='cuda:0')\nDistances: tensor([[0.0195, 0.0195, 0.0246, 0.0246, 0.0424, 0.0424, 0.0455, 0.0455, 0.0777,\n         0.0777, 0.0952, 0.0952, 0.0979, 0.0979, 0.1076, 0.1076, 0.1134, 0.1134,\n         0.1196, 0.1196, 0.1596, 0.1596, 0.1731, 0.1731, 0.1818]],\n       device='cuda:0')\nIndices: tensor([[251, 249, 290, 210, 209, 211, 291, 289, 252, 248, 212, 208, 170, 330,\n         292, 288, 171, 169, 331, 329, 168, 172, 247, 253, 213]],\n       device='cuda:0')\n</pre> In\u00a0[4]: Copied! <pre>query_point_np = query_point.cpu().numpy()[0]\nneighbor_points_np = points_tensor[indices[0]].cpu().numpy()\nmask = torch.ones(points_tensor.size(0), dtype=torch.bool, device=points_tensor.device)\nmask[indices[0]] = False\nnon_neighbor_points_np = points_tensor[mask].cpu().numpy()\n\nprint(\"Neighbor Points:\\n\", neighbor_points_np.shape)\nprint(\"Non-Neighbor Points:\\n\", non_neighbor_points_np.shape)\n\nfig = go.Figure()\n\nfig.add_trace(\n    go.Scatter3d(\n        x=non_neighbor_points_np[:, 0], \n        y=non_neighbor_points_np[:, 1], \n        z=non_neighbor_points_np[:, 2], \n        mode='markers', \n        marker=dict(size=5, color='blue'), \n        name='Non-Neighbors'))\nfig.add_trace(\n    go.Scatter3d(\n        x=neighbor_points_np[:, 0], \n        y=neighbor_points_np[:, 1], \n        z=neighbor_points_np[:, 2], \n        mode='markers', \n        marker=dict(size=5, color='red'), \n        name='Neighbors'))\nfig.add_trace(\n    go.Scatter3d(\n        x=[query_point_np[0]], \n        y=[query_point_np[1]], \n        z=[query_point_np[2]], \n        mode='markers', \n        marker=dict(size=10, color='green'), \n        name='Query Point'))\n\nfig.update_layout(\n    title='KD-Tree Nearest Neighbors', \n    scene=dict(\n        xaxis_title='X', \n        yaxis_title='Y', \n        zaxis_title='Z'\n    )\n)\n\nfig.show()\n</pre> query_point_np = query_point.cpu().numpy()[0] neighbor_points_np = points_tensor[indices[0]].cpu().numpy() mask = torch.ones(points_tensor.size(0), dtype=torch.bool, device=points_tensor.device) mask[indices[0]] = False non_neighbor_points_np = points_tensor[mask].cpu().numpy()  print(\"Neighbor Points:\\n\", neighbor_points_np.shape) print(\"Non-Neighbor Points:\\n\", non_neighbor_points_np.shape)  fig = go.Figure()  fig.add_trace(     go.Scatter3d(         x=non_neighbor_points_np[:, 0],          y=non_neighbor_points_np[:, 1],          z=non_neighbor_points_np[:, 2],          mode='markers',          marker=dict(size=5, color='blue'),          name='Non-Neighbors')) fig.add_trace(     go.Scatter3d(         x=neighbor_points_np[:, 0],          y=neighbor_points_np[:, 1],          z=neighbor_points_np[:, 2],          mode='markers',          marker=dict(size=5, color='red'),          name='Neighbors')) fig.add_trace(     go.Scatter3d(         x=[query_point_np[0]],          y=[query_point_np[1]],          z=[query_point_np[2]],          mode='markers',          marker=dict(size=10, color='green'),          name='Query Point'))  fig.update_layout(     title='KD-Tree Nearest Neighbors',      scene=dict(         xaxis_title='X',          yaxis_title='Y',          zaxis_title='Z'     ) )  fig.show() <pre>Neighbor Points:\n (25, 3)\nNon-Neighbor Points:\n (737, 3)\n</pre>"},{"location":"examples/spatial/ds_kdtree/#kdtree-data-structure-example","title":"KDTree Data Structure Example\u00b6","text":"<p>This notebook demonstrates how to use the <code>KDTree</code> data structure provided by <code>geocutool</code> for efficient nearest neighbor queries on a point cloud. We'll start by importing the necessary libraries.</p>"},{"location":"examples/spatial/ds_kdtree/#1-generate-a-point-cloud","title":"1. Generate a Point Cloud\u00b6","text":"<p>We generate a 3D point cloud by sampling the vertices of a sphere mesh created using Open3D. The resulting numpy array is converted to a PyTorch tensor for processing.</p>"},{"location":"examples/spatial/ds_kdtree/#2-build-kdtree-and-query","title":"2. Build KDTree and Query\u00b6","text":"<p>Next, we transfer our point cloud tensor to the GPU, build a <code>KDTree</code> using <code>geocutool</code>, and query the 25 nearest neighbors for a specific point in the cloud. We use <code>exclude_self=True</code> to exclude the query point itself from the results.</p>"},{"location":"examples/spatial/ds_kdtree/#3-visualize-the-results","title":"3. Visualize the Results\u00b6","text":"<p>Finally, we visualize the query point, its neighbors, and the rest of the point cloud using Plotly. Neighbors are shown in red, the query point in green, and the non-neighbors in blue.</p>"}]}